# 전서_토론후 — brain 재구축 4자 회담 결론 (2026-08-13)

> - 참가자: codex · ck · grok · cc (패널 4) + 사회자(이 세션). 3라운드 허브 릴레이.
> - 재료: `전서.md`(코드 실측 전수) 기준. 회담 원문: `~/.claude/debate/20260813-1345-jeonseo/r{1,2,3}-*.md`
> - 이 문서가 전서 수정과 0.3.0 재구축의 결정 원장이다. 아래 "확정"은 패널 합의 + 사회자 판정.

---

## 0. 총평 — 재구축의 축

- 병의 진단(4자 일치): 기능 부족이 아니라 **전량 주입(recall) · 전량 위임(scribe) · 전량 사본(Router·미러)** 이 컨텍스트를 먹는 구조.
- 처방의 축: **선별(8KiB 상한) · 직접(PM 쓰기) · 단일 원본(레포=프로젝트 문서, 볼트=기억)**.
- 15개 쟁점 중 "빼는 쪽"이 12곳에서 이겼다. 새 인프라(큐·cron·그래프·org repo·전역 레지스트리)는 전부 기각.

## 1. 쟁점별 결론 (15)

| # | 쟁점 | 결론 | 표결 |
|---|---|---|---|
| 1 | projects 문서 위치 | **레포로 이전** — PRD·ARCHITECTURE·CODE_CONVENTION·RUNBOOK·feature·ADR·P_POLICY·COMPLIANCE 전부 레포 `docs/`. 볼트 원본 금지, 포인터만 | 전원 |
| 2 | 회사 정보 | **볼트 공통층 유지·축소** — 정책(`*policies*`)·머신 구성·네트워크만. recall 기본 주입 제외, 무인 쓰기 금지 유지 | 4:1 (codex org-repo 소수) |
| 3 | PM role | **유지하되 scribe 폐지** — 볼트 잡무(frontmatter·인덱스·Progress)는 PM 직접 Edit(CAS). 위임 = coder·verifier·researcher만 | 4:1 |
| 4 | 기억 외부화 | **유지** — 내장 메모리 대신 볼트 + 선별 주입 | 전원 |
| 5 | IA.md 필수 | **기각** — 필요 시 ARCHITECTURE 한 절. "pre-created ≠ evidence" | 전원 반대 (사용자 원주장과 배치 — §4 소수의견) |
| 6 | 에이전트 4종·worktree | **4종 유지**(disallowedTools·isolation이 실물 이빨). worktree 기본값 = CC `isolation: worktree`, **Orca가 workspace worktree를 연 세션은 isolation off** — 격리 주인 1개 | 전원 / 3:2 |
| 7 | sr 토큰 폭식 | **recall 수술 확정** — §2.1 스펙 | 전원 |
| 8 | dreaming 큐 | **큐 기각**(락+증분 커서가 이미 유실 0). sc ⑧ 동기 실행 제거 — "dreaming 권장 — 미통합 N건" 1줄 제안, 실행은 사용자 승인 | 전원 |
| 9 | CLAUDE.local.md | **잔존·1키 축소** — `vault-root:`만. Router 14행 폐지(드리프트 2건 실측·심링크 훅이 대체) | 전원 |
| 10 | brain/docs 노후 | **10종 → 4종 병합** — §2.2 | 전원 |
| 11 | feature 명명 | **규약 1줄** — "도메인 내 유일 식별 명칭, 범용 동작어(process·handle·manage) 금지". 린트 강제 안 함. (참고: summarize는 가상 프로젝트 vidnote의 정당한 기능명이었음) | 전원 |
| 12 | 문서 오염 | **3종 확정** — ①doc-templates HTML 주석 스켈레톤 삭제 ②Handoff `Docs draft`에 금지 1줄 ③validate.sh 신규 계열: `\[Image #[0-9]+\]`·`<!--` finding(코드펜스 스킵) | 전원 |
| 13 | COMPLIANCE 출처 | **필수화** — 본문 `## Legal Sources` 표. §2.3 | 전원 |
| 14 | Data→Information | **인프라 없이** — 노트 3절(Trigger/Insight/Why) 유지 + validate 3절 부재 finding · `brain-recall <query>` read-only 스크립트 · 레포 AGENTS.md 5줄 안내. 그래프·벡터 기각 | 전원 (스크립트 3:1) |
| 15 | 정책 3계층 | **기존 배치 명문화** — 회사=공통층 `*policies*` · 프로젝트=레포 `docs/P_POLICY.md` · 기능=feature `§Rules`. 우선순위는 **현행 유지(추상 우선)** + 기록된 override(§2.4) | 4:1 (grok 순수 현행 소수) |

## 2. 확정 스펙

### 2.1 recall 수술 (쟁점 7)
- 주입 범위: **채택 세션 awk 요약 + 현재 프로젝트 `p_memory/_index.md`** (ss는 인덱스만). 공통층·neocortex·docs TOC = "N건·경로" 1줄 포인터.
- 하드 상한 **8KiB** + fail-visible 절단 보고(파일 수·바이트·잘린 수 상시 출력). 16KiB 기각 — 인덱스 15,406B 실측 기준 절단 압력이 사라짐(cc).
- 핫패스 질의 랭커 기각(0.2.0 폐기 전과). 질의는 on-demand `brain-recall`로.
- 기각된 가설: "Router가 canon 153KB를 자동 주입"(grok R1) — hooks.json 실측으로 반증(자동 로드는 CLAUDE.local.md 1,802B뿐), grok 스스로 철회. Router 폐지 근거는 드리프트 2건으로 충분.

### 2.2 canon 재구성 (쟁점 10)
- `vault-tree.md`(단축) · `memory.md`(sessions-note+knowledge+knowledge-escalate+memory-control 4합병) · `project-docs.md`(catalog+templates+convention 3합병) · `git-convention.md`(유지).
- `security-audit.md`는 canon 제외 — verifier 절차 문서로 `brain/agents/` 옆 이동(파일 유지).
- 각 문서 **첫 줄에 소비자 명시**(어느 스킬·에이전트가 Read하는지) — 자기기술 vs 호출자 불일치 3건 실측의 재발 방지.
- ROADMAP.md 소멸(잔여 항목은 티켓으로). codex의 신규 어휘 4계약(SCHEMAS 등)은 이행 비용으로 기각.

### 2.3 COMPLIANCE 스키마 (쟁점 13)
- 위치: 레포 `docs/business/COMPLIANCE.md` (grok 이의 채택 — Q-H 표 누락 보정).
- 본문 필수 절 `## Legal Sources` 표 — 열: `규범 | 구분(법률/시행령/시행규칙/고시) | 상태(시행중/제정예정/개정예정/입법예고) | 시행·적용일 | 적용여부(적용/일부/비적용/검토중) | 적용근거 | 조문 | 하위법령 | 확인일 | 근거 URL`.
- frontmatter 신키 금지(KJP-89 warn 42건 교훈). 기계 검사 = "COMPLIANCE 존재 시 §Legal Sources 부재 = finding" 1건. 셀 내용 판단은 PM 몫. 1차 출처 도구: korean-law MCP.
- `적용=비적용`도 행으로 남긴다(왜 아닌지가 Insight).

### 2.4 정책 충돌 (쟁점 15)
- 방향: **현행 유지** — `*policies* > P_POLICY > PRD NFR > PRD > feature §Rules`, "the concrete never beats the abstract".
- 예외 절차: 하위가 상위를 벗어나야 하면 **override 레코드**(상위 정책 ID · 사유 · 만료일)를 남기고 close 리포트에 보고. 침묵 오버라이드 금지.
- grok 소수의견: override 절차 자체 반대(만료 검사 기계 부재 — 예외는 상위 개정으로만).

### 2.5 원본 위치 지도 (쟁점 1·2·15 통합)
| 대상 | 원본 | 비고 |
|---|---|---|
| PRD·ARCHITECTURE·CODE_CONVENTION·RUNBOOK·feature·ADR·P_POLICY·COMPLIANCE | 레포 `docs/` | 멀티레포 = 주 레포 1곳, 볼트 원본 금지·포인터만 |
| API_SPEC 볼트 미러 | **폐기** | 예외 자체가 드리프트 원천 |
| 회사 정책 | 볼트 공통층 `*policies*` | recall 기본 제외 · 무인 쓰기 금지. 접근통제 요구 실측 시 org repo 재론 |
| 머신 구성·네트워크 | 볼트 공통층 `machines/`·`network/` | 승격·recall 완전 제외 · 쓰기는 onboard step 6 실측만. 교차 머신 뷰가 존치 근거(cc). 머신 로컬 파일 기각 |
| 도구 설치 표면 | `tools_root`(opt-in·gitignore) | 현행 유지 — 31KB vs 3KB drift가 분리 근거 |
| 세션(hippocampus)·지식(p_memory·neocortex) | 볼트 | 유일 잔존 코어 |

### 2.6 이전 컷 필수 조건 (cc 이의 채택)
- 문서 레포 이전 시 **검사기 3종이 0-스캔 무음 사망**: validate.sh 8계열(docs frontmatter)·9계열(ADR 원장)·value-axis-drift.sh(스코프 `NNN_*/docs/**`).
- 결정: **재조준** — 세 검사기를 레포 `docs/` 대상으로 실행 가능하게(경로 인자 기반 스코프 옵션) 이전과 **같은 컷**에 랜딩. 무결정 금지.

### 2.7 스킬·구조 변경 요약 (0.3.0 방향)
- ss/sr/sl/sh/sc 5동사 유지. sr·sh·sc에서 scribe 위임 제거 → PM 직접 Edit(CAS 규율·경로 규약은 그대로).
- sc: ⑧ 동기 dreaming 제거 → 커서 이후 미통합 카운트 1줄 제안 + 승인 실행. 승격 ①은 유지(유일 호출자).
- dreaming: 락·증분 커서·ff-only·제안-승인 모델 유지. 큐 없음.
- init: CLAUDE.local.md = vault-root 1키. Router 생성 제거. 볼트 스캐폴드에서 프로젝트 docs/ 트리·스텁 5종 제거(문서는 레포로) — 볼트 쪽은 p_memory·hippocampus·허브만.
- onboard: 문서 매핑 대상이 레포 `docs/`로 변경. step 6(환경 체크)은 유지 — 쓰기는 공통층 machines(장비 사실)·tools_root(설치 표면)만.
- 신규: `brain-recall <query>`(read-only grep top-N, bash 3.2·의존성 0) · 레포 AGENTS.md 5줄(볼트 소비 안내).
- validate.sh: +오염 패턴 계열 · +지식 3절 부재 계열 · +레포 docs 스코프 재조준. selftest 픽스처 동반.
- 에이전트: worker(scribe 절 삭제)·coder·verifier·researcher + KERNEL 유지. force-delegate.sh 영구 미등록(jq 결함 실측), org-guard.sh는 opt-in 유지.

## 3. 기각 목록 (재론 시 이 표부터)
- dreaming 큐·cron 스케줄 · IA.md 필수 스텁 · 핫패스 질의 랭커 · 그래프/벡터 인프라(mem0 그래프 제거 실측·cognee는 인프라 제품) · frontmatter 10필드 확장 · `~/.config/brain/*` 전역 레지스트리 · 별도 org policy repo · 정책 폴더 부활(KJP-79) · feature 명명 린트 강제 · codex 신규 어휘 canon 4계약 · VAULT.md 진입점(AGENTS.md와 이중화).

## 4. 소수의견 기록
- 사용자 원주장 "IA.md 필수" — 패널 전원 반대로 기각. 단 최종 결정권은 사용자에게 있음: 필수로 되살리려면 "빈 스텁 금지, 트리거 생성(UI 프로젝트 한정)" 조건 권장.
- codex: 회사 정책 org repo 원본(승인·리뷰·롤백) — 1인 운영 근거로 기각, 팀 전환 시 재론 조건 부착.
- grok: 정책 override 절차 반대(순수 추상 우선) — 채택된 (c)의 만료일 필드는 검사 기계가 없다는 지적은 유효, 운영에서 확인.
- ck: PM 직행 "5줄 이하" 정량 기준 — "임계가 새 드리프트"(grok) 반박으로 기각.

## 5-A. 회담 후 사용자-사회자 확정 (2026-08-13 오후) — 볼트 구조

> 회담이 다루지 않은 볼트 물리 구조를 레퍼런스 6종 실측 조사 후 확정. 근거: `scratchpad/ref-{claude-tools,cognee-llmwiki,graphify-autoresearch}.md`

### 확정 구조 — 2층 평탄
```
<vault>/
  sessions/   # 구 hippocampus — git 밖, 휘발
  memory/     # 전 지식 단일 트리 — 폴더 계층 없음
```
- **projects/NNN_slug/ 폴더 계층 폐지** — docs 레포 이전 후 p_memory 하나만 남는 래퍼라서. 번호 밴드도 폐지.
- **org 공통층 폴더 폐지** — 볼트 루트가 이미 org 경계(techtainment/beafter 볼트 분리). 회사 지식도 memory 노트.
- **neocortex 폴더 폐지** — 아래 scope 모델이 대체.

### scope/kind frontmatter 모델
- 구분축은 폴더가 아니라 frontmatter 2키: `scope: [kjp, ...]`(프로젝트 식별자 목록) + `kind: fact|policy`.
- kind 어휘는 2종으로 확정(2026-08-13 사용자 확정) — 판별: **위반할 수 있으면 policy, 틀릴 수 있으면 fact**. 생략 시 기본값 fact. 교훈·취향·절차는 신설 없이 둘로 환원(교훈·절차→fact, 취향→policy). 제3종은 실측 필요가 나올 때 validate 어휘 1줄 추가로.
- 승격 ①(sc): `memory/<topic>.md` 생성 + scope 1개. 승격 ②(dreaming): **scope에 식별자 추가** — 구 git mv + 링크 재배선 소멸. scope 2개 이상 = 교차 지식(구 neocortex 상당). `scope: [org]` = 회사 지식(구 공통층 상당).
- scope 값은 **명시 선언만**(CLAUDE.md brain config `project:` 키) — 경로·폴더 파생 금지. 근거: claude-mem이 경로 파생 키로 monorepo·worktree·동명 폴더 파편화(정본 이슈 4건).
- `kind: policy` = 규범 마커 — 우선순위 판정(`org policy > P_POLICY > feature §Rules`)의 물리 앵커. 회담 §2.4의 "공통층 *policies* glob"을 대체.
- `_index.md` 1파일 공용, 줄 형식 `- [[stem]] (scope) — summary` — recall이 인덱스 grep만으로 scope 필터.
- recall 기본 필터 = scope에 현재 프로젝트 포함(org는 명시 요청 시) — 회담 "공통층 recall 제외"를 필터로 충족. 무인 쓰기 금지는 스킬 규율로 유지.

### 지식 일원화 경계
- **코드 PR과 함께 리뷰돼야 하면 → 레포 `docs/`. 세션에서 배운 것이면 → 볼트 `memory/`.** 레포 밀착 교훈도 볼트(scope로 표시) — 레포에 지식 파일 금지.
- 레포 안내(최종 확정 2026-08-13): **CLAUDE.md·AGENTS.md 둘 다 실작성**(사용자 확정 — import·심링크 아님). 내용 = brain config + 필수 규칙 + 볼트 소비 안내, TOC형·얇게. 두 파일의 공용 블록은 마커로 동일하게 쓰고 갱신은 항상 스킬이 두 파일 동시에 — 드리프트는 두 블록 바이트 대조 검사로 기계가 잡는다(검사기는 0.3.0에서 신규 작성).
- **구현 전제(사용자 확정 2026-08-13): 훅·스크립트 전부 백지 재설계** — 기존 validate.sh·kernel-check.sh 등의 코드 재사용을 전제하지 않는다. 본 문서의 기존 스크립트 언급은 요구사항의 출처 표시일 뿐, 구현은 0.3.0 설계에서 새로 짠다.
- 이유: 승격·dreaming·validate·recall 파이프가 저장소 1개만 순회.

### 회담 확정에 대한 수정(supersede) 목록
- §2.4 정책 앵커: 공통층 glob → `kind: policy` + `scope: [org]`.
- Q-3(머신·환경 사실): "공통층 machines/ 잔존" → memory 노트 `scope: [org]`(또는 머신 토픽)로 재해석. 파이프라인 제외 원칙은 동일.
- 승격 ② 메커니즘: git mv + aliases + 링크 재배선 → scope 확장 1줄 편집.
- vault-paths.sh 축 리졸버·`.brain-paths`: 폴더 2개 고정이라 대부분 소멸.
- canon 병합(§2.2)의 vault-tree.md: 내용이 이 절로 대체 수준 축소.
- 레퍼런스 실측 요약: 지식이 레포를 따라가는 형(graphify·autoresearch) / 전역+자동 키 파편화 실패(claude-mem) / 격리 없음(claude-obsidian) / dataset 네임스페이스(cognee — 인프라 규모) / 토픽 폴더+명시 promote(llm-wiki) → 채택안은 "전역 볼트 + 명시 scope + 토픽 파일명" 조합.
- CLAUDE.md 원칙: TOC처럼 얇게 — brain config 키 + 필수 규칙 몇 줄 + 포인터. PM role 산문은 canon으로 내림.

## 5. 후속 작업 (실행 순서)
1. 전서.md에 본 결론 반영(0.3.0 설계 절 추가) — 이 문서가 근거.
2. 볼트 리셋 재실행 여부 사용자 결정(현재 복원된 상태 — tar 백업 후 삭제 권장).
3. 0.3.0 구현 티켓 분해: ①recall 수술+8KiB ②scribe 폐지 리라이트(sr/sh/sc) ③sc-dreaming 분리 ④init/onboard 개편(1키·레포 docs) ⑤canon 4종 병합 ⑥validate 확장+재조준 ⑦brain-recall+AGENTS.md ⑧COMPLIANCE 템플릿.
4. 이전 컷 규칙: ⑥은 ①~④와 같은 릴리스에 묶는다(§2.6).
