# kj-plugins 전서 (최종본 · 2026-08-13)

> - 레포 루트 = `/Users/nwkim/ClaudeCode/kj-plugins` — 이하 경로는 이 루트 기준 상대 표기.
> - 근거 = 현재 워킹트리의 파일 내용만. git 히스토리·볼트·과거 문서 미참조, 추측 없음. 파일에 없으면 "코드에 없음"으로 기재.
> - 재료 = 전체 초안 1 + 부문별 실측 서베이 4 를 교차 대조 후, 쟁점 7건을 파일 직독·명령 실측으로 재판정해 병합.
> - 커버리지 = `find . -path ./.git -prune -o -type f -print` 실측 62파일 전수 대조 — 재료 미등장 파일 2건(`.DS_Store` 2개) 보충 완료.

---

## [레포 구조]

- 정체: Claude Code 플러그인 마켓플레이스 레포 (`README.md:1-3`) — 등록 플러그인은 `brain` 1개.
- 최상위 구성 (`.git` 제외, 62파일 실측):
  - `.claude-plugin/marketplace.json` — 마켓플레이스 매니페스트.
  - `brain/` — 유일한 플러그인 본체.
    - `brain/.claude-plugin/plugin.json` — 플러그인 매니페스트 (version 0.2.5).
    - `brain/skills/` — 스킬 8종 (`ss`·`sr`·`sl`·`sh`·`sc`·`init`·`onboard`·`dreaming`, 각 `SKILL.md`) + `_session-shared/` 공유 참조문서 5종.
    - `brain/agents/` — 에이전트 프로필 4종 (`worker`·`coder`·`verifier`·`researcher`).
    - `brain/hooks/` — `hooks.json` + 훅 스크립트 3종 (활성 1 · opt-in 2).
    - `brain/scripts/` — 셸 스크립트 5종 (리졸버 1 · 린터 1 · 셀프테스트 1 · 드리프트 검출기 2).
    - `brain/docs/` — 규약(canon) 문서 10종.
    - `brain/README.md` (469줄) · `brain/CHANGELOG.md` (519줄) · `brain/ROADMAP.md` (50줄).
  - `docs-samples/` — 가상 프로젝트 vidnote 로 채운 문서 통합안 시안 스냅샷 12파일. 자체 배너가 "2026-07-25 스냅샷, 현행 canon 아님"을 명시 (`docs-samples/README.md:8`).
  - `README.md` — 설치법·플러그인 표·레포 레이아웃·validate.sh 사용법.
  - `CLAUDE.md` — 프로젝트 공유 brain config + PM role + Worker profiles (git 추적).
  - `CLAUDE.local.md` — vault-root + Router (git 미추적 — `.gitignore:5`).
  - `.gitignore` — 5항목 (아래 [설정·매니페스트]).
  - `.artifact/brain-0.2.0.html` — 188,698 B 단일 HTML, git 추적. 0.2.0 설계 전서 (아래 [루트 문서]).
  - `.impeccable/hook.cache.json` — 2,241 B, git 미추적 (`.git/info/exclude:9` — `.gitignore` 아님). impeccable(프론트엔드 스킬) 훅의 세션별 편집 캐시 — 구조 `{version, sessions: {<uuid>: {updatedAt, files: {<path>: {editCount, findings[]}}}}}`, 세션 8건, 대상 대부분 `.artifact/brain-0.2.0.html`·scratchpad HTML, findings 는 1건 외 전부 빈 배열.
  - `.claude/worktrees/` — 빈 디렉토리 (`ls -la` 실측: 파일 0건), `.gitignore:11` 무시 대상.
  - `.DS_Store` 3개 — `./.DS_Store` · `brain/.DS_Store` · `brain/hooks/.DS_Store` (셋 다 Apple Desktop Services Store, 6,148 B — macOS Finder 부산물, 내용 무의미. 루트 것만 `.gitignore:2` 로 커버).

### brain/docs/ — 규약 문서 10종 (플러그인이 배포하는 canon)

| 문서 | 정의하는 것 (파일 자체 기준) |
|---|---|
| `brain/docs/vault-tree.md` | 볼트 트리·층(raw/wiki/schema)·쓰기 권한 표·트리 축 매니페스트(`.brain-paths`)·공통층 `*policies*` 규칙·예약 번호 밴드(001–899 프로젝트, 9xx 인프라)·네이밍 |
| `brain/docs/sessions-note-convention.md` | 세션 노트 스키마 — 파일명=식별자, 필수 5키(`status`·`project`·`updated`·`related_ticket`·`cc_session_ids`), status 3값(`active\|parked\|done`), 폐기 키(`uid`·`created`·`writer`·`tags`·`session_type`·`result`), Progress 상태 접미사 어휘 |
| `brain/docs/knowledge-convention.md` | 메모리 노트 양식 — 4키(`summary`·`updated`·`related`·`aliases`) + 3절(`## Trigger`/`## Insight`/`## Why`), 파일명 `<pp>_<slug>`/`NEO-<slug>`, `related` 인용부호 wire format, frontmatter 파싱 규칙, `_index.md` 손 갱신 규칙 |
| `brain/docs/knowledge-escalate-convention.md` | 승격 2단 위상 — ① `sc`: hippocampus→p_memory · ② `dreaming`: p_memory→neocortex (git mv + aliases + projects 3줄). 점수·가중치·임계값 없음 |
| `brain/docs/memory-control-convention.md` | Handoff 고정 포맷(Done/Mistake/Learned/Outputs/Risks/Next/Ask + optional Docs draft), recall 규칙, dreaming 가드레일, scribe 거버넌스(PM 중재·직행 금지) |
| `brain/docs/git-convention.md` | 커밋 타입 11종(Conventional Commits)·표면별 표기(티켓/커밋/PR/브랜치/워크트리)·워크트리 통합 순서(`-d` 만, `-D` 금지)·PR 게이트 리모트 실측(rulesets+classic 둘 다)·Share scope·PM 커밋 예외 |
| `brain/docs/project-docs-convention.md` | docs frontmatter v2(`status` 필수 + `updated`; ADR 만 `id`; API_SPEC 미러 키; optional `history`), kind←path 행렬, Value Axes 표(가격·티어 원본=PRD §BM), 사전 스텁 5종 규칙, Policy 시스템(`P_POLICY.md` `## POL-NNN`), ID 발급(`<PREFIX>-ADR-0000N`+`next_id`), 문서 충돌 우선순위 5단 |
| `brain/docs/doc-catalog.md` | 어떤 문서를 언제 만드나 — kind·owner(브리프 라우팅 라벨)·위치·트리거·tier(universal/situational) 선정표. 사전 스텁 5(PRD·ARCHITECTURE·CODE_CONVENTION·RUNBOOK·THREAT_MODEL) — BUSINESS 는 폐기 (`brain/docs/doc-catalog.md:64`) |
| `brain/docs/doc-templates.md` | 본문 템플릿 — DESIGN·MILESTONE 2종만. 다른 kind 템플릿 금지 |
| `brain/docs/security-audit.md` | verifier 가 보안 브리프를 받았을 때 읽는 절차 — 모드(daily 신뢰 8/10 · comprehensive 2/10 · 범위 플래그), Phase 0–13, report-only |

- `versioning-convention.md` 는 **brain/docs/ 에 없음** (find 실측 — 10파일 목록에 부재; `brain/CHANGELOG.md` 0.1.8 항목이 git-convention.md 로의 흡수를 기록). 현행 `CLAUDE.local.md` Router 는 이 이름을 여전히 가리킴 → 아래 [설정·매니페스트] 드리프트 참조.

### docs-samples/ — 시안 스냅샷 12파일 (canon 아님)

- `docs-samples/README.md` — 배너: 2026-07-25 스냅샷. 이후 0.2.0 랜딩이 ①정책 폴더형→`P_POLICY.md` ②경로 중첩(`develop/`) ③API_SPEC 생성 주체(dreaming→PM 위임 워커) 3축에서 이를 지나쳤음을 명시. 현행 정본 = `brain/docs/` + `.artifact/brain-0.2.0.html`.
- 사전 스텁 시안 6: `docs-samples/PRD.md` · `ARCHITECTURE.md` · `CODE_CONVENTION.md` · `RUNBOOK.md` · `THREAT_MODEL.md` · `BUSINESS.md` (BUSINESS 는 현행 canon 에서 폐기).
- 트리거 생성 샘플 2: `docs-samples/DESIGN.md` · `docs-samples/MILESTONE.md`.
- 기능 세트 시안 3: `docs-samples/feature/summarize/FRD.md` · `TDC.md` · `policy/VDN-POL-00001.md` (FRD·TDC 분리형·정책 폴더형 — 현행 canon 은 단일 feature 파일 + `P_POLICY.md`, `brain/docs/project-docs-convention.md:122-148`).

---

## [플러그인]

- 마켓플레이스: `.claude-plugin/marketplace.json` (401 B) — 필드 전수: `name: "kj-plugins"` · `description: "koreanjoker's Claude Code plugin marketplace."` · `owner: {"name": "koreanjoker"}` · `plugins` 1개: `{name: "brain", source: "./brain", description: "Memory harness for Claude Code: session capture → knowledge promotion → recall. Vault conventions, session lifecycle skills, and a PM orchestration model."}`. 컴포넌트(skills/agents/hooks) 경로 등록 없음 — 소스 디렉토리만 지정.
- 플러그인 **brain**: `brain/.claude-plugin/plugin.json` (311 B) — 필드 전수: `name: "brain"` · `displayName: "Brain — Second-Brain Harness"` · `version: "0.2.5"` · `description`(marketplace 와 동일 문자열) · `author: {"name": "koreanjoker"}`. `commands`/`skills`/`agents`/`hooks` 필드 없음 — 컴포넌트는 규약 디렉토리로 암묵 로드.
- 설치 명령 (`README.md:7-10`): `/plugin marketplace add kimnamwook1/kj-plugins` → `/plugin install brain`.
- 버전 주장 (`brain/CHANGELOG.md:3-5`, 0.2.5 · 2026-08-13): 0.2.2~0.2.4 는 레포 버전 문자열이었을 뿐 설치된 적 없음(설치 캐시 최대 0.2.1) — 그 내용 전부가 0.2.5 컷으로 처음 배포된다고 스스로 기재. (설치 캐시는 레포 밖 — [미확인 사항].)

---

## [스킬 전수]

> 공통: 실행자 = PM(메인 세션), 볼트 콘텐츠 쓰기 = `scribe` 워커 위임(단 `sl` 은 쓰기 자체가 없음). VAULT = `CLAUDE.local.md` 의 `vault-root`. 트리 축은 `brain/scripts/vault-paths.sh` 해석 — 경로 하드코딩 금지. `ss`·`sh`·`sc` 는 문두에 "애매하면 5동사(ss/sr/sl/sh/sc) 분기 먼저 제시" 규칙 보유.

### 1. ss — Session Start (`brain/skills/ss/SKILL.md`)
- frontmatter: `name: ss` · `argument-hint: "[project] [title...]"`.
- 하는 일: 새 세션을 `hippocampus/<PREFIX>_YYYYMMDD_<slug>.md` **단일 파일**로 생성(`status: active`), `recall.md` 수행 결과를 `## Recall` 절에 주입. 프로젝트 폴더 없으면 멱등 생성 — PM 은 존재 확인·번호 계산만(`brain_project_dir`·`brain_next_project_num`·`BRAIN_PROJECTS`, 9xx 예약 밴드 제외), 생성은 scribe 위임. PREFIX 는 프로젝트 허브 `_index.md` 의 `PREFIX:` 에서 읽음.
- 입력: 인자(선택) · `CLAUDE.local.md` vault-root(없으면 질문 + `/brain:init` 안내) · CC 세션 id.
- 출력: 세션 파일 경로 + project·vault + recall 파일 수·총 바이트.
- 금지: 재개·목록·파킹 세션 스캔·고지 일절 금지("N개 파킹 존재" 한 줄도 — KJP-43) · `obsidian create` 금지(Write 만, 주체는 scribe) · status 는 `active` 만 · retired 키(`uid`·`created`·`writer`·`tags`) 금지 · 같은 날+같은 slug 자동 접미사 금지(사용자에게 질문) · vault-root 밖 쓰기 금지 · worktree 질문 없음(KJP-42).
- 특이: 0.2.0 이전 구형 파일명(`<PREFIX>-YYYYMMDD-HHMMSS.md`)은 rename 하지 않음 — 두 형태 공존, sr/sl 은 frontmatter 를 읽음.

### 2. sr — Session Resume (`brain/skills/sr/SKILL.md`)
- frontmatter: `name: sr` · `argument-hint: "[project]"`.
- 하는 일: 열린 세션(`parked`+`active`)을 공유 스캔(`active-sessions.md` §1)으로 발견 → §2 awk 요약 추출(전체 Read 금지 — 추출 ~6KB vs 세션 90KB+) → §3 렌더로 후보 제시(1건이면 yes/no) → 사용자 선택 → 채택. frontmatter 만 scribe 위임 갱신: `status:`→`active`(블록 내 해당 줄만), `updated:` 갱신, `cc_session_ids` 앞에 현재 CC id prepend. **Progress 엔트리는 추가하지 않음.** recall 은 필수·**화면 출력 전용**(PM 직접 수행, 파일 미수정).
- 출력: 채택 세션 경로·project·vault·recall 파일 수·바이트. 0건이면 "ss 로 새로 시작" 한 줄 후 정지.
- 금지: 세션 파일 신규 생성 절대 금지 · 쓰는 status 는 `active` 뿐(`done` 금지 — 종료는 sc) · 스캔 루프 인라인 복사 금지(`|| :` 종결자 load-bearing — KJP-41) · 사용자 거절 시 ss 자동 전환 금지.
- 특이: `[active]` 후보도 목록 유지(sh 없이 끊긴 세션의 복구 경로가 sr).

### 3. sl — Session List (`brain/skills/sl/SKILL.md`)
- frontmatter: `name: sl` · `argument-hint: "[project|all]"`.
- 하는 일: `status: parked|active` 세션을 스캔해 한 줄씩 출력 — `[parked]`/`[active]` 마커 필수(§1 status 컬럼에서 — Progress `(parked)` 접미사 추론 금지, KJP-48), `updated:` 내림차순, `all` 스코프면 `project:` 접두. 이 대화의 세션이 아닌 `[active]` 가 있으면 "sh 로 파킹하라" 한 줄 추가.
- 입력: 인자(선택; 추론 실패 시 추측 금지 — `all` 폴백 후 고지).
- 금지: **완전 read-only** — `Write`·`Edit`·scribe 브리프·frontmatter 터치(`updated:` 조차) 전부 금지("sl 에서 scribe 를 띄우는 것은 버그") · 채택 금지(그건 sr) · 생성 금지(빈 목록에도 ss 자동 전환 금지) · 전체 Read 금지.
- 특이: 5개 세션 스킬 중 유일하게 위임할 쓰기가 없음. description 은 "uid 출력"이라 쓰나 §3 예시·`active-sessions.md` 는 "식별자는 파일명, `uid:` 키 없음" — 용어 불일치 (파일 자체 실측).

### 4. sh — Session Park (`brain/skills/sh/SKILL.md`)
- frontmatter: `name: sh` · argument-hint 없음(코드에 없음).
- 하는 일: 현재 세션 **무조건 파킹** — ① 대상 확정 ② PM 이 pending 마커 grep(`USER-COMMENT|NEEDS USER INPUT|TODO|FIXME|NEEDS CLARIFICATION`; index/_index 두 철자 제외) ③ scribe 1회 위임 — Park Entry(`### YYYY-MM-DD (parked)` + Stopped at/Done/Mistake/Fixed/Learned/Outputs)를 `## Progress` 맨 위 삽입 + `## To-Do-List` 갱신(발견 마커 Where·What·Who + 사용자 방향 맨 앞) + `status:`→`parked`·`updated:` 갱신 + (docs 문서를 만졌을 때만) 해당 문서 `history:` 1행 ④ PM git commit — pathspec = scribe 반환 경로 − 세션 파일, 커밋 전 `git -C "$VAULT" log --oneline -10` 컨벤션 확인, push 금지.
- 출력: 파킹 안내(경로 + status + sr/sl/sc 안내). 보통 커밋할 것 없음이 정상(`hippocampus/` 는 git 밖 — vault-tree.md).
- 금지: 🔴 승격 금지(승격 ① 호출자는 sc 뿐 — 파킹마다 승격하면 중복 기록 + 파킹이 비싸짐) · `done` 금지 · scribe 커밋 금지 · push 금지 · 빈 커밋 금지 · naive 전역 치환 금지(본문 Progress 에도 `status:` 문자열 존재) · PM sed/리다이렉션 직접 패치 금지.
- 특이: `(parked)` 헤딩 접미사는 역사 기록일 뿐 머신 판정 소스 아님(KJP-48 — 상태는 frontmatter). 범위 밖 dirty 는 보고만, 파킹을 막지 않음.

### 5. sc — Session Complete (`brain/skills/sc/SKILL.md`)
- frontmatter: `name: sc` · `argument-hint: "[session-file?]"`.
- 하는 일 (① ~ ⑨): ① 대상 확정(인자 우선, 불명확하면 공유 스캔 후보 제시 — `parked` 직접 종료 정상) ② pending 마커 스캔(남으면 y/n — n 이면 정지) ③ 종결 판정 — **status 는 항상 `done`**(abandoned 도 done + `**Final state:**` 산문; `cancel` 폐기 — validate.sh 가 보고) ④ 승격 ① — `knowledge-promotion.md` Read 후 실행(**유일한 호출자**; scribe 위임; 세션에 되쓰기 금지 — 마커·백링크·카운터 없음, `related:` 는 `[]`) ④b 문서 체크 — Outputs 가 아키텍처·API surface·배포·스키마를 건드렸으면 `doc-catalog.md` 로 라우팅; 워커 Handoff `Docs draft` 는 축어 복사(PM 라우팅만·scribe 복사만); 빈 스텁 신설 금지 ⑤·⑥ 종결 엔트리(`### YYYY-MM-DD (completed)` + Final state + Done/Learned/Outputs) + frontmatter 전이를 scribe 1회 위임 ⑦ PM git commit — pathspec = scribe 반환 경로(승격 노트 + `p_memory/_index.md`); 세션 파일 미포함; push 금지; 커밋 실패해도 종료는 진행 ⑧ `dreaming` 트리거(`brain/skills/dreaming/SKILL.md` — 락은 dreaming 이 잡고, 실행 중이면 skip) ⑨ `Session closed: <path> (status: done) · vault committed`(스킵 시 문구 교체).
- 출력: 종료 확인 + common-policy candidate 구두 보고(있으면).
- 금지: `done` 을 쓰는 유일한 스킬 · CLI 쓰기 금지 · 터치 범위 = vault-root 하위 + `<project>/p_memory/` · PM 직접 status 패치 금지.

### 6. init — 하네스 구조 설치 (`brain/skills/init/SKILL.md`)
- frontmatter: `name: init`.
- 하는 일: ① 4값 AskUserQuestion — org · vault-root(절대 경로, 🔴 trailing slash 제거 — ss·sc 쓰기 경계가 문자열 prefix 비교) · project · PREFIX(2–4 대문자). 기존 볼트면 **adopt 모드** — 구조 diff 는 read-only 보고 전용(`.brain-paths` 먼저 읽고 vault-tree canon 과 해소된 축으로 비교; 공통층 sub-축은 topic-free — `*policies*` 만 normative; tools 층 부재 ≠ mismatch), 마이그레이션은 사용자 승인 후에만 ② 설정 파일 2개 — `CLAUDE.md`(커밋: brain config 공유 키·PM role·Worker profiles; `ticket-system: TBD`; 🔴 크레덴셜 금지 — 식별자만) · `CLAUDE.local.md`(gitignored: `vault-root` + Router — `$HOME/.claude/brain-docs/<doc>.md` 안정 심링크 경로를 **홈 literal 전개**해 기입, 플러그인 설치 경로 금지). 기존 파일이면 `<!-- brain:begin -->`…`<!-- brain:end -->` 마커 블록만 교체. Router 문서 9종 = vault-tree · sessions-note-convention · doc-catalog · project-docs-convention · knowledge-convention · knowledge-escalate-convention · memory-control-convention · **doc-templates** · git-convention (`brain/skills/init/SKILL.md:75` 실측) ③ `.gitignore` 에 `CLAUDE.local.md` 멱등 추가(🔴 `CLAUDE.md` gitignore 절대 금지) ④ 볼트 스캐폴드 — scribe 1브리프 위임, 멱등: `.brain-paths` 최우선(`schema_version: 2` + `common_root`·`projects_root`·`tools_root`; 기존 키 보존) · 공통층 기본 5폴더 `{about,machines,network,platforms,policies}/_index.md`(신규 볼트만) · `neocortex/_index.md`+`dream-logs.md` · 프로젝트 폴더+`p_memory/_index.md`+`docs/` TOC(`docs/{business,develop,develop/feature,adr,resources}/_index.md`)+**스텁 5**(PRD·ARCHITECTURE·CODE_CONVENTION·RUNBOOK·THREAT_MODEL — H1 만, `status: created`; 6번째 BUSINESS 는 해체됨 — §BM→PRD·§GTM→MARKETING; API_SPEC 은 pre-create 안 함; 예외 — RUNBOOK 만 `## Delivery` 시드: 볼트 delivery 분류 노트 포인터 + 버킷 TBD + 예외 줄) · `hippocampus/_index.md` + 볼트 자체 `.gitignore` 의 `hippocampus/` 항목 · 허브·루트 `_index.md` ⑤ 보고 + `/brain:onboard` 안내.
- 금지: 전체 파일 덮어쓰기 금지(마커 블록만) · 크레덴셜 금지(별도 env 파일 `~/.config/claude/<system>.env`) · 기존 볼트 마이그레이션 금지 · 기존 `.brain-paths` 키 덮어쓰기 금지 · 내용 채우기는 하지 않음(그건 onboard). CLAUDE.md/CLAUDE.local.md 만 볼트 밖이라 PM 직접 작성.

### 7. onboard — 프로젝트 인터뷰 (`brain/skills/onboard/SKILL.md`)
- frontmatter: `name: onboard`.
- 전제: init 완료(brain config 가 두 설정 파일에 존재; pre-split 은 CLAUDE.local.md 에 전부 — 둘 다 읽음). 없으면 정지 + `/brain:init` 안내.
- 하는 일: 5질문 일괄(AskUserQuestion) — ①티켓 시스템(예시는 메뉴 아님 — 자유 식별자) ②목표 ③스택 + 하위 3문(Q1 배포 통제 주체 / Q2 실사용자 SaaS 여부 / Q3 디자인 툴 — tool-neutral) ④규제·민감정보 ⑤배포 대상 → **step 6 환경 체크**(실측이지 인터뷰 아님; worker 브리프 1건 위임, 멱등 `verified:` 스탬프) — 머신 구성 → 공통층 machines 축 `<machines-dir>/…`(🔴 폴더·노트 모두 해소 — hostname 재귀 검색, `<hostname>.md` 가정 금지; 발견 시 `Edit` in place — `vault-io.md` §1 canon (`brain/skills/onboard/SKILL.md:33`); 형태 불일치면 보고 후 정지 — migrate·duplicate 금지) / 도구 인벤토리(opt-in 1질문 후에만) → `<BRAIN_TOOLS>/tool-{mcp,skill,cli,plugin}.md` — 🔴 공통층에 쓰기 절대 금지; 이 step 이 tools 층 생성 owner(gitignore 를 쓰기 **전에** 등록) → 답→문서 매핑: ①→`ticket-system` ②→PRD(§BM; GTM 은 MARKETING — situational, 안 만듦) ③→CODE_CONVENTION·ARCHITECTURE·RUNBOOK §Delivery(Q1/Q2 로 버킷 server-SaaS/server-personal/client; 🔴 git-flow 값 하드코딩 금지 — 볼트 delivery 분류 노트 포인터만; normative tier 는 glob `*/*policies*/*`; Q3→DESIGN §SSOT 시드) ④→COMPLIANCE·THREAT_MODEL ⑤→RUNBOOK → **답한 것만** worker 브리프로 stub→draft(`status: draft` + `updated:` + `history:` 1행; 모르면 TBD — 스텁 유지, 강제 채움 금지) → 보고(채운 문서 + 남은 스텁 + 환경 체크 노트 경로, 머신별 created/refreshed 구분, migrate 거절 표면화).
- 특이: 실측 사고 3건이 날짜와 함께 본문 박제(2026-07-25 인벤토리 drift 31KB vs 3KB · 2026-08-05 machines 폴더 불일치 · 2026-08-12 hostname 파일명 가정 오류) · "도구가 안 쓰이는 이유는 인벤토리 부재가 아니라 recall 부재".

### 8. dreaming — 볼트 수면 통합 (`brain/skills/dreaming/SKILL.md`)
- frontmatter: `name: dreaming`.
- 하는 일 — 배치 3연산: **Refine**(형태 정합·패딩 제거 — 사실 불변: 코드·명령·경로·에러·숫자·버전·부정문 그대로, 편집 전후 값 추출·비교 후 하나라도 사라지면 쓰기 취소) · **Link**(`related` 연결 — 한 줄에 quoted 링크 1개 `  - "[[stem]]"`; 2홉 도달 가능한 지름길 금지; bare `[[a]]` 표기 금지 — YAML 이 `[[` 를 중첩 시퀀스로 읽음, 규칙 이전 위반 354개 실측 2026-08-12) · **승격 ②**(두 프로젝트에 선 지식 → `neocortex/NEO-<slug>.md` — 동일성은 내용 판단, 숫자·경로·버전·조건 하나라도 다르면 링크만; 노트 내 3줄 변경 + `git mv` + 인바운드 링크(`p_memory/_index.md`·`neocortex/_index.md`·모든 `related:`) 같은 커밋 재배선; `aliases:` 는 커버 못 함 — Obsidian resolver 미참조 실측; 검증은 `obsidian unresolved verbose` **target list** 비교).
- 입력: `p_memory`·`neocortex` 만 — 🔴 **세션은 읽지도 쓰지도 않음**(`hippocampus/` 양방향 밖). `sc` ⑧이 트리거; vault-root 는 `CLAUDE.local.md`, 다중 볼트면 순회.
- 운영 규칙: 볼트 루트 원자 락(실행 중이면 skip; `active` 세션 있는 프로젝트의 p_memory 스캔 제외) · 별도 git worktree + `merge --ff-only`(실패 시 결과 폐기·커서 유지) · 1 run = 1 commit · 증분(커서 = 마지막 성공 커밋) · 같은 판단 2회 불일치 시 방치 · `<common_root>/` 쓰기 전·커밋 전 2회 거부.
- 출력: `neocortex/dream-logs.md` 단일 파일 append(frontmatter 1키; 줄에 ID 발급 금지 — 날짜 접두 + append 순서가 주소; 쓰기는 scribe 위임). 파괴적 변경(merge·delete·move)은 **제안만** — 승인 후 적용("never silently overwrite memory").

### 공유 참조문서 5종 (`brain/skills/_session-shared/` — Read 로 소비, 단독 실행 없음)

| 파일 | 내용 | 실제 소비자 (SKILL.md 본문 실측) |
|---|---|---|
| `_session-shared/active-sessions.md` | §1 스캔(find + sed/grep, `<status>\t<path>` 방출, frontmatter 첫 `status:` 만 — 본문 오탐 방지, `\|\| :` 종결자 필수/KJP-41) · §2 awk 요약 추출(Goal/To-Do/최신 Progress 만 — 전체 Read 금지) · §3 렌더(`[parked]/[active]` 마커·updated 내림차순) | `sl` · `sr` · `sc` ① |
| `_session-shared/knowledge-promotion.md` | 승격 ① 절차 — 대상 2종(사용자가 정정한 것 · AI 가 자인·복구한 실수), 점수·임계값 없음. scribe 브리프 규격: 축어 전달 · glossary anchoring(recall 이 주입한 `_index.md` summary 재사용 — 별도 스캔 금지) · 목적지 `<project>/p_memory/<pp>_<slug>.md`(update-over-create) · `_index.md` 같은 커밋 append · `related: []` · 세션 역기록 금지. 공통층은 목적지 아님 — 후보는 프로젝트 지식으로 승격 + close 리포트에 `common-policy candidate` 구두 고지(미고지로 9건 방치 실측 2026-08-12) | `sc` 만 (sh 의 2회 참조는 "여기서 승격 금지"의 부정 인용) |
| `_session-shared/project-inference.md` | 프로젝트 결정 4단 — ①인자 override ②`CLAUDE.md` `project:`(pre-split 은 CLAUDE.local.md — 둘 다) ③cwd `<work-root>/<org>/<project>/<repo>` 파싱 ④실패 시 질문(추측 금지) | `ss` · `sr` · `sl` · `sc` |
| `_session-shared/recall.md` | recall 정본(13파일 중 유일한 한국어 본문) — 주입 대상 = `_index.md` 뿐(프로젝트 전량 + 공통층 전량 + `neocortex/_index.md`), 랭킹·상한·후보 선별 없음, `hippocampus/**` 미스캔, fail-visible(파일 수·바이트 상시 보고, 0건도 0건이라 말함). 출력처 분기: ss=파일 기록 · sr=화면만 | `ss` · `sr` |
| `_session-shared/vault-io.md` | §1 쓰기 = `Write`/`Edit` 만(`Edit` old_string = compare-and-swap — 동시 충돌 시 시끄럽게 실패; `Write` 는 신규만) · raw 층 검증은 git diff 불가 → 바이트 산술(실측 2026-08-12) · §2 `obsidian create`(및 모든 변이 서브커맨드) 금지 — 무플래그 충돌 시 `X 1.md` 무음 포크(실측 2026-07-27, Obsidian 1.12.7); 읽기·링크 진단(`unresolved`/`orphans`/`deadends`)은 허용·정본; `unresolved` 는 distinct target 카운트(total 427 vs occurrence 2350 — 5.5배 과소, 실측 2026-08-12) → 이동 검증은 target list diff; regex 링크 카운트 금지 · §3 앱 내장 `obsidian` CLI vs 서드파티 `obsidian-cli`(미설치) 구분 | `ss` · `sr` · `sc` · `dreaming` · `onboard`(step 6) · `knowledge-promotion.md` |

- **모듈 자기 기술 vs 실제 호출자 불일치 3건** (레포 내 드리프트, 실측):
  - `active-sessions.md:63` "used by sl + sr only" ↔ `sc/SKILL.md` ①이 §1 참조.
  - `project-inference.md:3` "shared by `ss` and `sc`" ↔ `sr`·`sl` 도 참조.
  - `vault-io.md:3` 호출자 목록(ss·sr·sc·knowledge-promotion·dreaming)에 onboard 없음 ↔ `onboard/SKILL.md:33` 이 §1 참조.
- 세션 status 3값의 쓰기 권한 분할: ss=`active` 생성 · sr=`active` 복원 · sh=`parked` · sc=`done` · sl=쓰기 없음.
- 승격 2단: ① 세션→p_memory (sc 단독) · ② p_memory→neocortex (dreaming 단독).

---

## [에이전트 전수]

> 4프로필 모두 `## KERNEL-BEGIN`…`## KERNEL-END` 6줄 블록을 **글자 그대로 동일하게 의도적 중복** 탑재 (worker.md L8-15 · coder.md L9-16 · verifier.md L9-16 · researcher.md L9-16) — 드리프트는 `brain/scripts/kernel-check.sh` 가 검출. 6줄: ①브리프(Goal·제약·포인터·DoD)가 스코프 전부 — 모호하면 Ask 반송, 문서 충돌은 보고 ②인프라·환경 사실 기억 단언 금지 — 볼트→실측→Ask ③주장마다 증거(파일·줄·명령 출력) — 없으면 "근거 없음 — 추정" ④볼트 직접 쓰기 금지 — 산출물은 Handoff ⑤하위 워커 보고는 위로만 ⑥Handoff 고정 7절 Done/Mistake/Learned/Outputs/Risks/Next/Ask.
> `model`·`tools` 키는 4파일 어디에도 없음 (코드에 없음).

### 1. worker (`brain/agents/worker.md`)
- frontmatter: `name: worker` · description "범용 작업 워커. PM 이 티켓·브리프를 위임할 때의 기본 프로필. scribe(기록) 브리프도 이 프로필이 받는다". 도구 제한·isolation 없음.
- 규율: 하위 워커는 병렬·격리·재검증이 값을 할 때만(L17-18) · 도구 제작 전 `<BRAIN_TOOLS>/tool-*.md` 확인 — opt-in 층, 없으면 skip(L20-21).
- scribe 브리프일 때(L23-31): 볼트 콘텐츠 쓰기 전량 담당(세션 파일·Progress·frontmatter·p_memory·neocortex·`_index.md`·dream-logs) · 코드 금지·커밋 금지(커밋은 PM) · 축어 필사 · `Edit`(기존)/`Write`(신규)만 · 워커→scribe 직행 금지 · 공통층도 정상 목적지이되 무엇을 쓸지는 사용자 지시가 결정, 무인 사이클(`sc`·`dreaming`)은 공통층 전체 제외 · `_index.md` 는 같은 커밋에서 손 갱신(생성기 없음 — KJP-77 폐기; 줄 형식 `- [[<basename>]] — <summary>`) · 기록 경로 전량 반환.
- Docs draft(L33-34): 작업이 문서를 무효화·확장하면 `Risks` 에 명명 + `Docs draft` 절 — 초안은 워커, 복사는 scribe, 배치 결정은 PM(doc-catalog).

### 2. coder (`brain/agents/coder.md`)
- frontmatter: `name: coder` · `isolation: worktree`.
- First action — 베이스 확인(L18-26): 코드 읽기 전 `git log --oneline -1 && git log --oneline -1 main && git log --oneline main..HEAD`. stale base 는 기본값(워크트리는 `origin/main` 에서 잘림 — L23). 뒤처짐+로컬 커밋 없음→`git reset --hard main` 무질문 / 있음→멈추고 보고.
- Branch(L28-32): `git switch -c <type>/<PREFIX>-<번호>-<slug>` 40자 상한, 티켓 없으면 `<PREFIX>-adhoc-<slug>`. `<type>` 어휘 정본 = `brain/docs/git-convention.md` (포인터만).
- 품질(L34-37): 새 함수 40줄·중첩 3단·단일문자는 루프 인덱스만·판정 로직 2위치 복제 금지·주석은 제약/이유/절충만.
- 테스트(L39-41): production 변경 **전** 유닛 작성 + 실패 확인 → 구현 → e2e 최소 1개. 못 돌리면 `Done` 표기 금지 — `Risks` 에 원인·재현 명령.
- 로그(L43-46): 실패 경계마다 구조화 로그 — 같은 correlation id 로그만으로 실패 특정 불가면 `Done` 전 보강. 토큰·비밀번호·개인정보·원문 payload 금지.
- Last action — 리모트 게이트 실측(L53-60): `gh api` rulesets + branch protection 둘 다. 게이트 없음→브랜치에서 멈춤(push·PR 금지, PM 로컬 머지) / 있음→토픽 브랜치 push + draft PR(제목 `<type>(<PREFIX>-<번호>): 요약`) / `gh` 부재·미인증→멈추고 보고. `main` 직접 push 금지.
- Handoff(L62-69): `Outputs` 첫 3줄 고정 — `branch:` / `base:` / `pr:`(`none (unprotected)` 까지 명기, 공란 불가).

### 3. verifier (`brain/agents/verifier.md`)
- frontmatter: `name: verifier` · `disallowedTools: Write, Edit, NotebookEdit`.
- 규율: report-only — 고치지 않고 재현·증거로 보고, 수정 제안은 텍스트만(L18-19, 26). 기본 대상 = 코드(재현 절차·테스트 출력·경계값·회귀 범위). 보안 브리프면 `brain/docs/security-audit.md` Read 후 동일 report-only — daily 신뢰 8/10 이상만 · comprehensive 2/10(L21-23). `file:line` 인용 없는 발견은 unverified 강등(L27). 하위 워커는 읽기 브리프만 — 읽기 전용 경계는 자식에게 상속되지 않으므로 규율로 강제(L28).

### 4. researcher (`brain/agents/researcher.md`)
- frontmatter: `name: researcher` · `disallowedTools: Write, Edit, NotebookEdit`.
- 규율: First action = 스코프 게이트 — 조사 범위 1줄 PM 확인 후 시작(L18-19). 출처 우선순위 1차 원본→벤더→커뮤니티(블로그 단독 인용 금지, L22) · 반증 탐색 의무 — 없으면 "반증 자료 없음" 명시(L23) · 인용 = URL+접근일+버전, 못 채우면 "근거 없음 — 추정"(L24) · 도구 = context7·Exa/Tavily·WebFetch, 레포 안 검색은 Explore(L25) · `Outputs` = 출처표(주장/근거 URL/반증/신뢰도)(L27-28).

---

## [훅]

### hooks.json 등록 전수 (`brain/hooks/hooks.json`)

| # | 이벤트 | matcher | 명령 | timeout |
|---|---|---|---|---|
| 1 | SessionStart | (matcher 키 없음) | `ln -sfn "${CLAUDE_PLUGIN_ROOT}/docs" "$HOME/.claude/brain-docs"` | 5 |
| 2 | SessionStart | (없음) | `"${CLAUDE_PLUGIN_ROOT}/hooks/kernel-check-hook.sh"` | 10 |
| 3 | PostToolUse | `Edit\|Write\|NotebookEdit` | `"${CLAUDE_PLUGIN_ROOT}/hooks/kernel-check-hook.sh"` | 10 |

- 근거 `brain/hooks/hooks.json:4-31`, type 3건 전부 `command`. description(L2)이 목적 자기 서술: 심링크 유지(버전 범프·레포 이동에도 Router 경로 생존) + KERNEL drift 보고, 통과 시 무음.
- `force-delegate.sh` · `org-guard.sh` 는 **미등록** — 각 헤더가 "OPT-IN — inactive unless wired into hooks.json" 명시 (`force-delegate.sh:2` · `org-guard.sh:2-3`).

### 1. kernel-check-hook.sh (`brain/hooks/kernel-check-hook.sh` — 등록됨)
- 정체: `brain/scripts/kernel-check.sh` 의 배선 어댑터 (KJP-71 — 검사기는 있었는데 아무도 안 불렀다, L2-6).
- 동작: `hook_event_name` 분기 — **SessionStart**: `KERNEL_CHECK_DIR` 또는 `$HERE/../agents` 검사, findings 는 **stdout + exit 0**(SessionStart 는 차단 불가, stdout 이 모델 컨텍스트 주입 채널) / **PostToolUse**: `file_path`→`notebook_path` 추출(jq 없이 grep/sed **첫 매치** — Write content 자기참조 오염 방어) → `.md` 이고 디렉토리가 basename `agents` + `^## KERNEL-BEGIN` 보유(이름 아닌 **내용 기반 판별** — 일반 프로젝트 `.claude/agents/` 오탐 방지)일 때만 검사, findings 는 **stderr + exit 2**(사후 훅이라 차단 아님 — 모델 피드백 "지금 복구하라") / 그 외 이벤트 exit 0.
- 검사기 부재·비실행도 finding 으로 보고("drift is NOT being checked" — 무음 삼킴 금지, L78-86). 통과 시 양 이벤트 무음(SessionStart "OK" 인쇄는 영구 토큰세, L35-38).
- 이스케이프: `KERNEL_CHECK_OFF=1`. 테스트 심: `KERNEL_CHECK_DIR`. 이식성: bash 3.2 + POSIX, jq 불사용(L42-43). CI 대신 hooks.json 을 택한 실측 근거(2026-08-05: `.github/` 부재·활성 ruleset 0·`core.hooksPath` unset) 헤더 문서화(L16-26).
- 결함(경미): 헤더 L8 주석 "matcher Edit|Write" ↔ 실 matcher `Edit|Write|NotebookEdit`(코드는 `notebook_path` 처리로 NotebookEdit 지원 — 주석만 낡음) · `_kh_str` 은 escaped quote 에서 잘림.

### 2. force-delegate.sh (`brain/hooks/force-delegate.sh` — opt-in 미등록)
- 정체: PreToolUse(Edit|Write) 배선 시 메인 세션(PM)의 파일 편집을 차단해 위임 강제. 판별 = stdin JSON top-level `agent_id` 키(jq `has()`, 없으면 grep fallback) — 있으면 서브에이전트 → 통과, 없으면 main → stderr 안내 + exit 2 (L36-57).
- 예외 경로(L48-53): `$HOME/.claude/*` · `$HOME/Documents/Obsidian/*`(기본 볼트 위치 근사 — vault-root 를 읽지 않음) · `*/CLAUDE.md` · `*/CLAUDE.local.md`. 이스케이프: `FORCE_DELEGATE_OFF=1`.
- 결함:
  - 🔴 **버그 의심 — jq 미설치 시 예외 경로 전멸**: 예외 검사(L48-53)가 `command -v jq` 블록 안에만 있어 jq 없는 환경에선 `~/.claude`·볼트·CLAUDE.md 편집도 전부 차단 — stderr 안내문(L56)의 "허용" 서술과 실동작 불일치.
  - grep fallback(L40)은 top-level 검사가 아님 — Write `content` 에 `"agent_id":` 문자열이 있으면 main 도 subagent 로 오판(허용 방향 오탐).
  - `agent_id` 는 비공식 필드 — CLI v2.1.178 실측, 업그레이드 후 재확인 필요라고 헤더 자인(L24-25).

### 3. org-guard.sh (`brain/hooks/org-guard.sh` — opt-in 미등록)
- 정체: PreToolUse(Write|Edit|NotebookEdit) 배선 시 볼트 **공통층**(`common_root`) 쓰기 차단. 호출자가 무인 사이클인지 추론하지 않음 — `writer` 키 폐기로 페이로드에 주체가 없어 사후 판별이 원리적으로 불가 → 예방(PreToolUse)만 가능(L23-27). 경로만으로 판정하는 평평한 규칙이라 기본 off(L29-32).
- 해석: VAULT = `BRAIN_VAULT`/`VAULT` env 우선 → cwd 상향 탐색으로 `CLAUDE.local.md` 의 `vault-root:` 파싱(L87-110). 공통층 = `$HERE/../scripts/vault-paths.sh` source(리터럴 금지, L112-127). 경로 비교 = 부모 디렉토리만 `pwd -P` 물리 해석 + **파라미터 확장 prefix 비교**(glob 금지 — `[`·`*` 재해석 차단, L129-157).
- 실패 방향: **fail-open** — env off·경로 없음·볼트/리졸버/공통층 미해석 전부 stderr 한 줄 + exit 0 허용("false positive 가 miss 보다 나쁘다", L39-42). allow 경로의 stdout 은 의도적으로 비움(exit 0 stdout 은 JSON 으로 파싱됨, L44-47). 차단 시 stderr 3줄(BLOCKED·Why·What to do) + exit 2.
- 이스케이프: `ORG_GUARD_OFF=1`. jq 의도적 배제("jq 유무로 다르게 행동하는 훅은 추론 불가" — force-delegate 방식 명시 거부, L50-53).
- 결함(경미): basename 심링크 우회 — 물리 해석이 부모까지만이라 파일명 자체가 공통층 안을 가리키는 심링크면 관통(fail-open 설계 방향과는 일관).

---

## [스크립트]

### 호출 관계 (실측)

```
vault-paths.sh  (source 전용 — 스스로 실행 없음)
  ▲ source (validate.sh:49)     ▲ source (value-axis-drift.sh:99)
  │                             │
validate.sh                value-axis-drift.sh
  ▲ /bin/bash 실행              ▲ /bin/bash 실행
  └── validate-selftest.sh ─────┘  (VALIDATE_SH / DRIFT_SH env 로 대상 교체 가능)

kernel-check.sh — 독립 (누구도 호출하지 않고 호출되지도 않음; 볼트가 아니라 ../agents 를 읽음)
```

### 1. vault-paths.sh (`brain/scripts/vault-paths.sh` · 128줄)
- 목적: 볼트마다 다른 트리 축을 `<vault>/.brain-paths` 매니페스트에서 해석하는 **source 전용** 라이브러리 (`:2` "Sourced, not run").
- 사용: `VAULT=/path . vault-paths.sh` — `VAULT` env 필수 (`:33` `${VAULT:?}` 미설정 시 중단). 인자 없음.
- 해석: env 오버라이드(`BRAIN_COMMON_REL` 등) → 매니페스트 키 → default. `common_root`→`000_common` · `projects_root`→`.` · `tools_root`→`999_tools` — 이 리터럴들의 **유일한 원산지**가 이 파일이라고 헤더 선언(`:19-28` — 소비자가 복사하면 버그). `BRAIN_SCHEMA_VERSION` 은 유일하게 default 없는 키, `BRAIN_SCHEMA_VERSION_EXPECTED=2` (`:54`).
- 부재 처리 3원 비대칭: common 루트 없음 → **stderr 경고 + 빈 값**("Loud, not silent", `:72-75`) / projects 루트 없음 → 경고만, 변수 유지(`:76-78`) / tools 루트 없음 → **무음 빈 값**(opt-in 층 — 부재가 합법, `:85-87`).
- 헬퍼: `brain_projects`(maxdepth 1, `[0-8][0-9][0-9]_*` — 9xx 밴드 제외, common 제외) · `brain_next_project_num`(%03d) · `brain_project_dir <slug>` · `brain_find_notes`(재귀, 제외: `index.md`·`_index.md`·`0.*`·`dream-log.md`·`_templates/`·`999_Archive/`·`Archive/`·`_dreaming_logs/`).
- ⚠ 제외 파일명 불일치: `brain_find_notes` 는 `dream-log.md`(단수, `:125`) 제외 ↔ `validate.sh:346,439` 의 maxdepth-1 스캔은 `dream-logs.md`(복수) 제외 — common 층에 `dream-logs.md` 가 놓이면 `brain_find_notes` 는 제외 못 함. 교차 픽스처는 selftest 에 없음.
- exit: source 전용이라 자체 규약 없음 (`${VAULT:?}` 외 exit 문 없음).

### 2. validate.sh (`brain/scripts/validate.sh` · 1,370줄)
- 목적: brain 볼트 스키마 린터 — `file:line: message` 형식 전수 점검.
- 사용: `validate.sh <vault-root> [--strict]` · `-h` usage. exit `0`=정상 수행(finding 있어도 기본 모드 0) · `1`=`--strict` + finding≥1 · `2`=usage 오류·vault 부재·mktemp 실패.
- 검사 전수 (9계열):
  1. `.brain-paths` `schema_version` — 부재·기대값(2) 불일치 finding; 매니페스트 자체 부재는 침묵(default 가 정의상 옳음) (`:69-86`).
  2. 세션 노트(`hippocampus/` maxdepth 1) — 읽기 불가 · frontmatter 파스 체크(KJP-97 3-테스트) · frontmatter 부재 · 필수 5키(`status·project·updated·related_ticket·cc_session_ids`) · 폐기 키 3종(`uid`·`created`·`writer` — 이행 지침 포함) · status 어휘(`active|parked|done` 외: 문서 status 오용/`cancel` retired/invalid 각각 구분) (`:235-311`).
  3. 위키 노트(p_memory maxdepth 1 + `neocortex/` 루트 고정 + tools 루트 + common 재귀) — `summary:` 부재·빈 값 · frontmatter 비인용 위키링크(KJP-56) · 파스 체크 · 폐기 0.1.x 키 10종(`uid title type tags dri species source_sessions source_items recalled useful`) (`:313-410`).
  4. p_memory 파일명(KJP-63/98) — 허브 `PREFIX:` 대조 `<pp>_<slug>.md`; 허브 미선언은 허브 1건 finding; 복합 결함은 한 finding 이 동시 명명 (`:412-495`).
  5. 폴더 인덱스(KJP-82) — 위키 인덱스는 dangling+라인 형식(`- [[<stem>]] — <summary>`), docs 인덱스는 dangling 만(형식 캐논 부재 — 85건 오탐 실측 근거); 타깃 집합은 `LC_ALL=C sort -u`(KJP-74 한글 stem 붕괴 실측); `aliases:` 의도적 미수확 (`:497-690`).
  6. 인덱스 커버리지(KJP-82 역방향) — 어떤 인덱스도 명명 않는 위키 노트 `uncovered note` + 추가 라인 제시 (`:692-787`).
  7. 공유면 세션-uid 위키링크 — docs·p_memory·common·neocortex 에서 0.1.x uid 형태 검출; `hippocampus/`·tools 루트 미스캔 (`:789-867`).
  8. docs frontmatter v2 — findings: `session:` 키 전 형태 · barewiki · 파스 · `status` 부재/어휘(`created|draft|approved|deprecated`; 세션 어휘 오용·`stub` retired 구분) · v1 `history` 서브키(`date:`/`by:`) · ADR `id:`/`next_id:` · API_SPEC 미러(`source:`+`readonly: true`) / stderr warns(--strict 미차단): 미지 톱레벨 키(인정 집합 `status|updated|id|summary|supersedes|source|readonly|synced|history`) · `updated:` 형식(date-only 는 `docs/adr/` 아래 침묵) (`:869-1110`).
  9. ADR ID 원장 감사(KJP-83) — duplicate(id 당 1 finding·참여 파일 전부) · gap(최고 id 파일 anchoring, GAPCAP=10) · counter(`next_id` ≤ 최고 발급 → finding; 선발급 초과는 침묵); id 없는 폴더는 침묵(KJP-92 — init 직후 정상) (`:1112-1331`).
- 리포트: 스캔 계수 **9종** 상시 출력 — `N sessions, N wiki, N p_memory names, N wiki indexes, N docs indexes, N entries, N shared, N docs, N adr ids` (`:1360` 실측) — 붕괴된 스캔(0)과 클린 볼트 구분. finding 0 → `OK`.
- 이식성 규율: bash 3.2 + POSIX(one-true-awk — ERE interval `{n}` 금지·bracket class 내 `/` 금지·`[` 비교는 index/substr), `LC_ALL=C` 2개소는 정합성(macOS sort collation 붕괴 실측), vault 경로는 find start path 로만(glob 스플라이스 금지).
- 명시된 한계(ponytail): 심링크 노트 skip · frontmatter 중복 키 last-wins · uid 스캔 코드펜스 미인식 · 부분 경로 위키링크 dangling 오탐 가능 · 비옵션 인자 다중 전달 시 마지막 것이 무경고 승리(`:39`).

### 3. validate-selftest.sh (`brain/scripts/validate-selftest.sh` · 1,401줄)
- 목적: 고의로 깨진 픽스처 볼트를 mktemp 에 짓고 validate.sh(+value-axis-drift.sh)의 전 규칙 발화/스코프 침묵을 grep assert 로 전수 검증 (`:2-4`; 프레임워크 없음).
- 사용: 인자 없음. env 심 2개 — `VALIDATE_SH`(`:13`) · `DRIFT_SH`(`:1279`) 로 검증 대상 교체(뮤테이션 테스트용). exit `0`=전 assert 통과 · `1`=실패≥1 · `2`=초기 mktemp 실패.
- 구성: 메인 픽스처 볼트(세션 13종·위키·인덱스·docs·ADR 3폴더·API_SPEC PASS/FAIL·p_memory 파일명 3프로젝트) + 격리 볼트 8종(--strict 단독·ADR 선발급 침묵·읽기 불가·경로 강건성 `my[vault]`·env seam·빈 볼트·schema_version 3상태·재구조화 볼트 `common_root: _primary` 등) + value-axis-drift 섹션. 약 200 assert, 정확 계수 assert(`:1075` — `(13 sessions, 46 wiki, 9 p_memory names, 7 wiki indexes, 10 docs indexes, 47 entries, 95 shared, 40 docs, 10 adr ids)`)는 픽스처 추가마다 수기 갱신되는 의도된 취약성.
- 규율: 모든 스코프 경계는 positive 픽스처로 고정 — 단독 no_match assert 는 vacuous (`:6-9`). 픽스처 줄번호가 assert 와 결합("append only").
- 한계: **kernel-check.sh 는 유일하게 미커버**(관련 픽스처·assert 코드에 없음) · `$W` 이후 볼트들은 mktemp 실패 가드 없음(`:1087` — `$V` 와 달리 `|| exit 2` 없음) · root 실행 시 unreadable 테스트 skip.

### 4. kernel-check.sh (`brain/scripts/kernel-check.sh` · 183줄)
- 목적: `agents/*.md` 의 `## KERNEL-BEGIN`…`## KERNEL-END` 블록 **바이트 동일성** 검사 (CR 제거 후 비교 — CRLF/LF 동등).
- 사용: `kernel-check.sh [<agents-dir>]` — 기본 = `$HERE/../agents`(`:51-54`). exit `0`=전 블록 존재·동일 · `1`=finding≥1(**--strict 없음** — "KERNEL drift 에 비엄격 독법이 없다"는 명시 결정, `:24-27`) · `2`=usage·디렉토리 불가·mktemp 실패.
- 검사: 파일별 블록 상태(`none`/`orphan-end`/`unterminated`/`duplicate`/`ok`) + reference(정렬 순 첫 정상 블록 — "합의이지 위계가 아님") 대비 diff. 🔴 **0파일 스캔은 그 자체가 finding**(`:170-173` — "all blocks agree" 공허참 방어).
- 출력: 통과 `OK — N agents, KERNEL identical (M lines)` / 실패 `file:line: message` + 라인 diff. finding 수는 카운터 누적(multi-line diff 라 `wc -l` 부풀림 방지).
- 의존: vault-paths 미사용(볼트가 아니라 레포의 agents/ 를 읽음 — validate.sh 에 넣지 않은 이유 명시, `:17-20`). 환경변수 없음.

### 5. value-axis-drift.sh (`brain/scripts/value-axis-drift.sh` · 190줄)
- 목적: 가격·티어 **리터럴**이 홈 문서 밖 docs 트리에 복제됐는지 찾는 report-only 드리프트 검출기 — **자동수정 절대 없음**(`:19-23`).
- 사용: `value-axis-drift.sh <vault-root> [--strict] [--convention <file>]`. exit `0`/`1`(--strict)/`2`(usage·캐논 불가·pricing 행 부재).
- SSOT: 캐논 default `$HERE/../docs/project-docs-convention.md`(`:64`)의 `## Value Axes` 섹션 내부 pricing 행을 **매 실행** 읽음(자체 표 인라인 금지 선언) — 행 부재 = **exit 2 LOUD**("조용히 0 스캔"이 SSOT 이동을 숨기는 경로, `:76-79`).
- 스코프: `brain_projects` 의 `NNN_*/docs/**` 재귀, 홈 제외 2형태(경로 `docs/<home_lc>/` · 파일명 `<HOME_DOC>.md`). 탐지 6패턴: 통화기호+금액 · 금액+통화어 · 통화코드+숫자 · 영문 티어명+tier/plan 인접 · 한글 티어명+티어/플랜/요금제 인접 · 티어 사다리 리스트. 스킵: frontmatter(1행 `---` 시작 시만)·코드펜스·인라인 코드. 시맨틱 중복(산문 재진술)은 명시적 범위 밖 — PM 중재 몫(`:31-34`).
- 출력: `<file>:<line>: value-axis drift (<axis>): "<literal>" — …` + 스캔 문서 수 상시.
- 한계(코드에 방어 없음): 펜스 토글이 ```/~~~ 미구분(`fence = !fence`, `:157`) · BOM·선행 공백 파일은 frontmatter 미인식(`:153`).

### 공통 관찰
- 4개 실행 스크립트 전부 헤더에 "macOS stock bash 3.2 + POSIX" 선언 (`validate.sh:28-29` · `kernel-check.sh:34-35` · `value-axis-drift.sh:39-40` · `vault-paths.sh:30-31`).
- "스캔 계수 상시 출력 — 붕괴 스캔(0)과 클린 구분" 계약을 3개 검사기가 공유; kernel-check 는 0 스캔을 finding 으로 승격.
- 셀프테스트 커버리지: validate.sh·value-axis-drift.sh·vault-paths.sh(간접) 커버 / **kernel-check.sh 만 미커버**.
- 줄 수는 `wc -l` 실측 (5파일 전부 trailing newline 존재 확인 — `tail -c 1 | od -c` 실측).

---

## [설정·매니페스트]

- `.claude-plugin/marketplace.json` · `brain/.claude-plugin/plugin.json` — 필드 전수는 [플러그인] 참조.
- `brain/hooks/hooks.json` — 등록 전수는 [훅] 참조.
- `CLAUDE.md` (레포 루트 · git 추적 · 2,484 B) — 전체가 `<!-- brain:begin -->`…`<!-- brain:end -->` 마커 블록 1개(init 관리 대상). §brain config 4키(`org: techtainment` · `project: kj-plugins` · `prefix: KJP` · `ticket-system: plane` — identifier only + Plane UUID·CLI 구분·Huly 이관 2026-08-12 주석: 구 Huly N = Plane N−4, 코멘트 미이관) · §PM role 6규칙(단일 접점 · 외부 티켓 = 정식 큐 · PM 볼트 직접 쓰기 금지/커밋만 PM · 티켓 루프 Plan→coder→verifier · 브리프 규율 Goal/제약/포인터/DoD · 문서 중재 정본 + 모르면 볼트 먼저) · §Worker profiles(4프로필 + 세션 스킬 1동사 1스킬).
- `CLAUDE.local.md` (레포 루트 · git 미추적 — `.gitignore:5`, `git check-ignore` 확인 · 1,802 B) — 마커 블록 1개. §brain config: `vault-root: /Users/nwkim/Documents/Obsidian/second-brain/techtainment`. §Router 14행: 규약 문서 9행(`~/.claude/brain-docs/` 대상) + 볼트 경로 4행 + 도구층 1행.
  - 🔴 **드리프트 1 — Router 규약 목록이 init canon 과 상이**: 현행 파일의 9행 = vault-tree · sessions-note-convention · doc-catalog · project-docs-convention · knowledge-convention · knowledge-escalate-convention · memory-control-convention · **versioning-convention** · git-convention ↔ init canon 의 9종(`brain/skills/init/SKILL.md:75` 실측)은 versioning-convention 대신 **doc-templates**. `versioning-convention.md` 는 `brain/docs/` 에 존재하지 않으므로(find 실측) 이 Router 행은 심링크 너머 dangling.
  - 🔴 **드리프트 2 — pre-0.2.0 경로 표기**: Router 의 세션 = `<vault>/sessions/` · 지식 = `projects/013_kj-plugins/knowledge/` ↔ 현행 canon 용어는 `hippocampus/`·`p_memory/`(`brain/docs/vault-tree.md`; `brain/CHANGELOG.md` 0.2.0 항목이 용어 전면 이행을 기록).
- `.gitignore` (293 B · git 추적) — 5항목: `.DS_Store` · `*.bak-*` · `CLAUDE.local.md`(머신 로컬 config 주석) · `archive/`(보관·정본 아님·재설계 대기 주석 — 현 워킹트리에 해당 폴더 없음) · `.claude/worktrees/`(에이전트 worktree 격리 산출물 주석). `.impeccable/` 은 이 파일에 없음 — `.git/info/exclude:9` 가 담당.
- 볼트 쪽 설정(레포 밖, 스킬이 생성): `<vault>/.brain-paths` — `schema_version: 2` + `common_root`/`projects_root`/`tools_root`. 정본 = `brain/skills/init/SKILL.md` step 4 + `brain/scripts/vault-paths.sh` 헤더.
- git 추적 상태 실측(`git ls-files`): 추적 = 위 매니페스트·설정 + 문서·코드 일체 / 미추적(무시) = `CLAUDE.local.md` · `.impeccable/hook.cache.json` · `.DS_Store` 류 · `.claude/worktrees/`(빈 디렉토리).

---

## [루트 문서]

### 1. README.md (레포 루트 · 1,998 B)
- 정체: 마켓플레이스 소개 — 설치·투어만, 전체 문서·설계 근거는 `brain/README.md` 로 위임.
- §Install(`:7-10`) · §Plugins(brain 1행 표) · §brain quick tour(스킬 8종 한 줄씩) · §Repo layout · §Vault schema check(`brain/scripts/validate.sh <vault-root>` / `--strict`) · §License "Not yet specified."(`:62`).

### 2. brain/README.md (469줄) — 플러그인 정본 문서
- 은유: 인간 기억계 — hippocampus(빠른 손실 캡처) → sleep consolidation(dreaming) → neocortex(의미 기억) → cue-based recall.
- §Why: ① 에이전트는 세션 간 전부 잊는다 ② 검색되지 않는 지식은 없는 지식(trigger-first + recall 주입) ③ 페르소나는 죽었다 — 살아남는 것은 격리·권한·브리프. 운영 원리 = fast-lossy capture + periodic consolidation.
- §Architecture: Vault ↔ PM ↔ Workers 다이어그램 · 3 루트(common·projects·tools)는 `.brain-paths` + `vault-paths.sh` 로 per-vault, `neocortex/`·`hippocampus/` 는 루트 고정 · 🔴 episodic↔semantic 상호 참조 금지(세션 역기입 없음).
- §Schema check(`:141` "It runs **four scans**"): 세션·wiki·공유면 uid·docs v2 의 4개 스캔 카테고리로 서술. ⚠ 실제 `validate.sh:1360` 리포트 계수는 9종 — README 의 "4 스캔" 서술과 검사기 실물 사이의 표현 드리프트 (검사 계열 자체도 9계열로 성장 — [스크립트] 참조).
- §Worker profiles(`:300-320`): 4프로필 표 · coder 첫 행동 = base check — 워크트리가 `origin/<branch>` 에서 잘리므로 stale base 가 기본값(`:311`; `brain/agents/coder.md:23` 의 "워크트리는 `origin/main` 에서 잘린다" 와 정합 — 절단점은 origin, 비교·리셋 대상은 로컬 main) · 마지막 행동 = 게이트 실측 시에만 draft PR · PM 은 `git branch -d` 만(`-D` 금지) · Handoff 고정 7절(+optional Docs draft; `Fixed` 는 0.2.0 KERNEL 에서 제거).
- §Conventions(9종 표) · §Ownership boundary(플러그인=규칙/볼트=내용; 포인터는 `~/.claude/brain-docs/` 심링크로) · §Single-Source Map(팩트별 정본 1곳 + 포인터 ~23행; 집행은 리뷰 — 기계 검사는 value-axis-drift 1슬라이스뿐) · §Design principles(one-source+pointers · trigger-first · small docs · pre-created ≠ evidence · measurements over memory · fail-visible) · §FAQ · §License "TBD."(`:468`).

### 3. brain/CHANGELOG.md (519줄) — 버전 원장 (각 항목은 파일 자기 서술)
- 0.2.5(2026-08-13): 0.2.2~0.2.4 는 미설치 버전 문자열 — 이 컷으로 첫 배포. canon 정합 스윕: feature 양식 `version`·`species` 폐기 · validate.sh ADR 인식 키·date-only 면제 · org-guard 미등록 판정 · `_index.md` "재생성 산출물" 모순 4곳 정정 · `aliases:` 효과 반증 · p_memory 파일명 규약+검사(위반 480건 실측) · frontmatter 파싱 검사 신설 · `related` 표기 canon+검출(354건) · 정책 모델 단일 파일 확정(KJP-79) · feature 문서 1파일 통합 · `.base` 7종 폐기 · 스텁 6→5(KJP-85) 등.
- 0.2.3(2026-08-12): init 구 docs/ 트리 생성 결함 수정 · API_SPEC 미러 경로 미검사 수정 · docs 층 `_index.md` dangle 검사 추가(93개 무검사였음).
- 0.2.2(2026-08-05): `_index.md` 커버리지 검사(역방향) 추가.
- 0.2.1(2026-08-05): `sort -u` 로케일 붕괴 수정(`LC_ALL=C`).
- 0.2.0(2026-08-05): 레포 축 랜딩 — `knowledge/`→`p_memory/` · `sessions/`→`hippocampus/` 용어 이행 · 세션 필수키 6→5 · recall 161→23줄(랭커 폐기) · researcher 신설 · KERNEL 4정의 랜딩 · dreaming 기능군 6종 폐기/재연결.
- 0.1.8(2026-08-05): 드리프트 릴리스 컷(설치본 29항목 차이) · versioning-convention.md → git-convention.md 흡수 · kernel-check.sh · org-guard.sh · `.artifact/brain-0.2.0.html` 추적 시작.
- 0.1.7(2026-07-29): docs frontmatter v2 + SSOT value axes · `.brain-paths` 신설. / 0.1.6(2026-07-27): vault-io.md 신설(쓰기 금지/읽기 허용 분리) · `999_tools/` 분리 · status 3값 재정의. / 0.1.5(2026-07-25): 스텁 19→6 · Handoff Docs draft 신설. / 0.1.4(2026-07-25): CLAUDE 2파일 분리(KJP-23) · 승격 2게이트. / 0.1.3(2026-07-23): doc-templates.md 신설 · docs 트리 결정 뒤집힘. / 0.1.2(2026-07-18): validate.sh 최초 도입. / 0.1.0(2026-07-16): 최초 릴리스.

### 4. brain/ROADMAP.md (50줄)
- 헤더: 0.2.0 완성용 문서 — "정본은 docs/ **9문서**" 표기 ↔ 현행 `brain/docs/` 는 **10파일**(find 실측 — security-audit.md 포함; 파일 자체에 실측 병기 있음). 소멸 조건 충족(0.2.0 컷 완료)이나 잔여 항목이 canon 에 없어 지우면 유실 → PM 판단 대상이라 자기 명시(`:5`).
- 확정: 뇌 용어 개명 레포 축 완료(KJP-63) · 볼트 축 해소(2026-08-12) · 미결 1건 = canon 문서 파일명 개명 여부.
- 잔여: hippocampus 골격 재정의 잔여(Progress 포맷·To-Do 규율·cc_session_ids 빈 값 13건) · dreaming 잔여(증분 커서·감사 목록·recall-fix 루프) · agents/skills/hooks 정규화 미랜딩 · 기계 이빨 3종 미랜딩(쓰기 시점 검증 · canon→인스턴스 마이그레이션 절차 · 릴리스 게이트 byte 대조) · 머신 스코프 층 절반(머신 노트 이원 서술 잔존).
- 열린 티켓 접점(Plane, 구 Huly N−4): #48 cc_session_ids 빈 값 / #55 frontmatter 미규정 키 / #56 YAML 파싱 실패 / #57 참조 섹션 규약.
- 서명 대기: `learned/` 잔류 해소(2026-08-12 실측) · 절의 전제 변경(규범 결정은 사용자) · 미처리 잔고 9건(`0.rejected.md` common-policy 후보).

### 5. .artifact/brain-0.2.0.html (188,698 B · git 추적 — 0.1.8 에서 추적 시작이라 CHANGELOG 기록)
- `<title>` = `kj-plugins — brain 0.2.0` · `<h1>` = `brain 0.2.0 — 설계 전서`.
- h2 골격 6절: ① 폴더 트리 및 양식 ② 원리 ③ 에이전트 — 페르소나는 죽었다 ④ 검증·스크립트 — bash 3.2 · 의존성 0 ⑤ 스킬 ⑥ 릴리스·적용 범위.
- 성격: 0.2.0 설계 정본(전서) — `brain/docs/vault-tree.md:104-132`·`docs-samples/README.md:8`·CHANGELOG·ROADMAP 이 인용하는 참조 대상.

---

## [미확인 사항] (코드에 없음 / 이 조사에서 판별 불가)

- **라이선스** — `README.md:62` "Not yet specified" · `brain/README.md:468` "TBD" · LICENSE 파일 부재(find 실측).
- **설치 캐시 상태** — `brain/CHANGELOG.md:5` 의 "설치 캐시 최대 0.2.1" 주장은 레포 밖(`~/.claude/plugins/cache/`) 사실 — 본 조사 범위에서 미검증. ROADMAP 의 "재설치 byte 검증·두 볼트 재적용 실행 여부"도 레포에서 판별 불가(릴리스 게이트 부재가 원인이라고 파일 자체가 명시).
- **`agent_id` 필드의 공식성** — `brain/hooks/force-delegate.sh:24-25` 스스로 "비공식(undocumented)·CLI v2.1.178 실측·업그레이드 후 재확인" 명시.
- **`archive/` 폴더** — `.gitignore:8` 이 제외를 선언하나 현 워킹트리에 폴더 없음(find 실측).
- **worker 프로필 spawn 타입 표기** — `brain/skills/_session-shared/knowledge-promotion.md:26` 이 "`subagent_type: worker` (설치 형태에 따라 `brain:worker` — 설치 후 검증해 이 줄을 갱신하라)" 라고 미확정 자기 명시.
- **`.artifact/brain-0.2.0.html` 본문 세부** — h1·h2 골격 6절까지 실측, 절 내부 전문은 본 전서 범위 밖(canon 문서들이 "설계 정본"으로 인용).
- **validate.sh 개별 검사 구현의 전수 검증** — 검사 목록·리포트 라인·exit 규약은 실측 인용, 1,370줄 전 구현의 정오 판정은 `validate-selftest.sh` 의 역할(단 kernel-check.sh 는 셀프테스트 미커버 — [스크립트] 참조).
- **README 앵커** — `README.md:58` 의 `brain/README.md#schema-check` 가 가리키는 실제 절 제목은 `## Schema check (scripts/validate.sh)`(`brain/README.md:125`) — 앵커 일치는 렌더러 의존이라 미판정.
- **`(parked)` 세션 실물·볼트 상태 일반** — 볼트는 레포 밖이라 본 전서의 근거 대상 아님(스킬·스크립트가 볼트에 남긴 실측 기록은 각 파일의 자기 서술로만 인용).
