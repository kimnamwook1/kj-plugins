# Changelog

## 0.2.4 — 2026-08-12

### Fixed
- 🔴 **도구층 내부 모델 충돌 판정 — `tool-*.md` 평면이 이기고 전서 §트리를 고쳤다 (KJP-93).** 전서 `.artifact/brain-0.2.0.html` §트리는 종류 폴더 `cli/ mcp/ plugins/ skills/` 를, `brain/docs/vault-tree.md` 는 평면 파일 `tool-{cli,mcp,plugin,skill}.md` 를 그려 **KJP-90 기준의 「모순」**(누락은 합법, 모순은 불법)이었다.
  - 🔴 **「전서 우선」이 적용되지 않았다 — 전서가 스스로 갈려 있었기 때문이다.** 같은 전서 `:1094`(«하네스 파일» `worker.md` 초안)이 `<BRAIN_TOOLS>/tool-*.md` **평면 글롭**을 쓰고 — 이 글롭은 `cli/ripgrep.md` 를 매치하지 못한다 — `:1391` 은 도구 표면을 **4종(MCP·skill·CLI·plugin)**으로 열거한다. 폴더형을 말하는 곳은 §트리 **한 줄뿐**이었다. precedence 는 문서 *사이*의 충돌을 푸는 규칙이지 한 문서 *안*의 충돌을 풀지 못하므로, 판정 근거는 밖에서 와야 했다.
  - 🔴 **결정적 근거 = 실측. 스캐너가 폴더형을 아예 못 본다.** `scripts/validate.sh:251` 이 도구 루트를 **`-maxdepth 1`** 로 스캔한다(공통층만 `brain_find_notes` 로 재귀하고, 그건 「볼트마다 하위 축 모양이 다르다」는 이유로 의도된 예외다 — `:253`). 픽스처 실측 2026-08-12: `summary:` 없는 평면 `999_tools/tool-cli.md` → **2건 보고 · `1 wiki` 집계**, 같은 결함의 `999_tools/cli/ripgrep.md` → **0건 · 집계에도 없음**. 즉 폴더형이면 이 루트는 **아무것도 스캔하지 않는 스캔 루트**가 되고, 그건 `validate.sh:231-233` 이 이 루트를 등록한 이유(「wiki 스키마를 지켜야 할 wiki 층 노트를 담고 있으므로」)와 정면으로 어긋난다.
  - **보강 근거 셋.** ① 이 층의 **유일한 생산자**인 `skills/onboard/SKILL.md:33` 이 네 노트를 **이름으로** 쓰고(`tool-mcp.md`·`tool-skill.md`·`tool-cli.md`·`tool-plugin.md`, 각각 측정 명령 1개에 묶여 있다) 종류 폴더를 만든 적이 없다 ② 소비자 넷이 전부 평면 글롭이다 — `agents/worker.md:21` · `skills/init/SKILL.md:81`(init 이 볼트마다 CLAUDE.local.md 에 심는 Router 줄) · `scripts/validate-selftest.sh:241` 픽스처 · `docs/vault-tree.md` 트리 ③ **층을 만들게 한 실측 자체가 평면 파일**이다 — `tool-mcp.md` 31KB vs 3KB(2026-07-25, KJP-44).
  - **「4종이 늘면 폴더형」은 성립하지 않는다.** 다섯째 종류는 평면 파일 하나가 느는 것으로 끝난다. 폴더형을 정당화하는 유일한 모델은 **도구당 노트 1개**이고(§트리 주석 「각 노트 frontmatter 로 설치·사용 상태 구분」이 함의하던 것), 그런 노트를 쓰거나 읽는 코드는 하네스에 **0곳**이다.
  - 🔴 **실볼트로는 중재할 수 없었고, 그 사실도 실측으로 확인했다** — `999_tools/` 부재, 나아가 `.brain-paths` 에 `tools_root` 키 자체가 없다(`schema_version: 2` · `common_root: org` · `projects_root: projects` 3줄뿐). 표본 0. **이 축은 모델 판정만이고 `999_tools/` 생성은 별건이다** — opt-in 층이라 부재가 곧 합법 상태다.
  - **재발 방지는 이긴 쪽에도 적었다.** `vault-tree.md` §The tools root 에 평면 모델과 `-maxdepth 1` 근거 + 「스캔을 재귀로 고치지 말 것」(같은 줄이 `p_memory/` 하위 폴더를 스코프 밖으로 유지한다) 추가 · §This diagram vs the transcript's §트리 의 「진짜 차이 3건」이 이제 티켓별 판정 표를 갖는다.
- **MILESTONE 홈이 두 문서에서 갈려 있었다 — `doc-templates` 가 틀렸다 (KJP-88).** `doc-templates.md` §MILESTONE §Boundaries 가 홈을 `docs/` 루트라 적고, `doc-catalog.md` §MILESTONE home 노트는 `docs/business/` 라 적고 있었다. **판정은 새로 하지 않았다 — 전서가 이미 답을 갖고 있다**: `.artifact/brain-0.2.0.html` §트리의 `business/` 블록이 `MILESTONE.md` 를 `◇ 상황적` 로 등재한다(`PRD`·`MARKETING`·`COMPLIANCE` 와 같은 블록). `brain/docs/vault-tree.md` `docs/business/` 블록도 같은 자리에 적고 있어 **틀린 쪽은 `doc-templates` 하나**였다.
  - **볼트 실물이 canon 과 일치한다** — `find` 실측: `projects/013_kj-plugins/docs/business/MILESTONE.md` 1건. 문서만 뒤처져 있었고 볼트 이동은 필요 없다.
  - 고친 문장은 홈만 바꾸지 않고 **왜**(로드맵은 business 축 문서)와 **대조 포인터 2개**(`doc-catalog` §MILESTONE home 노트 · `vault-tree` `docs/business/` 블록)를 같이 실었다 — 한쪽만 적힌 사실은 다음 스윕에서 또 갈린다.

### Added
- 🔴 **`related` 위키링크 표기를 canon 에 신설하고 검출기를 붙였다 — 정본이 없어서 한 필드에 네 표기가 공존했다 (KJP-56).** 근본 원인은 도구가 아니라 **규약의 공백**이었다: `knowledge-convention.md` 는 `related: []` 빈 값만 보여줬고 `skills/dreaming` 은 「related 로 잇는다」는 **의미만** 적어 표기를 아무도 정하지 않았다.
  - 🔴 **따옴표가 규칙이지 블록 리스트가 규칙이 아니다.** PyYAML 실측 2026-08-12로 네 표기를 대조했다 — `related: [[a]], [[b]]` → **ParserError**(frontmatter 전체가 파싱 실패, Obsidian 이 본문으로 렌더) · `related: [[a]]` → `[['a']]` **중첩 리스트** · `- [[a]]`(따옴표 없는 블록) → `[[['a']]]` **역시 중첩** · `- "[[a]]"` → `'[[a]]'` ✅. 블록 리스트로 바꾸는 것만으로는 아무것도 고쳐지지 않는다.
  - 🔴 **조용한 쪽이 더 위험하다는 것이 이 카드의 요지다.** 하드 실패 302건은 화면에 보여서 사용자가 발견했다. 반면 **52건은 파싱을 통과하는데 링크가 아니다** — 오류도 경고도 없고 노트는 멀쩡해 보인다. 검출기가 없으면 영원히 안 보인다.
  - **실볼트 실측 = 354건**(frontmatter 보유 660 중 파싱 실패 303). 프로젝트별: `006_infra-manage` 101 · `013_kj-plugins` 69 · `002_cc-setting` 67 · `008_sns` 19 · `011_ai-video-confront` 18 · `007_youtube-stts` 16 · `004_multi-stt` 16 · `003_clone-moshi` 15 · `009_make-video` 13 · `010_ai-video-creation` 8 · `012_plane-new-function` 8 · `005_immersivet` 2. **볼트 수선은 이 카드가 아니다**(별건).
  - 🔴 **검사 범위를 wiki 층에 가두지 않았다 — 실측이 docs 층 2건을 찾아냈다.** `004_multi-stt` 의 ADR 1건과 resources 노트 1건이 같은 결함을 갖고 있다. 이것은 KJP-89 의 「unknown key = warn only」와 **다른 축**이다: 그 판정은 *어휘*(어떤 키 이름이 합법인가)에 관한 것이고, 여기는 **frontmatter 가 파싱되지 않는 문법 결함**이다. wiki 층에만 달았으면 두 건은 영구히 안 보였다.
  - **키 목록을 두지 않았다.** 브리프의 `related|aliases|projects` 3키 대신 **임의 키**를 받는다 — 실측상 결함을 가진 키는 `related` 뿐이고(`aliases`·`projects` 0건), 어떤 스키마에도 list-of-lists 인 키가 없어 bare `[[` 값은 어느 키에서도 옳을 수 없다. 유지보수할 목록이 없으므로 드리프트하지 않는다.
  - 🔴 **YAML 파서는 만들지 않았다 — `:691` 의 「not worth a YAML parser until one appears」 판단은 유지된다.** 사례는 규모 있게 나타났지만(354건) **substr 비교 2개**로 답이 나왔다. 그 주석은 지우지 않고 **재검토 지점과 남은 사각**을 그 자리에 기록했다: 나머지 파싱 실패 1건은 `summary:` 값에 콜론+공백이 들어간 별개 원인이고 이 파일의 어떤 검사도 보지 못한다 — 파서를 저울질할 대상은 그쪽이다.
  - **생산자도 고쳤다** — `skills/dreaming` Link 연산에 출력 형식을 명시했다. 354건을 쓴 것이 이 연산이라, 안 고치면 검출기가 세는 숫자가 계속 는다.
  - **셀프테스트 4 픽스처 · 변이 4종으로 비공허성 증명**(`VALIDATE_SH` seam). 하드 실패·조용한 오독·따옴표 없는 블록 항목은 잡히고 **정본 형태는 조용하다**(마지막 것이 없으면 「`related:` 면 무조건 finding」으로 퇴화해도 통과한다). 변이: wiki 호출부 제거 → wiki 3개만 FAIL · docs 호출부 제거 → docs 1개만 FAIL · 블록 항목 arm 무력화 → 해당 1개만 FAIL · 키 arm 무력화 → 나머지 3개 FAIL.
  - **기존 출력 무변화 · 신규 354줄 전부 이 검사의 것.** stderr 경고 42건은 전후 **바이트 동일**, 스캔 카운트 8종 전부 동일, 기존 finding(`YS-ADR-00001` 홀)도 그대로. 독립 측정(Python + PyYAML)과 검출기 출력이 **파일·줄 단위로 집합이 일치**한다(354 = 354, 차집합 양방향 0).
- 🔴 **docs 층에 폐기키 검사를 만들지 않는다 — 없는 것이 결정이었다 (KJP-89).** 「폐기키 검사가 docs 층을 흘린다」는 전제를 실측으로 **반증**했다. 검사를 신설하지도, unknown-key 경고를 finding 으로 승격하지도 않고, **그 판정을 canon 에 적고 어서션으로 고정**했다.
  - 🔴 **폐기키로 지목된 6건은 규약을 지키고 있었다.** 볼트 feature 3파일의 `version`·`species` 는 stale 이 아니라 **전서 feature 템플릿(`ft-editor` 블록)이 살아있는 필드로 선언한 것**이다 — `version: 0.0.0` · `species: Feature — 기능 명세 (FRD+TDC 통합)` 까지 템플릿과 문자 그대로 일치한다(`PNF_status-cascade_00001.md` 실측). 전제가 근거로 삼은 폐기 선언은 **전서 `adr-editor` 블록 안**에 있어 **ADR 한정**이고, 실볼트 ADR 7건에는 그 키가 하나도 없다.
  - **승격안은 규약이 명시적으로 금지한다.** `project-docs-convention` §Absence semantics 가 「unknown key = warn only, never a hard fail」을 **이유와 함께**(외부에서 들여온 문서 보호) 정해 두었고, `validate-selftest.sh` §docs frontmatter v2 의 legacy 픽스처 주석이 같은 것을 「migration 은 '더 이상 쓰지 않는다'이지 강제 재작성이 아니다 — 그러니 이 중 무엇도 finding 이 되어선 안 된다」로 못박고 어서션으로 지키고 있었다.
  - **승격의 대가를 셌다: 실볼트 unknown-key 경고 42건, 그중 폐기키 0건.** 전부 `related_ticket`·`screen_id`·`summary`·`adr_number`·`methodology` 같은 **살아있는 kind 별 필드**다. 참 양성 0인 규칙을 42 findings 로 바꾸는 것이 곧 무시당하는 게이트다.
  - 🔴 **docs 의 폐기 여부는 kind 별이라 합칠 목록 자체가 없다.** 같은 문자열이 kind 마다 다르다 — feature 는 `version`·`species` 를 살리고 ADR 은 죽인다. `title` 은 더 날카롭다: **wiki 층에서는 finding 이고 docs 층에서는 절대 finding 이 아니다.** 두 목록을 합치면 이 구분이 사라진다(직전 KJP-83 이 `species` 로 같은 것을 실측했다).
  - **어서션 3개 추가 — 판정을 되돌리려는 스윕에서 깨지도록.** docs 경로는 절대 `retired key` finding 을 내지 않는다는 경계 어서션 1개 + legacy 픽스처의 `title`·`owner` 가 stderr 경고에 실제로 도달하는지 2개(종전에는 `kind` 하나만 고정돼 있어 뒤 키가 조용히 스캔에서 빠져도 통과했다).
  - **변이 2종으로 비공허성 증명** (`VALIDATE_SH` seam). **M1** wiki 폐기키 검사를 docs 로 이식(경고 → stdout finding, 문구 `retired key`) → 경계 어서션 **FAIL** · **M2** 인식 키 집합에 `title` 을 몰래 추가 → `deleted title` 어서션만 **FAIL** 하고 `owner` 는 ok(스코프가 정확하다는 증거).
  - **볼트 출력 무변화** — 규약의 deleted fields 8종(`kind`·`title`·`project`·`owner`·`scope`·`feature`·`tags`·`description`)은 실볼트 docs 162파일 어디에도 없다(실측 0건). 볼트 3파일 정리도 **필요 없다** — 위 반증대로 규약 준수 상태다.

### Changed
- 🔴 **전서 §트리 ↔ 레포 `vault-tree.md` 관계를 canon 에 명문화 · 기계 검사는 만들지 않는다 (KJP-90).** KJP-83 이 드러낸 불일치(§트리 `.base` 7종 vs `vault-tree` 4종)의 재발 방지책을 판정했다. `vault-tree.md` 에 §「This diagram vs the transcript's §트리」 신설.
  - 🔴 **KJP-81 의 「두 트리」와 다른 쌍이다 — 표로 갈라 적었다.** KJP-81 축 = **§트리 vs 전서 init 스켈레톤**(도달점 vs 생성물, **의도적으로 다름**). 이 축 = **§트리 vs 레포 `vault-tree.md`**. 둘을 한 이름으로 부르면 「어긋나도 설계다」라는 KJP-81 의 면허가 이 쌍까지 덮어버린다.
  - **관계 판정: 정본과 예시이지 대등한 둘이 아니다.** `vault-tree.md` 의 트리는 **자기 입으로** 「Default layout — **an example, not a location canon**」이라 선언한다. 따라서 **누락은 합법이고 모순만 결함이다.** ⚠ 다만 precedence 를 맹목 적용하지 않는다 — 이 파일은 전서에 없는 **날짜 있는 실측**(예: 도구층 분리 근거 `tool-mcp.md` 31KB vs 3KB, 2026-07-25)을 싣고 있어, 그 지점에서 갈리면 자동 판정 대신 PM 에게 올린다.
  - 🔴 **기계 검사 판정: 만들지 않는다 — 짐작이 아니라 만들어 돌려보고 결정했다.** 이름 집합 diff 프로토타입 실측 2026-08-12: 전서 44 · `vault-tree` 42 · 공통 33 · **차이 보고 19건 중 진짜 3건 = 오탐률 84%.** KJP-82 가 「없는 규약을 검사로 만들면 85건 오탐이 나고 그 검사는 즉시 무시당한다」로 같은 규모를 이미 판정한 바 있다.
  - **오탐 16건은 파서를 고쳐서 없앨 수 있는 종류가 아니다.** ① **플레이스홀더 어휘** — 같은 항목을 `<pp>_YYYYMMDD_<slug>.md` / `<PREFIX>_YYYYMMDD_<slug>.md`, `NNN_<slug>/` / `NNN_<project>/`, `<vault>/` / `<Vault root>/` 로 쓴다(검사를 위해 canon 표기를 통일하는 것은 도구에 맞춰 정본을 고치는 것) · ② **토크나이저 인공물** — `<Vault root>/` 의 공백이 쪼개져 `root>/` 라는 없는 항목이 생기고, `vault-tree` 가 ADR 파일명을 트리 행이 아니라 `adr/` 행의 **주석**에 실어 또 한 건이 뜬다 · ③ 🔴 **부정 표기의 반전 — 이 아이디어를 못 쓰게 만드는 항목.** 전서 트리에는 `✗ legal/ 없음` 처럼 **부재를 명시하는 행**이 있는데, 텍스트 추출은 이것을 「전서에 `legal/` 이 있다」로 읽고 `vault-tree` 가 그것을 빠뜨렸다고 보고한다. **canon 이 말한 것의 정반대를 주장하는 게이트는 없느니만 못하다.**
  - **대신 무엇을 하나 — 관계를 적는 것 자체가 방책이다.** KJP-83 의 실제 문제는 트리가 달랐다는 것이 아니라 **그 차이가 의도인지 아무도 판별할 수 없었다는 것**이다. 인식의 공백이므로 KJP-81 이 다른 쌍에 쓴 것과 같은 처방(관계를 canon 에 적기)이 맞는 처방이다.
  - **프로토타입이 실제로 잡은 진짜 차이 3건은 덮지 않고 기록한다.** ① **고침** — 전서가 ADR 파일명을 `<pp>-ADR-0000N.md` 로 쓰고 있었다(2곳). `<pp>` 는 소문자 위키링크 접두이고, `vault-tree.md` `adr/` 행과 `project-docs-convention` §frontmatter 의 `id: <PREFIX>-ADR-0000N`, 그리고 실볼트 파일명(`YS-ADR-00002`·`PNF-ADR-00001`)이 모두 대문자 프로젝트 접두라 **2 canon + 실물이 한쪽**이었다 → `<PREFIX>` 로 정정 · ② **미해결 보고** — 도구층 내부 모델이 정면으로 갈린다: 전서는 폴더 `cli/ mcp/ plugins/ skills/`, `vault-tree` 는 평면 파일 `tool-{cli,mcp,plugin,skill}.md`. **실볼트에 `999_tools/` 가 없어**(opt-in 미생성, `ls` 확인) 실측으로 중재할 수 없고, `vault-tree` 쪽은 날짜 있는 실측을 근거로 달고 있어 precedence 만으로 접을 수 없다 — PM 판단 대상 · ③ **미해결 보고** — `hippocampus/assets/` 가 `vault-tree` 에만 있고 전서 §트리에 없다.
- 🔴 **feature frontmatter 5필드 필드별 재판정 — 존치 4 · 폐기 1 (KJP-91).** `category`·`depth`·`priority`·`devices`·`feasibility`. KJP-83 은 이 다섯을 **한 덩어리로** 판정해 전부 남겼다(0.2.4 §Changed 의 KJP-83 항목). 이 카드는 같은 다섯을 **하나씩** 갈랐고, 그래서 결론이 갈라졌다.
  - 🔴 **KJP-83 이 근거로 쓴 두 실측은 다섯을 가르지 못한다.** 「읽는 코드 0건」과 「실볼트 feature 3파일 전부 빈 값」은 **다섯에 똑같이 걸린다**(재실측 2026-08-12로 확인: `brain/{skills,scripts,hooks,agents}` grep 히트 0 — 잡힌 3줄은 전부 무관한 영어 산문 · 3파일 3/3 빈 값). 판정 기준으로 쓰면 5폐기 아니면 5존치밖에 안 나온다.
  - **빈 값은 「축이 없다」의 증거가 아니라 「소비자가 없다」의 증거다.** 같은 3파일에서 **유일하게 채워진 필드가 `summary`** 이고, 그것이 **유일하게 기계 소비자를 가진 필드**다(`_index.md`). 사람은 기계가 읽는 칸만 채웠다. 옆칸 `screen_id: []`·`note: ""` 도 같이 비어 있는데 이 둘은 판정 대상도 아니다 — 빈 값이 폐기 사유라면 그 둘도 함께 죽어야 한다.
  - **실제로 가른 기준 = 축이 정의돼 있는가.** 존치 4는 전부 어휘나 의미가 명시돼 있다: `priority`(상\|중\|하 — 전서가 **PRD MoSCoW·티켓 실행 우선순위와 별개 축**이라고 이웃 둘을 지목해 구분) · `feasibility`(가능\|확인필요\|불가능 — minery 실무 어휘와 문자 그대로 일치하는 역검증이 있고 `status`와 직교) · `devices`(열거형 `[mobile, web, ...]`) · `depth`(계층 경로 + 예시 `[회원가입, 간편가입, 카카오]`).
  - 🔴 **`category` 만 폐기 — 주석이 「카테고리」라는 동어반복이고, `depth` 의 첫 원소가 이미 카테고리다.** 어휘도 예시도 없어 채우는 사람이 무엇을 쓸지 알 수 없는 유일한 필드였고, 동시에 **같은 사실의 두 번째 사본**이다. 전서 feature 결정표 자신의 「본문에 재진술하지 않는다(근거는 중복 방지)」 규칙이 frontmatter 안에서도 성립한다. 폐기 대신 `depth` 주석에 「첫 원소가 곧 카테고리」를 명기해 축은 보존했다 — 필드를 지우고 그 축까지 잃는 것이 아니다.
  - 🔴 **폐기키 목록에 등재하지 않는다 — 축 2(KJP-89)가 그 목록이 존재해선 안 됨을 증명했다.** docs 층의 마이그레이션은 「더 이상 쓰지 않는다」이지 강제 재작성이 아니므로, 템플릿에서 빼는 것으로 끝난다. **기계적 영향 0**: `category` 는 전·후 모두 `validate.sh` 의 인식 키가 아니라 unknown-key **경고**로 처리된다 — 실볼트 출력이 문자 하나 바뀌지 않았다(게이트 diff 로 확인).
  - **볼트 3파일 정리 불필요** — 위 규칙대로 기존 파일은 그대로 두어도 finding 이 아니다. 손댄 곳: 전서 feature 템플릿(`category:` 줄 삭제 · `depth` 주석 보강) · feature 결정표 SSOT 행. 레포 다른 곳에 `category` 참조는 **0건**(grep 실측).
- 🔴 **`.base` 7종 폐기 — 못 만든 것이 아니라 안 만들기로 했다 (KJP-83).** 사용자 판정 2026-08-12: 「저 base 는 사람이 보기 위함이야. 필요 없으면 그냥 없애자, 어차피 `sl` 로 보면 되니까.」 §트리의 7종(`projects`·`feature`·`adr`·`p_memory`·`neocortex`·`hippocampus`·`tools`)과 **그것에 기대던 근거 문장**을 전서·`vault-tree`·`skills/dreaming` 에서 함께 걷어냈다.
  - 🔴 **「content spec 부재」는 폐기 사유가 아니다** — 이전 카드 문구를 물려받지 않았다. 포맷은 Obsidian Bases 의 것이라 **발명할 것이 없었고**, 볼트 실물이 동작 형태를 이미 보여주고 있었다. 만들 수 있었는데 **필요 없다고 판정한 것**이다.
  - **폐기 근거 = 실사용 부재이고, 그 증거가 유일한 실물 자신이다.** 볼트 전체에 `.base` 는 1건(`hippocampus/_sessions_overview.base`, `find` 실측)뿐인데 필터가 0.2.0 에서 개명된 **구 폴더 `sessions`** 를 가리킨다. 그 경로는 지금 존재하지 않아(`ls` → `No such file or directory`) **빈 표를 낸다** — 깨진 채 방치돼 있었다는 사실이 아무도 쓰지 않았다는 증거다.
  - **삭제 전 원문 보존** — `hippocampus/` 는 gitignore 라 지우면 git 에 사본이 남지 않는다. 전문 10줄(`wc -l` = 10):
    ```yaml
    views:
      - type: table
        name: 표
        filters:
          and:
            - file.inFolder("sessions")
        order:
          - file.name
          - project
          - status
    ```
    🔴 **볼트 실물 삭제는 이 커밋 범위 밖이다**(레포 축만 처리) — 원문이 위에 남았으므로 삭제해도 복원 가능하다.
  - 🔴 **딸려 죽는 근거 3곳을 함께 고쳤다 — 줄만 지우면 무근거 주장이 canon 에 남는다.**
    - **`species` 폐기 판정은 유지하되 이유를 다시 썼다** (`:1052`). 종전 근거가 「`.base` 가 `file.inFolder` 로 필터」라서 **폐기의 근거 자체가 `.base`** 였고, `.base` 가 죽으면 붕 뜬다. 새 근거는 **경로가 곧 종류**라는 것 — `docs/adr/` 와 파일명이 이미 ADR 이라고 말하는데 필드는 같은 사실의 3번째 사본이다. 실측으로 세웠다: 읽는 코드 **0건**(`grep -rn species brain/skills/`), `validate.sh:723` 의 docs 인식 키는 `status|updated|id|source|readonly|synced|history` 뿐이라 **린터조차 이 필드를 모른다**(unknown key 경고 대상), 실볼트 ADR **7건 전부 이 필드가 없다.** `.base` 는 그 사실의 소비자였을 뿐 근거가 아니었다.
    - **feature frontmatter SSOT 주장은 살아남되 범위가 줄었다** (`:829`·`:854`). 「집계 필드의 유일 SSOT(`feature.base` 원료)」에서 **괄호 안 소비자는 죽었다.** 규칙의 실체는 뷰어에 원료를 대는 것이 아니라 **본문 재진술 금지**(중복 방지)라 뷰어와 무관하게 성립한다. 다만 **기계로 읽히는 축은 `summary` → `_index.md` 하나뿐**임을 명기했다 — `category`·`depth`·`priority`·`devices`·`feasibility` 는 읽는 코드가 없고 실볼트 feature 3파일에서 **전부 빈 값**이다. 필드는 사람이 채우는 기획 축으로 남기되 **기계 집계를 약속하지는 않는다.**
    - **두 트리 설명은 `.base` 절만 걷어냈다** (`:1323`·`:1355`·`:1366`). 「§트리가 더 넓다」는 설명은 상황적 문서(`MILESTONE`·`COMPLIANCE`·`DESIGN`)만으로 성립한다. `:1366` 의 「content spec 이 없어 대상 아님」은 **판정이 바뀌었으므로** 「폐기돼 트리에서 사라졌으므로 스캐폴드 논의 대상 자체가 아님」으로 교체했다.
  - **부재 진술에서도 뺐다** (`:954` · `skills/dreaming/SKILL.md:47`). `.base` 가 전역 폐기되면 「`dream-logs` 엔 `.base` 가 없다」는 대비가 무의미해진다 — 폴더·`_index.md` 부재 진술은 그대로 유지.
  - 🔴 **전서 7종 대 `vault-tree` 4종 불일치가 이미 있었다** — `vault-tree.md` 에는 `adr.base`·`tools.base`·`projects.base` 가 **애초에 없었다**(실측 4줄: `:53`·`:59`·`:84`·`:98`). 폐기가 그 불일치도 함께 닫았다.
  - **0.2.3 §Changed 의 KJP-85 항목(「설계 전서의 두 트리를 명시」)은 손대지 않았다** — 그 줄의 「상황적 문서·`.base` 포함」은 **당시 §트리가 그랬다는 기록**이다. 기록을 현재에 맞춰 고치는 것은 정합이 아니라 위조다(`vault-tree.md` §Renaming a path term).
  - 손댄 곳: 전서(`:145`·`:168`·`:170`·`:189`·`:194`·`:201`·`:206`·`:210`·`:216`·`:222`·`:829`·`:854`·`:954`·`:1052`·`:1323`·`:1355`·`:1366`) · `vault-tree`(`:53`·`:59`·`:84`·`:98`) · `skills/dreaming/SKILL.md:47`.
- 🔴 **Huly 하드코딩 제거 — 벤더가 아니라 `ticket-system` 설정 키를 가리킨다 (KJP-86).** Huly 인스턴스가 2026-08-12 내려갔다. 🔴 **답은 "Plane 으로 치환"이 아니다** — brain 은 어느 프로젝트에나 설치되는 플러그인이라 벤더를 박으면 안 된다. 예외는 이 레포 자신을 말하는 문장뿐.
  - **16건을 `vault-tree.md` §Renaming a path term 5분류로 판정 — 고친 것 12 · 보존한 것 4.** "0건"은 목표가 아니다; 기록을 현재에 맞춰 고치는 것은 정합이 아니라 위조다.
  - **고친 것**: 전서 MILESTONE 템플릿·주석 4곳(`:398`·`:414`·`:422`·`:423`)과 feature `priority` 축 3곳(`:830`·`:848`·`:856`) → **사용자 프로젝트로 나가는 템플릿**이라 "티켓 시스템"으로 일반화 · `skills/init`·`skills/onboard` 크레덴셜 예시 `~/.config/claude/huly.env` → `<system>.env` · `onboard` 질문 선택지 → Plane 추가 + **열린 목록임을 명시**(벤더가 죽을 때마다 쓸어야 하는 유지보수 부채 자체를 제거) · `ROADMAP` 티켓 접점 헤딩 → Plane.
  - **보존한 것 4곳** (`git diff` 로 무변경 증명): `CLAUDE.md:11-12`(이관 사실의 기록) · `CHANGELOG:270`(KJP-23 당시 init 이 그렇게 적고 있었다는 기록) · `docs-samples/MILESTONE.md:14`(2026-07-25 스냅샷, 배너 있음).
  - 🔴 **`ROADMAP` 은 헤딩만 고치고 티켓 번호는 고치지 않았다.** 그 목록은 2026-08-05 에 쓰였고 이관은 08-12 이라 **KJP-52·59·60·61 은 Huly 시절 번호**다. 헤딩만 Plane 으로 바꾸면 Huly 번호를 Plane 번호라고 주장하는 셈이라, 경고 주석을 달고 재매핑은 **Plane 조회로 확인한 뒤**로 남겼다 — `N−4` 산술만 믿고 고쳐 적으면 검증되지 않은 번호가 정본이 된다.
  - **`validate-selftest.sh:53` 픽스처는 바꿔도 무의미함을 실측하고 바꿨다.** `validate.sh:130` 은 `related_ticket` 을 **존재만** 검사한다(필수키 split) — 값도 벤더 접두도 보지 않고, 같은 파일의 다른 픽스처들은 이미 맨 `t` 를 넘긴다. 그래서 이 값은 `<system>:<id>` 형태의 예시일 뿐이고, 픽스처의 PREFIX 가 이미 `KJP`(이 레포)라 `plane` 으로 맞췄다. 근거를 주석으로 코드 옆에 남겼다.
- 🔴 **스텁 6종 → 5종 전파 — `init` 이 아직 `BUSINESS` 를 만들고 있었다 (KJP-85).** KJP-86 이 `BUSINESS` 를 해체(§BM → `PRD`, §GTM → `MARKETING.md`)했는데 **스캐폴드 생산자와 트리가 따라오지 않았다.** 판정은 새로 하지 않았다 — 답은 이미 `doc-catalog.md` 에 있었다: `MARKETING` 은 자리를 물려받지 않는다, **GTM 은 프로젝트 생성 시점에 필요한 것이 아니기 때문**이다(`MILESTONE`·`COMPLIANCE`·`DESIGN` 이 상황적인 것과 같은 이유). 이 카드는 그 판정의 **전파**다.
  - 🔴 **실효 결함은 `skills/init/SKILL.md:100` 하나뿐이었다** — 나머지는 서술이지만 이 줄은 **스캐폴드를 실제로 생산**한다. 6종 → 5종, `business/` 2 → 1.
  - **전서 자기모순 해소.** `:217` 이 "BUSINESS 한 파일 2절 모델 폐기 · 6번째 자리는 «스킬» 탭 `skills/init` §4 축에서 판정"이라 위임해 놓고 `:220`·`:1366` 은 스텁 6종에 `BUSINESS` 를 그대로 넣고 있었다. 위임받은 축이 5종으로 답했으므로 세 곳을 그 답으로 닫았다.
  - 🔴 **`MARKETING.md` 가 정본 트리에 아예 없었다** — KJP-86 이 만든 문서가 `vault-tree.md` `business/` 블록에 등재되지 않았다. `MILESTONE`·`COMPLIANCE` 와 같은 표기로 상황적 문서 등재.
  - **덤으로 잡힌 2건** (같은 `BUSINESS` 잔재 축): `init` 이 `API_SPEC` 생성 주체를 아직 `dreaming` 이라 적고 있었다 — canon 은 **PM 위임 동기화 워커**이고 `dreaming` 은 `docs/` 를 쓸 수 없다(KJP-77). 전서 `:1587` 은 `value-axis-drift.sh` 가 `"[[BUSINESS]] §BM 링크로 교체"` 를 낸다고 적고 있었는데 **실측 출력은 `[[PRD]] §BM`** 이다(힌트는 표의 home 열 파생, KJP-86 이 home 을 옮겼다).
  - 손댄 곳: `skills/init/SKILL.md` · `skills/onboard/SKILL.md` · `vault-tree`(`business/` 블록 · 네이밍 예시) · `doc-catalog`(머리말 개수 · §Core 머리말 · MARKETING 유보 노트) · 전서(`:217`·`:220`·`:1364`·`:1366`·`:1587`).
- 🔴 **feature 문서 모델을 전서에 맞춘다 — 기능당 폴더+2파일이 아니라 파일 1개 (KJP-84).** `FRD.md`·`TDC.md` 를 `docs/develop/feature/<pp>_<slug>_0000N.md` **단일 문서**로 통합. 전서(`.artifact/brain-0.2.0.html:188` §트리 · `:826` §양식 feature 결정표)는 처음부터 통합 모델이었고 레포 canon 4곳이 폐기된 폴더형을 말하고 있었다.
  - 🔴 **볼트가 이미 통합 모델로 살고 있었다** — 실측 2026-08-12: `RSS_ui-redesign_00001.md` · `PNF_status-cascade_00001.md` · `MOSH_host-key-trust_00001.md` 3건이 기능별 폴더 없이 `feature/` 직속에 있고 `id:` 도 파일명과 같다. **문서만 뒤처져 있었다.**
  - **`<fp>` 는 정의된 적이 없었다.** 전서 전체에서 `:188`(`<pp>_<fp>_0000N.md`)과 `:843`(`id: <PP>_<FP>_00001`) 두 곳에만 나오고 뜻이 어디에도 없다 — 대조군인 `<pp>` 는 `:200` 이 "접두는 위키링크 전역 유일성용"으로 설명한다. **정의를 새로 쓰는 대신 토큰을 없앴다**: 위 3개 실측 파일이 전부 kebab 슬러그였고, `<slug>` 는 같은 트리의 `p_memory`·`hippocampus` 행이 이미 쓰는 어휘다. 정의가 필요한 토큰을 새로 세우는 것이 곧 드리프트다. 두 곳의 대소문자 불일치(`<pp>` vs `<PP>`)도 같이 맞췄다 — `:843` 주석이 "파일명과 일치"라고 요구한다.
  - **kind `frd`·`tdc` → `feature` 1종.** kind 는 **경로에서 파생**되는데(`project-docs-convention` §kind ← path matrix) 경로가 하나면 kind 둘을 뽑을 수 없다 — 매트릭스가 판정 불능이 된다. **기계적 영향 0**: `validate.sh` 는 feature 경로를 매치한 적이 없다(경로 분기는 `*/docs/adr/*` 와 `*/docs/develop/API_SPEC.md` 둘뿐 — `:667-668`, `frd`·`tdc` grep 히트 0).
  - **충돌 서열 6티어 → 5티어.** `… > PRD > FRD > TDC` 는 두 파일이 하나가 된 순간 **문서가 자기 자신보다 상위**라는 뜻이 된다. `… > PRD > feature` 로 접고, 규약이 "새 랭크는 실제 충돌이 날 때만"이라고 못박아 둔 조항과 구별되게 **"문서가 사라져서 줄어든 것이지 랭크를 불필요하다고 판정한 것이 아니다"**를 명기했다.
  - 손댄 곳: 전서(`:188`·`:269`·`:587`·`:843`) · `project-docs-convention`(kind 매트릭스 · §stub 개수 · §TDC 절 개편 · §Policy 인용 · §ID Issuance · 서열) · `doc-catalog`(location 규약 · CODE_CONVENTION 행 · DESIGN 행 · MILESTONE 경계 · Feature 표) · `vault-tree`(트리 · 네이밍 예시) · `doc-templates`(DESIGN 제목 · MILESTONE 경계·표) · `sessions-note-convention`(문서 열거) · `README`(트리).
- 🔴 **정책 모델을 하나로 확정 — `docs/develop/P_POLICY.md` 단일 파일 + `## POL-NNN` 조항 헤딩 (KJP-79).** 전서·`project-docs-convention`·`doc-catalog`·`vault-tree`·`validate.sh` 다섯 곳이 이제 같은 모델을 서술한다. 종전에는 전서가 단일 파일을, 나머지가 `docs/policy/<PREFIX>-POL-0000N` **2티어 폴더형**을 말해 「스텁 6종」을 아무도 정확히 열거할 수 없었다.
  - 🔴 **전서는 이미 모델 전체를 명세하고 있었다** — 조항 헤딩 계약 · 번호=파일 내 연번(PM 발급) · 단일 기능 규칙은 feature 파일로 · 충돌 서열 + org 승격 · 비대 시 자연 분리. **설계할 것이 없었고 나머지 문서가 못 따라간 것이었다.**
  - **고친 트리는 §스킬 init 스켈레톤이다 — §트리는 손대지 않았다.** 근거: 스켈레톤 자신이 **2줄 간격으로 모순**이었다. `docs/{business,develop,develop/feature,adr,resources}/` 를 스캐폴드해놓고 바로 다음 줄에서 **자기가 만들지도 않는** `docs/policy` 의 `next_id` 를 규정했다.
- **`<PREFIX>-POL-0000N` ID 체계 폐지.** ID 는 *파일*을 식별하는 장치인데 정책이 더 이상 파일이 아니다. 불변성 논거도 전제(티어 간 승격 = 파일 이동)가 사라져 보호할 대상이 없다. **ADR 은 파일당 1결정이라 그대로** — `next_id` 는 이제 볼트에서 `docs/adr/` 하나뿐이다.
- **정책 승격 개념 폐지.** 프로젝트 안에 티어가 하나뿐이라 승격할 사이가 없다. `project-docs-convention` 의 깨진 포인터(`Promotion → knowledge-escalate-convention`)는 **재지정이 아니라 삭제** — 그 문서는 지식 사다리(`hippocampus` → `p_memory` → `neocortex`)이고 정책을 담은 적이 없으며, **스스로 공통층은 승격 티어가 아니라고 명시**한다. org 공통 `policies/` 로의 상향은 PM 판단이라 규약 본문에 직접 서술했다.
- **`validate.sh` 의 정책 경로 특별취급 제거.** `*/docs/policy/*` 분기가 사라지고 multi-instance `id:`/`next_id:` 규칙은 **ADR 전용**이 됐다. 빨간불(픽스처 선행 변경) → 초록불 관측, 변이 테스트로 두 어서션이 규칙의 존재를 실제로 검출함을 증명.
- **볼트 `docs/policy/` 10폴더 폐기** — 내용이 `_index.md` 뿐이라 손실 0. 🔴 **9개가 아니라 10개였다** — 기능 티어 1건(`012/docs/develop/feature/status-cascade/policy/`)이 추가로 있었고, 전서상 feature 는 폴더가 아니라 **파일**이라 같은 축으로 함께 폐기했다. 린터 docs 축 −10(삭제 수와 일치), sessions·wiki·entries 불변.

### Docs
- **`docs-samples/` 에 스냅샷 배너.** 2026-07-25 검토안이고 이후 0.2.0 랜딩이 **세 축에서 지나쳤다**(정책 모델 · `docs/develop/` 경로 중첩 · API_SPEC 생성 주체). 🔴 **고치지 않고 배너만 단다** — 기록을 현재에 맞춰 고치는 것은 정합이 아니라 위조다(`vault-tree.md` §Renaming a path term).

## 0.2.3 — 2026-08-12

### Fixed
- 🔴 **`init` 이 구 `docs/` 트리를 만들고 있었다 — 생산자가 정본과 어긋나 있었다 (KJP-63 후속).** `docs/{tech-design,business,policy,adr,research,feature}` → **`docs/{business,develop,develop/feature,adr,resources}`** + 빠져 있던 `docs/_index.md`. 스텁 6종도 재배치(`PRD` → `business/`, `ARCHITECTURE`·`CODE_CONVENTION`·`RUNBOOK`·`THREAT_MODEL` → `develop/`).
  - **`KJP-63`(0.2.0 용어 이행)이 놓친 축이다.** 그 카드의 치환 축 표가 4행(`knowledge/`·`sessions/`·`candidates/`·`000_common`)이었고 **`docs/` 하위 구조가 거기 없었다.** 폴더 이름만 보고 트리 구조를 안 본 결과다 — 볼트를 손으로 옮겨봤자 `init` 이 다시 구 구조를 만들었을 것이다.
- **API_SPEC 미러 계약이 canon 경로에서 미검사였다 (KJP-63 후속).** `validate.sh` 가 `*/docs/tech-design/API_SPEC.md` 를 키로 잡고 있어, canon 위치(`docs/develop/`)에 놓인 미러는 `source:`·`readonly: true` 강제를 **조용히 빠져나갔다.** 픽스처를 canon 경로로 옮기자마자 빨간불이 뜬 것이 그 증거다.

### Added
- **`validate.sh` docs 층·프로젝트 허브 `_index.md` dangle 검사 (KJP-82 후속).** 실볼트 실측: `_index.md` **113개 중 린터가 읽던 것은 19개**뿐이었다 — **93개가 무검사**였고, 그 층을 전부 망가뜨려도 `OK — no issues` 가 나왔다. 요약 줄에 `docs indexes` 별도 카운트 추가.
  - 🔴 **줄 형식·coverage 는 wiki 층에만 유지한다.** docs TOC 는 산문과 `next_id:` 를 정당하게 싣고, 형식 정본이 문서에 존재하지 않는다(`knowledge-convention.md` 의 줄 형식은 **메모리 노트** 규약이고 docs 적용 근거가 없다). 없는 규약을 검사로 만들면 85건 오탐이 나고 그 검사는 즉시 무시당한다 — 기존 제외 주석이 경고하던 바로 그것이다.
  - 변이 테스트 5종으로 비공허성 증명. **M2 가 공허한 어서션 하나를 잡아냈다** — 허브 픽스처 불릿이 전부 canon 형식이라 「허브 산문은 finding 이 아니다」 어서션이 아무것도 검증하지 않고 있었다.

### Changed
- 🔴 **경로 용어 스윕은 라이브 포인터에만 적용한다 (`vault-tree.md` 신설 절).** 폴더 개명 시 옛 문자열이 산문에 남는 이유는 다섯 가지이고 **오직 첫째만 스윕 대상**이다 — 살아 있는 포인터 / 과거 사건·실측 기록 / 외부 도구의 실경로 / 노트 안의 코드 예시 / 노트의 주제어 자체.
  - 실측 2026-08-11 (볼트 산문 54건, 줄별 분류): **라이브 26 · 보존 20 · 보류 4 · 오탐 4.** 기계 치환은 **절반 이상에서 틀렸을 것**이고, 그중 넷은 다른 도구의 실경로였다. 그리고 — 규칙을 확정한 사례 — **둘은 「경로 스윕은 한 방향을 놓친다」고 경고하는 노트였고 본문이 `sessions/` → `hippocampus/` 개명을 예시로 인용한다.** 일괄 치환은 그 스윕을 경고하는 노트를 파괴했을 것이다.
  - **「0건」은 목표가 아니다.** 0에 도달한 기계 치환은 이력을 덮어쓴 것이다.
- **raw 층 비추적의 내구성 귀결을 명문화 (`vault-tree.md`, KJP-80).** 「Outside git」은 **머신에 백업이 없으면 그 층에 백업이 없다**는 뜻이다. 볼트별 예외가 답이 아니고 **OS 레벨 백업**(Time Machine 등)이 담당한다. 이 문장이 여기 있는 이유는, 이전 예외가 `.gitignore` 주석으로만 살아서 일괄 마이그레이션이 **아무도 모르게 덮었기** 때문이다.
- **설계 전서의 두 트리를 명시 (KJP-85).** §트리 = 볼트가 **도달하는** 모습(상황적 문서·`.base` 포함) · §스킬 init 스켈레톤 = `init` 이 **만드는** 것(의도적으로 더 좁다). **교환 불가인데 그 사실이 어디에도 없었다** — §트리만 읽으면 `init` 이 상황적 문서를 미리 만들어 pre-creation 규칙을 위반한다.
- **`business/legal/` 충돌 해소 — 규약이 이겼다 (KJP-85).** 전서 §트리는 이용약관·개인정보처리방침을 볼트에 두라 했고 `project-docs-convention.md` 는 **레포에 두고 볼트에 복사하지 않는다**고 했다. 규약 승, 근거 4: ① 충돌 중재 정본이 그 규약이다 ② 규약이 API_SPEC 미러를 **"the sole exception"** 이라 못박았다 ③ 드리프트 사유가 legal 에서 **가장 강하다** — 전서 자신이 `effective:`(시행일)를 요구하므로 사본 두 벌은 불편이 아니라 **책임 문제**다 ④ 전서가 그 경로를 "공개 게시 원본"이라 불러 미러 예외로 구제되지도 않는다. 실볼트 `legal/` **0건**이라 이전 비용도 없었다.

## 0.2.2 — 2026-08-05

### Added
- **`validate.sh` `_index.md` 커버리지 검사 — dangle 의 역방향 (KJP-82).** 어떤 폴더 인덱스도 이름을 부르지 않는 wiki 층 노트를 `file:1: uncovered note: [[stem]]` 로 보고한다.
  - **두 방향은 대칭인데 커버리지가 더 조용하다.** dangle 은 recall 이 *없는 노트가 있다*고 믿게 하고, 커버리지 누락은 recall 이 *있는 노트를 영원히 못 보게* 한다 — 파일은 멀쩡하고 린터도 조용하다. `KJP-84`(`onboard` 가 `init` 과 다른 폴더에 머신 노트를 쓴 건)가 정확히 이 실패 모드의 실재 사례였고, 검사가 없어서 다른 카드의 드라이런 중 우연히 발견됐다.
  - **검사 `a` 와 같은 두 리스트(노트 목록 · 인덱스 목록) 위에 세웠다** — 스코프와 제외 규칙(`_index.md`·`index.md`·`0.*`·`dream-logs.md`)이 공유 데이터지 규칙의 두 번째 사본이 아니다. `hippocampus/` 와 `NNN_*/docs/` 는 밖에 남는다.
  - **링크 해석은 실볼트에서 측정한 두 형태를 따른다** — bare stem → 인덱스 자신의 폴더(483건) · 볼트 상대 경로 → 볼트 루트(8건). 🔴 **경로 형태가 있기 때문에 커버리지를 폴더 국소로 판정할 수 없다** — 그렇게 하면 `org/machines/clients/**` 의 살아 있는 노트 8건이 거짓 보고된다(자기 인덱스가 없고 상위 `_index.md` 가 덮는다).
  - 요약 줄에 `entries`(인덱스에서 수확한 고유 링크 대상 수) 추가. **검출 전용** — 인덱스를 쓰지 않는다(KJP-77).
  - **검증** — 빨간불 8건 → 초록. 🔴 **변이 테스트 3종**: M1 커버 집합 비우기 → 조용해야 할 어서션 8건 전부 발화(비공허 증명) · M2 볼트상대 분기 제거 → **정확히 2건**만 발화(외과적) · M4 `LC_ALL=C` 제거 → 비ASCII 노트가 스캔에서 조용히 사라지고 검사가 침묵(「0건 스캔이 깨끗한 볼트로 위장」 재현).
  - 🔴 **실볼트 in-situ 비공허성** — findings **0**. 그 0이 스킵이 아님을 M1 을 실볼트에 돌려 증명했다: **491 uncovered / 0 entries**, 즉 노트당 1건. **0 은 491대491 실측 일치**이고, KJP-74 의 마이그레이션이 커버리지 방향으로도 옳았다는 뜻이다.
  - **카운트 줄은 이 붕괴를 못 잡는다** — `n_wiki` 는 다른 리스트에서 계산되므로 노트 리스트의 붕괴가 안 보인다. 픽스처가 잡는다(M4 로 증명). **카운트와 픽스처는 서로 다른 것을 지킨다.**

## 0.2.1 — 2026-08-05

### Fixed
- 🔴 **`validate.sh` 타깃 집합 `sort -u` 의 로케일 붕괴 (KJP-74).** 일부 UTF-8 collation 로케일에서 macOS `sort` 는 **primary collation weight 가 없는 문자열을 서로 같다고 판정**하고 `-u` 가 하나만 남긴다. 실측 2026-08-05, 같은 입력 `가.md 나.md 다.md 라.md`:

  | 로케일 | 결과 |
  |---|---|
  | **`en_US.UTF-8`** (이 머신의 ambient `LANG`) | **1줄** |
  | `ko_KR.UTF-8` | 4줄 |
  | `C` | 4줄 |

  🔴 **「UTF-8 대 C」가 아니라 로케일별이다** — 즉 위험이 ambient `LANG` 에 달려 있고, 그래서 환경에 맡길 수 없다. 실볼트 `013_kj-plugins/p_memory` 74파일이 73개 타깃으로 줄어 **살아 있는 노트를 dangling `_index` 링크로 오보**했다.
  - 오보 방향이라 실제 dangle 을 숨기지는 않지만, **「0이어야 하는 게이트」를 통과 불가로 만든다.**
  - 수정 = 타깃 집합을 만드는 그 한 줄에 `LC_ALL=C`. 집합 멤버십에 필요한 것은 바이트 순서지 사람이 읽는 정렬 순서가 아니다.
  - 회귀 픽스처를 붙였다 — `가/나/다/라` 4노트 + stem 당 `assert_no_match` 1개. **수정을 되돌리면 정확히 3건이 발화한다**(마지막 하나만 살아남는 것이 이 버그의 모양이다).
  - **같은 계열의 선례가 이 프로젝트에 이미 있었다** — `git ls-tree | grep -c` 가 비ASCII 경로 인용 때문에 311을 144로 세어 멀쩡한 백업을 실패로 판정할 뻔한 건. **비ASCII 경로에서 텍스트 도구의 판정은 로케일·인용을 고정하기 전까지 믿지 않는다.**

## 0.2.0 — 2026-08-05

> 🔴 **이 컷은 레포 축만 랜딩한다.** 볼트 데이터 마이그레이션(`KJP-74`)은 아직이다. 순서를 이렇게 잡은 것은 실측 때문이다 — 볼트를 먼저 옮기면 **구 설치본이 `sessions/` 에 다시 쓴다**(죽은 폴더가 되살아나고 세션 층이 쪼개진다). 릴리스를 먼저 하면 스캔이 못 찾을 뿐이고(읽기 실패, 시끄러운 0), 그 사이 `ss` 가 쓰는 곳은 `hippocampus/` — 어차피 목적지다. 형제 카드 `KJP-68` 의 선행조건이 「볼트 완주」라 적혀 있었으나 그것은 볼트 데이터와 레포 코드를 섞어 적은 것이다.

### Added

- **`validate.sh` 신규 검사 3종 — 볼트 마이그레이션의 안전망 (KJP-65).**
  - **`_index.md` 줄 형식 + dangle 링크.** 0.2.0 이 만든 유일한 새 실패 모드에 대한 유일한 검출기다. recall 이 폴더 인덱스만 주입하므로, 없는 파일을 가리키는 인덱스 줄은 깨진 링크가 아니라 **거짓 목록**이다 — 이후 모든 세션이 그 노트가 있다고 믿고 아무것도 반증하지 않는다. 스코프는 wiki 층 TOC = 노트 스캔과 같은 루트이며, `hippocampus/`(raw 층, recall 대상 아님)와 `NNN_*/docs/`(TOC 가 산문·`next_id:` 를 정당하게 싣는다)는 **각각 dangle 픽스처로 경계가 고정**돼 있다. 링크 해석은 볼트 상대 경로와 파일명 stem 2형태(Obsidian 이 실제로 푸는 형태). **`aliases:` 는 의도적으로 수집하지 않는다** — 개명 시 인덱스를 같은 커밋에서 갱신하라는 것이 canon 이라, 구 이름을 가리키는 줄은 Obsidian 이 열어주더라도 위반이다. **검출 전용**이며 재생성 주체를 전제하지 않는다(사후 재생성은 KJP-77 로 폐기).
  - **`.brain-paths` `schema_version`.** 매니페스트가 축을 선언하면서 그 값이 어느 세대의 스키마인지 말하지 않으면, 「없는 키 = 문서화된 기본값」이라는 계약 자체가 성립하지 않는다 — 리졸버는 구 기본값으로 떨어지고 모든 스캔이 조용히 0이 된다. `vault-paths.sh` 가 태어난 그 사고다. **매니페스트 자체의 부재는 침묵**(부재 = 기본값 = 합법).
  - **`summary:` 부재는 이미 착륙해 있었다** — KJP-63 의 `title` → `summary` 전환이 검사 `b` 를 통째로 구현했고 빈 값도 이미 거부하고 있었다. 이번에 추가된 것은 빈 값 전용 픽스처뿐이다.
  - **`neocortex/` 가 공유표면 스캔에 들어왔다.** 빠져 있던 것은 판정이 아니라 잔여물이었다 — 린터 이행 6단계가 그 스캔을 **개명만 하고 확장하지 않았다.** 축이 질문을 끝낸다: `neocortex/` 는 git 추적 공유 층이라 거기 적힌 세션 wikilink 는 `docs/` 의 것과 똑같이 팀원 볼트에서 끊긴다. 그 결과 **tools root 만이 wiki 스키마 스캔 안 · 공유표면 밖에 남고**, 두 루트의 차이는 git 추적 여부 하나로 환원된다.
  - **요약 줄 라벨 `knowledge` → `wiki`, `indexes` 카운트 신설.** 전서가 이 층을 `wiki` 라 부르고 이 스크립트 헤더도 이미 그렇게 쓰고 있었다 — 카운트 줄만 폐어가 살아남아 있었다. 새 스캔에 카운트를 붙인 것은 같은 규율이다: 카운트 없는 신규 스캔은 「0건 스캔이 깨끗한 볼트로 위장」하는 바로 그 구멍이다.
  - **검증** — selftest ok 185 / FAIL 0. 빨간불 17건 관측 후 초록. 🔴 정숙 어서션의 비공허성은 **변이 테스트 3종**으로 별도 증명했다(픽스처와 어서션을 같은 커밋에서 함께 쓰면 「조용해야 할 픽스처가 말했다」 증거가 구조적으로 안 나온다). 실볼트 스모크 exit 0 · 53 findings · 1.5s.
- **`agents/researcher.md` — 네 번째 프로필 (KJP-64).** 레포 **밖** 근거 조사 전용(레포 안 검색은 `Explore` 의 몫). `Write`·`Edit`·`NotebookEdit` 차단. 신설 근거는 실측된 실패다 — DESIGN 이 외부 리서치 0건으로 r16 사인오프까지 통과했고, 사용자 지적으로 뒤늦게 첫 조사를 돌리자 값 축이 전복됐다. 🔴 **리서처가 없어서가 아니라 필요하다는 걸 아무도 떠올리지 않아서 난 실패다** — 그래서 프로필 목록 자체가 PM 의 체크리스트다. First action = 스코프 게이트(범위를 한 줄로 적어 PM 확인 후 착수, 확인 전 본조사 금지) · 출처 우선순위 1차 원본 → 벤더 문서 → 커뮤니티, 블로그 단독 인용 금지 · 반증 의무(주장마다 뒤집는 자료 1건, 없으면 `반증 자료 없음` 명시) · 인용 = URL + 접근일 + 그 문서의 버전/날짜.
- **`docs/security-audit.md` (KJP-64)** — `verifier` 가 보안 브리프에서 읽는 본문. 원료는 `cso` 스킬의 Phase 0–13 이며, 하네스로 옮기며 gstack 전용 기계(설정 CLI·학습 저장소·리포트 저장·텔레메트리·수정 로드맵 질문)를 걷어내고 report-only 경계를 절차로 재확인했다. **본문이 `verifier.md` 에 들어갈 수 없는 이유는 상한이다** — 에이전트 정의는 spawn 마다 100% 주입되므로 정의는 가리키고 본문은 문서에 둔다.
- **`## KERNEL-BEGIN` … `## KERNEL-END` 블록이 네 정의 전부에 랜딩 (KJP-64).** 0.1.8 이 알려진 결함으로 기록한 `missing KERNEL block` 3건이 닫혔다. `scripts/kernel-check.sh` → `OK — 4 agents, KERNEL identical (6 lines)`, exit 0.

### Changed
- 🔴 **`Docs draft` 는 KERNEL 에 들어가지 않는다.** `verifier` 는 report-only 라 문서 초안을 산출하지 않으므로 **넷에 전부 참인 규칙이 아니다.** 넷에 전부 참인 것만 넣는다 — **바이트 일치보다 정확성이 이긴다**(일치는 그 결과로 자동 성립한다). 실측: 절을 실제로 지는 파일은 **`worker`·`coder` 둘**이다. 전서는 report-only 인 `verifier` 만 예외로 보아 "넷 중 셋"이라 적었으나(전서 §KERNEL), **전서의 `researcher` 초안 자체에 절이 없다** — 22줄 상한이 이미 꽉 차 있다. `researcher` 가 절을 져야 하는지는 미결이며, 상한을 올리지 않고는 넣을 수 없다.
- **KERNEL 의 Handoff 고정형에서 `Fixed` 가 빠졌다** — `Done / Mistake / Learned / Outputs / Risks / Next / Ask` 7항목. 종전 세 정의는 `Fixed` 를 포함한 8항목이었다.

- **0.2.0 용어·구조 전면 이행 — 레포 축 (KJP-63).** `knowledge/` → `p_memory/` · `sessions/` → `hippocampus/` · `candidates/` 폐기 · `000_common` 리터럴은 `BRAIN_COMMON` 리졸버 경유. 폐기 키 11종의 canon 정의를 삭제하고 신규 검사 `e`(폐기 키 검출) 대상 목록에 등재했다 — 🔴 **폐기 키 문자열은 코드가 보유해야 검사가 성립하므로, `scripts/` 에서 폐기 용어 grep 0건을 목표로 삼으면 검출기가 함께 죽는다.** 세션 필수키 6 → **5**(`status`·`project`·`updated`·`related_ticket`·`cc_session_ids`).
  - 🔴 **은닉 12줄이 슬래시 앵커 치환을 전부 빠져나간다.** `s|sessions/|hippocampus/|g` 형태는 `find "$VAULT/sessions"` 를 못 잡는다. 결과는 **무음 0건 스캔** — 문서는 새 이름, 스캔 루트는 옛 이름. **두 방향을 따로 세라**(`X/` 와 `"$VAR/X`). 용어 grep 총계는 항상 과소다.
  - **`_session-shared/recall.md` 161줄 → 23줄.** 랭커(가중합·정규화)·4소스 스캔(K/C/X/M)·`RECALL_N` top-N 캡·1-hop `related` 확장·graphify 분기 전량 삭제. 0.2.0 은 `_index.md` 전량 주입이라 순위를 매길 대상이 없다.
  - **세션 파일명 = `hippocampus/<PREFIX>_YYYYMMDD_<slug>.md`, 같은 날 같은 슬러그 금지**(생성 시점 중복 검사, 무음 접미사 금지). 🔴 **기존 세션은 개명하지 않는다** — 실측 2026-08-05: 같은 프로젝트·같은 날이 9그룹 23파일(55%)이고 3그룹 8파일은 어떤 필드로도 구분 불가다. 두 형식이 공존하며 모든 소비자는 파일명이 아니라 frontmatter 를 읽는다.
- **`README.md`·`ROADMAP.md` 0.2.0 이행 (KJP-76).** 🔴 죽은 참조 6건 중 **4건은 절이 아니라 사실이 사라진 쪽**이었다 — 앵커만 갈면 "third-time test 가 있다"는 거짓이 남으므로 행을 삭제했다. **`drift-lint` 는 레포 전체에서 README 에만 존재했다**(구현체 0). 실제 기계 검사는 `scripts/value-axis-drift.sh` 하나뿐이고 나머지는 선언 + PM 중재다.

### Removed
- **`agents/coder.md` 85줄 → 69줄, 결정 이력을 이 문서로 이관 (KJP-64).** 에이전트 정의는 **규칙만** 담는다 — 이력은 spawn 마다 재주입될 이유가 없다. 삭제된 이력의 전문은 아래 §결정 이력에 보존한다.
- **dreaming 기능군 6종 전량 폐기/재연결 (KJP-77).** `stub-scan` · `api-mirror` · `id-audit` · `policy-promotion-scan` · 메트릭 스냅샷 · `_index.md` 사후 재생성. 🔴 **판정을 결정한 것은 「전서에 0건」이 아니라 「전서가 금지한다」였다** — 무인 사이클(`sc`·`dreaming`)의 쓰기는 `hippocampus/` · `<project>/p_memory/` · `neocortex/` 3곳으로 못박혀 있고 `docs/` 는 사용자 지시를 받은 AI 에게만 열린다. 6종 중 4종의 코퍼스가 `docs/` 층이다. `api-mirror` 만 **PM 위임 동기화 워커**로 재연결됐다. 처리는 삭제가 아니라 **「🔴 Never by dreaming — 이유 + 정본 포인터」 가드로 뒤집기** — 그냥 지우면 다음 사람이 같은 서술을 무해하다고 여기고 되살린다.
- **`reject-log`(`0.rejected.md`) 폐기 (KJP-78).** 소비자 0. 정본 책임 목록(`agents/worker.md` 로 나가는 초안)이 이미 뺐고 남은 한 곳은 그 목록의 **썩은 재서술**이었다.

### Fixed
- **설계 전서 자기모순 2건 (KJP-78).** 🔴 **「`recall.md` 상한 25 vs 35」는 모순이 아니라 두 대상이었다** — ① `recall.md` **파일** 크기 `≤25줄 / 2,500 B`(에이전트 정의 상한과 같은 열) ② **주입량**(`_index.md` 묶음) **캡 없음**(실측 20,481 B ~ 34,893 B = 상한의 8~14배). 초안 35줄에서 결정 이력 2절을 빼면 정확히 23줄 = 구현과 일치했다 — **상한은 「2절을 뺀 초안」에 맞춰 산정돼 있었다.** 잘못은 자른 것이 아니라 **옮기지 않고 자른 것**이다.
- **`validate.sh` 의 거짓 recall 미러 주장 (KJP-65).** tools root 를 「recall 이 스캔하므로 이 스캔이 미러」라고 정당화했으나, 실측 recall 0.2.0 의 스캔은 3곳뿐이고 tools root 는 없다. 스캔은 유지하고 **이유를 바로잡았다** — tools root 는 「recall 이 읽어야 할 것」이 아니라 **「wiki 스키마를 지켜야 할 것」**이라 스캔한다. 스캔 이유와 recall 이유는 다른 질문이다.
- **dreaming 유령 소유권 (KJP-77).** `validate.sh`·`doc-catalog.md`·`memory-control-convention.md`·`project-docs-convention.md` 가 dreaming 소유라고 적어둔 것 중 **실재하는 것이 하나도 없었다.** Router 재생성 주체는 `/brain:init`, API_SPEC 동기화는 PM 위임 워커, 의미 중복 리뷰는 §Value Axes 선언 + PM 중재다.

### 결정 이력 — `agents/coder.md` 에서 이관 (KJP-45 · KJP-46, 원문 보존)

**베이스 확인이 First action 인 이유 (KJP-45, 실측 2026-07-26)**
- **워커의 베이스는 로컬 통합 브랜치가 아니라 `origin/<branch>` 다.** 하네스가 워크트리 브랜치를 `branch: Created from origin/main` 으로 자른다 — `git reflog show <your-branch> | tail -1` 로 직접 읽어 확인할 수 있다.
- **그래서 stale base 는 사고가 아니라 기본값이다.** 이 canon 은 사용자가 말할 때만 push 하므로(`docs/git-convention.md`), `origin/` 이 멈춰 있는 동안 로컬 `main` 만 전진한다. 실측: **연속된 코더 5명(KJP-40 · 43 · 48 · 39 · 45)이 전부 `41b11ae` 에서 출발했고 그동안 `main` 은 5커밋 앞서 있었다.**
- **stale base 는 여기서 실패하지 않는다 — 머지에서 깨진 fast-forward 로 실패하고, PM 이 손으로 푼다** (KJP-43, 2026-07-25).
- 처리 규칙: 뒤처짐 + 로컬 커밋 없음 → `git reset --hard main`, 묻지 않는다. 뒤처짐 + 로컬 커밋 있음 → **멈추고 보고**(그 커밋이 산출물이다). 어느 쪽이든 찾은 베이스를 보고한다.

**브랜치 자가 명명이 필요한 이유**
- 하네스는 워크트리와 그 브랜치를 `agent-<hash>` / `worktree-agent-<hash>` 로 명명하고, **`Agent` 도구에는 이름 파라미터가 없어 설정으로 바꿀 수 없다.** 그래서 우리 쪽에서 우회한다 — 워커가 스스로 읽을 수 있는 이름을 만든다.
- 하네스의 `worktree-agent-<hash>` 브랜치는 **베이스를 가리킨 채 빈 상태로 남는다. 감수하는 비용이다** — 건드리지 말고 PM 이 정리에서 쓸어낸다(`docs/git-convention.md` §워크트리 통합 순서).

**브랜치에 타입 접두어를 얹은 결정 — KJP-46 을 뒤집음 (사용자 결정 2026-07-26)**
- **하나의 어휘, 네 개의 표면.** 티켓 · 커밋 · PR 제목 · 브랜치가 같은 언어로 읽히므로 `git branch` 만으로 *이게 무슨 종류의 변경인가* 에 답할 수 있다 — 트래커로 왕복하지 않고.
- **이는 KJP-46 의 반전이다.** KJP-46 은 세 가지 근거로 타입 접두어를 배제했었다. 이름 안에 `<PREFIX>-<number>` 를 유지하는 것이 그중 둘에 직접 답한다 — **정체성이 사라지지 않고**(PM 이 *어느 티켓이 이 브랜치를 냈는가* 를 이름에서 읽는다, 이것이 병렬 워크트리 요구사항이었다), **사본이 드리프트하지 않는다**(어휘를 어디에도 다시 적지 않고 가리키기만 한다).
- 🔴 **살아남은 하나의 근거 — 마찰점이 될 것이므로 기록한다.** 타입은 가변인데 **브랜치는 PR 이 열린 뒤 이름을 바꿀 수 없는 유일한 표면**이다. 티켓은 재분류되고, 커밋은 amend 되고, PR 제목은 편집되지만, 리뷰 중인 push 된 브랜치는 고정이다. **재분류가 잦은 것으로 드러나면 여기가 아플 지점이고, 이 항목이 읽기 시작할 곳이다.** 워커가 독자 판단으로 이탈할 근거는 아니다 — 형식을 따르고 마찰은 PM 에게 보고한다.
- **`<type>` 어휘 정본 = `docs/git-convention.md`** (2026-07-28 하네스로 승격 — 배포되는 플러그인은 사용자 개인 스킬에 의존할 수 없다. `at` 스킬과 볼트 미러가 이 문서를 가리킨다). 어휘를 다른 곳에 다시 적지 않는다 — 가리킨다.
- **레포의 사람 브랜치 접두어는 워커의 소관이 아니다.** 프로젝트 `RUNBOOK §Delivery` 가 그 레포의 사람들이 실제로 무엇을 쓰는지 실측으로 기록한다(`feature/*` 류 포함). 그건 그들 것이고, 에이전트 브랜치는 어느 레포에서든 위 형식을 쓴다. 둘의 충돌은 **해결할 분쟁이 아니라 보고할 실측**이다.

**PR 표기 (사용자 결정 2026-07-28)** — 제목 `<type>(<PREFIX>-<number>): 요약`. 티켓 ID 가 scope 슬롯에 들어간다. 브랜치에 얹은 타입을 쓰되, 작업이 처음 판단과 다른 타입으로 드러났으면 **제목에서 바로잡는다 — 브랜치는 push 된 채로 둔다.**

## 0.1.8 — 2026-08-05

**Release cut for drift, not for features.** The installed copy had fallen 6 commits behind `main`, and `plugin.json` read `0.1.7` on both sides — so the version string could not detect it. Measured against the `0.1.7` cache: **29 differing entries**, including a canon file that no longer exists in the repo and two new files the install had never seen. Since the Router symlinks `~/.claude/brain-docs` at the *installed* docs, every skill run was reading stale canon while the repo held the current one.

### Removed
- **`docs/versioning-convention.md` is gone — `docs/git-convention.md` absorbed it (commit `05cd3c8`).** The PR-title rule had two sources of truth; the vocabulary, surface notation, and branch/worktree naming now live in one document. 🔴 **The installed copy still carried the deleted file**, so pointers into it resolved against a document the repo had already retired — the exact failure this release exists to close.

### Added
- **`scripts/kernel-check.sh`** (commit `327ca4d`) — byte-identity check across the agent definitions' `## KERNEL-BEGIN … ## KERNEL-END` blocks. It cannot live in `validate.sh`: that script's scope is `<vault-root>` alone, and `agents/*.md` is plugin-repo. 🔴 **Known pre-existing defect, measured 2026-08-04:** the marker pair is absent from all three agent files (`grep -rn KERNEL agents/` → 0), so every agent currently reports `missing KERNEL block`. Landing the markers is 0.2.0 work (ticket KJP-64).
- **`hooks/org-guard.sh`** (commit `327ca4d`) — pre-blocks unattended-cycle writes to the common layer. Detection after the fact is impossible in the linter once `writer` is retired: file state alone cannot identify the author, so this has to be a gate, not a check.
- **`.artifact/brain-0.2.0.html`** — the 0.2.0 design canon (1,853 lines), now tracked. It is the source of truth for the terminology migration and is quoted by ticket bodies; leaving it untracked meant the tickets cited a file that existed on one machine.

### Changed
- **Docs `status` vocabulary: `stub` → `created`** (commit `55634a0`). Live vaults migrated ahead of the linter — techtainment 38, beafter 23 — because flipping the linter first fails every existing vault at once.
- **0.2.0 design canon: `type` key retired, agent line/byte ceilings reconciled, release-and-vault-application sections added** (commits `bd304b7`, `59c1898`). A schema key with no definition and no consumer is deleted, not documented — its only readers were its own output and its own linter check, which is a cycle rather than a consumer.

### Fixed
- **The design canon violated its own DoD in six places (commit `42e2156`, ticket KJP-75).** It warned that migration scripts must be `.brain-paths`-driven — and then wrote tree literals itself. Two of the six were **blocking gates**: `org-guard.sh`'s write-block and dreaming's path-refusal both named `org/` literally, so a vault whose `common_root` resolves to `personal` or `000_common` would have been **unguarded**. Now routed through `<common_root>`.
  - 🔴 **The distinction that decides these: is this position producing the value, or consuming it?** `init` writing `common_root: org` into a fresh `.brain-paths` is a *default*, not a hardcode — it is the producer. A gate that compares a path against `org/` is a consumer, and a literal there is the defect. Two of the originally-flagged sites turned out to be producers and were left alone.
  - Two prose literals are **deliberately left in place** (`onboard`'s `org/machines/`, the recall injection table's `org/**/_index.md`), and the warning paragraph now names them. A warning with no live example stops being read — and prose literals getting copied into code is how this defect propagates.
- **`scribe` governance still said `knowledge`** where the canonical schema had already moved to `p_memory · neocortex`. Same fact in two places; one got updated.

## 0.1.7 — 2026-07-29

### Added
- **Docs frontmatter v2 + SSOT value axes — the three-party consensus lands in canon (session KJP-20260725-154107, commit 7f1a366).** `status` becomes the only required key; `updated` is a machine-stamped datetime; multi-instance docs get `id`; mirrors get `source`/`readonly`/`synced`; provenance moves to optional `history {at, change, ticket}`. Ten v1 fields are deleted as derivable (path, H1, catalog, git). The `session:` key is banned outright — session linkage lives in vault boundary commit messages. Value-axis declaration (7 rows, e.g. pricing → BUSINESS §BM), precedence kept at 6 tiers, RUNBOOK §4 requires command+output records, TDC→ADR promotion test. `docs-samples/` (12 files) converted to v2 and tracked for the first time.
- **Type vocabulary, surface notation, and branch/worktree naming move into the harness as `docs/git-convention.md` (user decision 2026-07-28).** The 11-type canon previously lived in the user's personal `~/.claude/skills/at/SKILL.md` with a vault mirror — a distributed plugin cannot depend on either. Flow choice (GitHub Flow vs GitFlow) deliberately stays out: it is per-project (`docs/tech-design/GIT.md`, recommended at onboard), while the vocabulary and naming are project-type-invariant. `agents/coder.md` and the README single-source map now point at the new doc.

### Fixed
- **Full v2-canon alignment pass — every consumer catches up with 7f1a366 (session KJP-20260725-154107, commit 88d0d99).** A whole-repo drift scan found ~93 stale sites; all repo-side ones are fixed in one pass. Highlights: a new `tools_root` manifest key (tools root absent = legal, silently skipped — unlike the loud common root); 31 hardcoded tree literals routed through the resolver (`init` now *writes* `.brain-paths` — the axis declaration finally has a producer); 8 date-vs-datetime `updated:` instructions corrected; `sh`/`sc` scribe briefs now produce `history:` entries (the v2 provenance channel previously had no writer); `_index.md` accepted as a folder TOC everywhere (9 asymmetric exclusion sites unified); canon tree prose abstracted to "default layout example" with the manifest as the source of truth (org-tree canonicalization deliberately deferred to the roadmap's candidates redesign). `validate.sh` docs checks grow from 2 to 10: required `status` + vocabulary, policy/adr `id`, index `next_id`, API_SPEC mirror keys, datetime warn, and v1-history detection that survives block, inline-map, one-line-flow, and quoted-key spellings — the last two closed after an adversarial verifier produced working bypasses (red→green fixtures pin them). Live vault migrated in step: 104 files, 189 v1 history entries → v2, findings 302 → 0.
- **Vault tree axes move out of five consumers and into the vault's own `.brain-paths` manifest (KJP-adhoc).** The common layer and the projects root were literals in `scripts/validate.sh` (4 sites), `skills/_session-shared/recall.md` (2), `skills/ss/SKILL.md` (2) and `skills/sr/SKILL.md` (1). One vault restructured — `000_common/` → `_primary/`, `NNN_*` → `projects/NNN_*` — and every one of those scans went to zero **without saying anything**. Measured 2026-07-27 before the fix: `validate.sh <techtainment>` reported `OK — no issues (37 sessions, 0 knowledge, 0 shared)`, and `ss` failed to find `projects/013_kj-plugins` and would have minted a duplicate project folder.
  - 🔴 **The failure mode is the point, not the paths.** A collapsed scan reported `OK`, so the harness looked healthy while its memory layer was switched off; the same session that fixed it had been running with zero recall priming. `scripts/vault-paths.sh` now **warns on stderr** when a declared root does not resolve, and the selftest pins that warning.
  - **Absent manifest = the old layout**, so a vault that never restructured needs no file and no migration. Verified against the real `beafter` vault: `19 sessions, 102 knowledge, 321 shared, 24 issues` before and after, byte-identical.
  - **The common layer is now scanned recursively.** Its sub-axes are not the same shape in every vault (flat `{facts,patterns,policies}/` vs `patterns/` plus `_company/<folder>/`), so enumerating them is what re-hardcodes the tree. Two consequences, both measured and both deliberate: a note sitting at the common *root* is scanned now (it never was — the old scan named three subfolders), and `recall` finally reads `facts/machines/`, which **`validate.sh:128` had been asserting it read while `recall.md:62` capped that scan at `-maxdepth 1`**. The mirror claim was false; the scans now actually mirror.
  - Exclusions live in code, not the manifest, because they are structural rather than per-vault: `index.md` · `_index.md` · `0.*` · `_templates/` (skeletons with placeholder frontmatter lint as broken notes) · `999_Archive/` · `Archive/` · `_dreaming_logs/` · `dream-log.md` (one vault keeps dreaming output as a folder, the other as a single file).
  - Reserved-band handling is preserved, now in one helper instead of four copies: `brain_projects` drops the `9xx` band and the common root, so a project slugged `tools` cannot resolve onto `999_tools/` and `brain_next_project_num` cannot yield a 4-digit `1000`.
  - ⚠ **Not in scope, and still outstanding:** the structure *prose* in `docs/vault-tree.md`, `README.md`, `docs/doc-catalog.md` and `skills/init/SKILL.md` still describes the pre-restructure tree. Separately, project-internal document paths (`docs/tech-design` in 5 files, `docs/policy` in 6) are restated outside their canon `doc-catalog.md` — a second duplication layer whose fix is deleting the restatements, not adding config.
  - Touches `scripts/vault-paths.sh` (new), `scripts/validate.sh`, `scripts/validate-selftest.sh` (restructured-vault fixture + missing-root warning case), `skills/_session-shared/recall.md`, `skills/ss/SKILL.md`, `skills/sr/SKILL.md`.
- **The recall ranker's policy tier survives a policy-folder rename (KJP-adhoc).** `emit`'s `*/policies/*` glob demanded a directory named exactly `policies`. Measured 2026-07-27, hours after the fix above: the vault split that folder into `org_policies/{compliance,secret_management,service_operation}/`, and every policy note **silently dropped from the policies tier (sec=4) to the facts tier (sec=3)** — a 200-point swing in the weighted sum, enough to push a normative note out of the top-N on any goal that does not happen to share its vocabulary. Now matched as `*/*policies*/*`: any directory segment containing "policies". The trailing `/` keeps it a directory test, so a note merely *named* `policies-notes.md` still ranks as facts (verified on all four cases). Old `policies/` layouts are unaffected.
  - 🔴 **Second silent-demotion of the same shape in one day.** Path literals inside ranking logic fail differently from path literals inside scan roots — a broken scan root returns zero and is at least countable, a broken tier assignment returns *plausible output in the wrong order*. Worth remembering when the remaining prose canon gets updated.

## 0.1.6 — 2026-07-27

### Added
- **The `obsidian` CLI ban is split into a write ban and a read allowance, and the two programs sharing the name are told apart (KJP-adhoc).** Five sites carried the same sentence — `obsidian create` / `obsidian-cli create` "absolutely forbidden — duplicate-file bug" (`skills/sr:68` · `sc:115` · `ss:122` · `_session-shared/knowledge-promotion.md:69` · `dreaming:167`). It was wrong in one direction and over-broad in the other, and it was copied five times.
  - 🔴 **"Duplicate-file bug" named a symptom and missed the condition, which is the part that bites.** `create` is not broken — **without the `overwrite` flag it silently forks a name collision into `X 1.md`**, so the write appears to succeed and lands where nothing reads it. Measured 2026-07-27 (Obsidian 1.12.7): `create name="_zz-cli-test"` twice → `Created: _zz-cli-test.md` then `Created: _zz-cli-test 1.md`; with `overwrite` → `Overwrote:`. **The ban survives on the corrected reasoning**, and `overwrite` is explicitly *not* the escape hatch — it trades a silent fork for a silent clobber, which is what the `Write`/`Edit` rule exists to prevent. The write discipline itself is unchanged, compare-and-swap rationale intact.
  - **The official reference does not carry this.** `obsidian.md/help/cli` documents `overwrite` as "Overwrite if file exists" and stops; the collision behavior is undocumented, so reading the docs would not have surfaced it. That is the case for recording it in canon rather than pointing at upstream.
  - 🔴 **The ban had been suppressing a capability the harness needs.** The `obsidian` CLI's **read** side mutates nothing and its link diagnostics are authoritative — `unresolved` · `orphans` · `deadends` run **Obsidian's own link resolver**, the same one that decides whether a link works in the app. **Regex link-counting is a measured false-positive generator**: reproduced 2026-07-27 on this vault, a naive `\[\[[^]]+\]\]` sweep matches `[[tool.uv.index]]` and `[[tool.mypy.overrides]]` (TOML array-of-tables inside fenced code) and `[[synology-nas/software\|software]]` (`\|`-escaped table cells) — none is a broken link, and a regex cannot resolve a link, so it cannot answer the question at all. This lands on `dreaming` directly, whose §1 orphan·cross-ref check and §4 `index.md` dead-wikilink pass are exactly this judgment; its pointer now says not to hand-roll the scan. `verbose` on `unresolved` gives the source file, so a finding is traceable to the note holding it.
  - **Two different programs were being banned as one.** **Obsidian's built-in CLI** (`obsidian`, shipped in the app, 1.12+, `obsidian.md/help/cli`) is what exists here; the **third-party `obsidian-cli`** is a separate project and **is not installed** (`which obsidian-cli` → not found, 2026-07-27). The old wording banned the absent one by name while the one with the collision behavior went unnamed.
  - **Canon home = `skills/_session-shared/vault-io.md` (new), registered in `README.md` §Single-Source Map; the other four are pointers.** `_session-shared/` is the repo's established home for cross-skill canon — `recall.md`, `active-sessions.md`, and `knowledge-promotion.md` are all registered canonical homes from there. **Folding it into `knowledge-promotion.md` was rejected**: two of the five sites are session-file *create* (`ss`) and frontmatter *edit* (`sr`), neither of which is promotion, and that file is only Read at promotion time (`sh:117`, `sc:51`) — a rule needed at `ss`/`sr` time would never be loaded. The read/diagnostic half has no relation to promotion whatsoever.
  - **Counts are recorded as dated measurements, not canon values** — `unresolved` 206 · `orphans` 234 · `deadends` 393 (2026-07-27, vault `techtainment`). They drifted from the brief's same-day figures (236 / 395) during this ticket, which is the argument for taking the measurement rather than quoting one.
  - **Two environment gotchas recorded because both read as tool failure.** A read subcommand piped into `head` looks like a hang — the CLI ignores `SIGPIPE` and keeps running (`obsidian --help | head -60` hung; the same command redirected to a file exits 0 with 518 lines). And **`Info.plist` reports the installer, not the runtime**: `CFBundleShortVersionString` = `1.11.7` while `obsidian version` = `1.12.7 (installer 1.11.7)`, so the app-bundle version understates the CLI's.
  - Touches `skills/_session-shared/vault-io.md` (new), `skills/ss/SKILL.md`, `skills/sr/SKILL.md`, `skills/sc/SKILL.md`, `skills/_session-shared/knowledge-promotion.md`, `skills/dreaming/SKILL.md`, `README.md` §Single-Source Map. **No script changed** — `scripts/validate.sh` lints the vault, not repo canon; `validate-selftest.sh` and `validate.sh --strict` were run before and after and are unchanged.
- **PR/MR enters the canon — the gate is measured off the remote, the coder opens a draft, the PM releases it (KJP-46).** Before this ticket the harness had **zero** occurrences of the concept: `grep -rniE "pull request|merge request|\bPR\b|reviewer|리뷰어" docs/ agents/ skills/` returned nothing. Delivery was therefore undefined for any repo that refuses a direct push — the harness only knew how to hand a branch to the PM for a local merge.
  - 🔴 **Whether a PR is required is a measurement, not a policy preference — and one endpoint is not enough to measure it.** Classic branch protection and rulesets are separate GitHub systems, and either can gate a branch. Measured 2026-07-26 on `kimnamwook1/rss-proj`: `branches/main/protection` returns **`Branch not protected` (HTTP 404)** while `rulesets` returns an **active `main-protection` ruleset whose `pull_request` rule makes a PR mandatory**. A check that queried only the classic endpoint — the obvious one — would have reported "unprotected" for a repo where **no direct push can land at all** (`bypass_actors: []`, `current_user_can_bypass: "never"`). Both queries are canon; running one is worse than running none, because it answers confidently and wrongly.
  - **The two paths, and why this ticket changes nothing about how kj-plugins works today.** Measured the same day: `kimnamwook1/kj-plugins` has `rulesets` = `[]` and classic protection 404 — **unprotected**. So the existing flow (coder hands over a branch → PM merges locally → `git branch -d`) *is* the no-gate path, unchanged and still the default. The PR path activates only where the remote actually forbids the alternative, which was the standing objection to putting PRs in canon: on a personal repo an always-PR rule is pure ceremony. It is now impossible to trigger one there.
  - **The coder opens a `--draft`; the PM flips it to ready.** The reason it does not collide with "the committer is the PM": **a draft PR cannot be merged**, so creating one is submission, never release. Same axis as the existing `Docs draft` handoff — **content = whoever did the work · release = the PM** — and for the same reason: the coder is the only party that knows what happened, the PM is the only party that has verified it. It also *removes* a bottleneck instead of adding one, since CI fires on the draft's first push and is green or red before the PM ever looks; withholding PR creation until after review would serialize the machine behind the human for nothing.
  - 🔴 **The push carve-out is stated narrowly, because it genuinely collides with `docs/versioning-convention.md:11`** ("Push only when the user explicitly says so"). A PR requires the branch to exist on the remote, so on a gated repo the two rules are jointly unsatisfiable — the gate forbids the direct push, and the no-push rule forbids the PR. Resolved by scope: that rule governs **published history, the integration branch**. The coder may push **its own `<PREFIX>-<number>-<title-slug>` branch, only that branch, only under a measured gate.** `main` is never pushed without the user's word, on any repo. `gh` missing or unauthenticated → **stop and report**, never fall back to a direct push (rejected on a gated repo, an unasked-for publish on an ungated one).
  - **`Outputs` goes from two fixed lines to three** — `pr: <url> | none (unprotected)`, and **never blank**. "none (unprotected)" is a real answer that tells the PM to merge locally; an omitted line is indistinguishable from a check the coder skipped. Same reasoning as KJP-45's decision to widen `Outputs` rather than add a ninth top-level Handoff field.
- **Branch naming: one format, and the type prefix is deliberately excluded (KJP-46).** Two conventions were live at once — KJP-45's `<PREFIX>-<number>-<title-slug>` in `agents/coder.md:35` (`f41629d`, merged 2026-07-26) and the `feat/*`·`fix/*`·`perf/*`·`feature/*` prefixes recorded as observed in the reference vault's `001_rss-proj` delivery section. The open question was whether the type vocabulary that binds tickets, commits, and PRs should bind branches too. **It should not**, on three grounds now recorded in `agents/coder.md`:
  - **Type is mutable; the ticket ID is not.** A `feat` that turns out to be a `fix` is re-typed on the ticket, amended on the commit, and edited on the PR title — but a branch cannot be renamed once a PR is open on it. Pinning the one mutable classification to the one surface that cannot be corrected is backwards.
  - **The branch's job here is identity, not classification.** Under parallel worktree coders the PM's question is *which ticket produced this branch* — `KJP-46-…` answers it, `feat/…` does not — and the type is already recoverable from the ticket the ID names. A fourth copy of the type is a fourth place for it to drift, bought with characters the 40-char cap cannot spare.
  - **The vault's own boundary already points this way.** Its type-vocabulary policy enumerates exactly three surfaces (ticket · commit · PR) and explicitly hands the branch model to the delivery policy — an excluded surface, not a forgotten one. ⚠ **The reconciliation on the vault side is not done here** and is flagged to the PM: the org delivery policy names `feature/*` as its GitHub Flow instance, and whether that binds *naming* or only the *model* (short-lived branch off a protected trunk + PR + squash — which `KJP-46-pr-mr-canon` satisfies either way) is a vault edit, out of a coder's write scope. The four different prefixes observed in one repo are themselves evidence that no naming rule was ever enforced there.
- **`999_tools/` — machine-global tool inventory split off the vault-scoped axis, and the `9xx` band reserved (KJP-44).** The tool notes' *content* is machine scope (`~/.claude/**`) but their *home* was vault scope (`000_common/facts/` = "common to every project in this vault"). Measured 2026-07-25: two vaults on one machine each kept their own copy of one truth and the copies diverged an order of magnitude — the same `tool-mcp.md` at **31KB vs 3KB**. Nothing could arbitrate them, because each was correct for its own scope. The axis is now split by name: `000_common/` = this vault, `999_tools/` = this machine. Moves `tool-cli` · `tool-mcp` · `tool-plugin` · `tool-skill` (4 notes); **`machines/` deliberately stays in `000_common/facts/`** — machine *configuration* (which boxes this vault's work runs on) is a vault fact, machine *tool surface* is not. **Git-untracked**, registered in the vault `.gitignore` by `/brain:init` — the entry has to precede the first `/brain:onboard` step 6 write, since gitignoring a path after it is committed un-commits nothing.
  - 🔴 **The `[0-9]*_*` glob was swept for false positives, and one of five sites was actually broken.** `999_tools` matches every "numeric-prefixed folder" pattern in the repo, and `000_common` had been safe only *by accident* — max-based numbering ignores the minimum. A `9xx` folder breaks that accident from the other end. Measured on fixtures: **`skills/ss/SKILL.md:43` computed the next project number as `1000`** — a 4-digit prefix the `NNN_` convention does not allow. A second, quieter one: `ss:39` / `sr:22` resolve `-name "*_<project>"`, so a project slugged `tools` resolves onto `999_tools/` and scribe writes its knowledge into the gitignored inventory — silent, and lost from the shared surface. The other three are genuine no-ops, each for a checkable reason: `validate.sh:124`·`:178` require a `knowledge/`/`docs/` subfolder that no `9xx` folder has, and `recall.md:61`'s `$VAULT/[0-9]*_*/knowledge` is a **shell** glob, so a non-existent path is never in the expansion.
  - **Fixed by reserving bands rather than blacklisting a name** (`docs/vault-tree.md` §Reserved number bands): `000` common · **`001`–`899` projects, the only band `/brain:ss` allocates from** · `900`–`999` vault infrastructure. Numbering filters `grep -E '^[0-8][0-9][0-9]$'`, lookups filter `grep -Ev '/9[0-9][0-9]_[^/]*$'`. Band-based, so it bans a *number range*, not the word "tools" — verified on four fixtures: with `999_tools` next=`014` (was `1000`), without it `014` (guard is a no-op on existing vaults), empty vault `001` (documented invariant holds), and a genuine `007_tools` project still resolves.
  - **recall scope: `999_tools/` is IN, at the facts tier** (`skills/_session-shared/recall.md` source 2, `sec=3`). Three grounds, recorded in the file: (a) it **preserves measured behavior** — those notes were already scanned via `000_common/facts`, and KJP-44 moved a folder on the scope-of-truth axis without deciding to stop priming tool facts; dropping them would be an unvoted capability regression; (b) the cost is bounded — +4 candidates, and only `title:` + the first `## Trigger` line ever enter the pipeline, so the 31KB body never does, with `W_OVERLAP` gating injection; (c) **Router lookup and recall are not exclusive** — Router answers "where do I look this up", recall answers "what should I already know", and being well-suited to one is not an argument for blinding the other.
  - **The "validate scope mirrors recall scope" principle is upheld, and the one asymmetry is not a divergence.** `validate.sh`'s knowledge-title scan **is** the recall mirror → it gains the `999_tools` root. Its session-wikilink scan **is not** — that scan's axis is *share scope* (`validate.sh:161`), and `999_tools/` is gitignored, hence outside the shared surface by definition: no teammate pulls it, so no link in it can dangle for one. Different scan, different axis, no exception needed — and neither needs a guard against the `[0-9][0-9][0-9]_*` sweep, which cannot reach a folder with no `knowledge/` or `docs/`.
  - Self-test **58 → 62 asserts**, pinning both halves of the split: a no-title note under `999_tools/` must fire (recall mirror), a titled one must not, a session wikilink there must stay quiet (not shared surface), and `index.md` there must still be excluded by the meta rule. The scan-count assert carries both directions at once — `12 knowledge, 17 shared`, where the folder raises the first number and leaves the second untouched. Mutation-verified: dropping the KDIRS root kills "999_tools is scanned (recall mirror)" + the count; adding it to SDIRS kills "999_tools is outside the shared-surface scan" + the count. The `000_common/facts/tool-x.md` fixture is renamed `misc-x.md` — after this ticket a `tool-*.md` under `facts/` would contradict the canon it is testing.
  - Touches `docs/vault-tree.md` (tree · §`999_tools/` · §Reserved number bands), `skills/onboard/SKILL.md` (step 6 splits into two destinations by scope + step 9 report), `skills/_session-shared/recall.md` (source 2 root + rationale · source 3 comment · ranker table · marker), `skills/init/SKILL.md` (structure diff · scaffold + vault gitignore · Router pointer · numbering note), `skills/ss/SKILL.md` + `skills/sr/SKILL.md` (band guards), `scripts/validate.sh` (KDIRS root + both scope comments), `scripts/validate-selftest.sh`, `agents/worker.md` (inventory pointer), `README.md` (diagram · tree · validate scope · scope-axis note).
- **Worktree coder discipline: base check, self-named branch, and a merge-before-delete cleanup order (KJP-45).** Three rules the PM had been hand-carrying in every brief, or not at all.
  - 🔴 **The base rule was found, not assumed — and the standing hypothesis was wrong.** The guess was "the worktree is cut from HEAD at session start". Measured on this ticket's own worktree: it was created `2026-07-26 12:08`, by which time local `main` had been at `37ed3d9` since `00:27` — yet the base was `41b11ae`. The branch's own reflog gives the answer verbatim: `branch: Created from origin/main` (`git reflog show <branch> | tail -1`). **The base is the remote-tracking ref**, and `origin/main` had been parked at `41b11ae` since its last push (`2026-07-25 14:50:40`, `git reflog show origin/main`).
  - **Which makes a stale base structural, not sloppy.** `docs/versioning-convention.md` pushes only on the user's explicit word, so `origin/` lags every local commit **by design** — the two rules interlock. Reproduced across five consecutive coders (KJP-40 · 43 · 48 · 39 · 45): every one started at `41b11ae` while `main` advanced five commits. KJP-40 passed only because it launched minutes after a push; KJP-43 hit the real failure (broken fast-forward → `--no-ff` + a hand-resolved CHANGELOG conflict); KJP-48 and KJP-39 were rescued by a brief that said so out loud. **The fix is deliberately the coder-side check, not an auto-push** — auto-pushing would trade a checkable staleness for an unasked-for publish.
  - **`agents/coder.md` §First action** — three plain commands (own base · integration branch · `main..HEAD`), then: behind with **no** local commits → `git reset --hard main` and proceed without asking; behind **with** local commits → stop and report, never self-rebase. The base is reported either way, as a fact about the run. Works with no brief support at all, which was the point.
  - **The coder names its own branch, because the harness cannot.** The `Agent` tool's schema has `isolation: "worktree" | "remote"` and no name parameter — `worktree-agent-<hash>` is not configurable. So the coder cuts `<PREFIX>-<number>-<title-slug>` (kebab · lowercase · 40 chars over the whole name; `<PREFIX>-adhoc-<slug>` with no ticket) right after the base check, and the harness branch is **left behind empty at the base** — an accepted cost, swept by the PM at cleanup. 🔴 **There was no `<title-slug>` rule left to unify with**: the brief cited `skills/ss/SKILL.md`'s `EnterWorktree name: <title-slug>`, but KJP-42 deleted that call in `1e394c3`, and `title-slug` now greps to zero repo-wide. The slug rule is therefore defined in `coder.md` standalone, keeping only kebab+lowercase from the deleted line.
  - **Handoff keeps its eight fixed fields.** A ninth top-level branch field would have rippled through `docs/memory-control-convention.md:7`, `README.md`, and all three agent files; instead `Outputs` now **opens with `branch:` and `base:`**, which is what the PM actually needs to pick a merge target by name instead of by hash.
  - **`docs/versioning-convention.md` §Worktree integration order** — `worktree remove` → merge → **`git branch -d`, never `-D`**. `-d` refuses an unmerged branch, so *the tool is the confirmation step*; `-D` deletes that guard along with the branch. All four behaviors measured rather than recalled: `-d` on a worktree-checked-out branch (`error: cannot delete branch … used by worktree at …`), `-d` on an unmerged branch (`error: … is not fully merged`), `-D` on the same (deleted, commit orphaned), `-d` on an empty harness-style branch (deleted, no force). Grounded in the 2026-07-25 incident: base diverged → ff failed → cleanup deleted the branch **first** → recovery was naming the commit object by hash.
  - Touches `agents/coder.md`, `docs/versioning-convention.md`, `README.md` §Worker profiles. **No script changed** — `scripts/validate.sh` lints the vault, not repo canon, so there is nothing here for it to check; `validate-selftest.sh` (58 asserts) and `validate.sh --strict` were run before and after and are unchanged.
- **Session references on the shared surface: the emitting template is corrected, and `validate.sh` gains the backstop check (KJP-39).** Canon in two places already said a shared note cites a session as **plain uid text** — `docs/versioning-convention.md:6` (§Share scope: "plain uid text, never `[[wikilink]]`") and `docs/knowledge-convention.md:14` (`source_sessions`: "never `[[wikilink]]` a session from a knowledge note (**body included**)") — because a team vault gitignores `sessions/`, so a session wikilink written on the shared surface dangles in every teammate's vault. The vault violated it anyway.
  - 🔴 **The ticket's premise was wrong, and the linter is not the fix.** Measured on the reference vault 2026-07-26: **every** `session:` field in the vault was a wikilink — **zero** plain-uid instances. A 100% rate is not a habit, it is a single emitter, and the emitter was canon: **`docs/project-docs-convention.md:26`** prescribed the document `history:` line as `session: "[[PROJECT_PREFIX-YYYYMMDD-HHMMSS]]"`, **verbatim**. Workers were not breaking the rule, they were obeying a more specific document. **Shipping only the linter would have bought a permanent findings treadmill** — the next `scribe` follows the template, re-introduces the violation, the linter re-reports it, forever. So the template is corrected first (`session: "PROJECT_PREFIX-YYYYMMDD-HHMMSS"`) and the check is the backstop.
  - **`:29`'s rationale replaced, not just its wording.** It read "Never abbreviate (if wikilinks don't match actual filenames, they all break)" — an argument that *presumes* wikilinks and evaporates the moment they are gone, which is exactly how a corrected template drifts back. Re-grounded on `versioning-convention.md:6`: the session may be absent from a teammate's vault entirely. "Never abbreviate" survives on its own footing — the uid is the lookup key for finding the session.
  - **Repo-wide sweep: 1 emitter, 0 duplicates.** `grep -rnE '\[\[[^]|#]*((PREFIX|PROJECT_PREFIX|[A-Z][A-Z0-9]*)-)?(YYYYMMDD|[0-9]{8})-(HHMMSS|[0-9]{6})'` across `*.md` · `*.sh` · `*.json` returns `project-docs-convention.md:26` and nothing else outside the new self-test fixtures; a second sweep for `session: ` templates returns the same single line. 🔴 **Deliberately untouched** — the same document's `:61` (`[[<ID>]]` policy value), `:73` (`[[<PREFIX>-ADR-0000N]]`), and `:98` (API-mirror wikilink target) are **vault-internal doc-to-doc links and correct as they stand**. Only a link whose target is a *session uid* is banned. The self-test now pins that boundary with a quiet `[[KJP-ADR-00001]]` / `[[KJP-POL-00002]]` fixture, and reproduces the corrected `history:` line as fixture content so a regression to `[[…]]` fails a named assert.
  - **The check** — a `[[…]]` whose target is a session uid (`<PREFIX>-YYYYMMDD-HHMMSS`, or the PREFIX-less `YYYYMMDD-HHMMSS` a dreaming report carries by canon) is a finding, and **a wikilink finding alone fails `--strict`** (pinned by an isolated single-finding vault, not inferred from the multi-finding one). Forms covered: bare `[[uid]]` · path-prefixed `[[sessions/uid]]` · alias `[[uid|label]]` · heading `[[uid#Progress]]` · embed `![[uid]]`. No new option, no change to any existing check's logic.
  - **Scope = `NNN_*/docs/**` · `NNN_*/knowledge/**` · `000_common/**`.** 🔴 **`sessions/` is deliberately not scanned** — a session's own wikilinks are its record, and the file is never pulled by anyone else. Measured on the reference vault: **16** session-internal wikilinks exist that would every one become a false positive if that exclusion were dropped, against **0** on the 622-file shared surface.
  - **Two deliberate divergences from the knowledge-title scan above it**, both because the unit differs — that scan checks per-note schema and mirrors what *recall* reads, this one covers a tree a teammate *pulls*: it **recurses** (no `-maxdepth`), and it applies **no meta-file exclusion** (`index.md` and `0.*` are scanned — a dangling link in a TOC breaks for a teammate exactly like one in a note). Recorded as a decision, not drift, per the knowledge note "validate 스코프는 recall 스코프의 미러가 원칙 — 한쪽만 넓히면 갈린다".
  - Summary line gains a third count — `N sessions, N knowledge, N shared` — for the reason the first two exist: a collapsed scan must look different from a clean vault.
  - Self-test **44 → 58 asserts**. One positive fixture per scan root (`013_*/docs`, `013_*/knowledge`, `013_*/knowledge/nested`, `000_common/facts`, `000_common/` root) so narrowing the scope kills a named assert, one per link form, plus quiet fixtures for plain-uid text, non-uid wikilinks, ADR/policy doc links, and a session note whose wikilinks must **pass**. Mutation-verified against four broken variants: removing the check kills 10 asserts · adding `sessions/` to the scan kills "session wikilinks inside sessions/ are legal" · flattening the scan kills the recursion assert · dropping the path-prefix branch kills the `[[sessions/uid]]` assert.
  - Portability unchanged (macOS stock bash 3.2.57 + one-true-awk 20200816). Digit runs are spelled out rather than written `{8}`/`{6}`: one-true-awk has no ERE interval expressions, so a repetition count would not error — it would silently never match. Detection was cross-checked against an independent GNU-ERE `grep` over the same 622 files (both 0), so the clean-vault result is cleanliness and not blindness; replaying the vault's 16 real session-wikilink lines onto a scratch shared surface produced 16/16 findings. **Vault migration is not included** — no vault file was touched (a parallel worker was correcting the vault concurrently, so the live counts here are observational, not a DoD claim).
  - Touches `scripts/validate.sh` (new section only — no existing check's logic changed; the summary string gains one count), `scripts/validate-selftest.sh`, `docs/project-docs-convention.md` (`:26` template + `:29` rationale, 1-for-1 lines, no line shift), `brain/README.md` §Schema check. Fixed in passing while documenting there: the README claimed "`facts/machines/` is out of scope", which has been **false since KJP-16** — `validate.sh:131` adds it as an explicit scan root and a self-test assert has pinned it ever since.
- **`sr` (resume) and `sl` (list) split out of `ss` — one verb, one skill (KJP-43).** `ss` is now **creation only**. Measured 2026-07-25 on the reference vault (`sessions/` = 38 files / 600 KB, `status: active` = 11, spread one per project across 11 projects): the project filter that was supposed to keep the resume fork rare matched on **essentially every invocation**, so a user asking to *start* a session paid a fork question + a ~6 KB `awk` extract per candidate + a round-trip, every time. User decision: **remove the resume path from `ss` entirely — not even a "N parked sessions exist" notice.** Resuming is now an explicit verb.
  - **`skills/sr/SKILL.md`** — resume: shared scan → candidate list → user pick → adopt. Frontmatter touch-up delegated to `scribe` (`cc_session_ids` prepended, `updated` bumped, `status` **stays** `active`, body untouched — `sr` writes no Progress entry). **Recall re-presentation is required and screen-only** (no `## Context` rewrite, no counter bump) — skipping it would leave a resumed session with zero priming. 0 matches ⇒ stop and name `ss`; never falls through into creation.
  - **`skills/sl/SKILL.md`** — list: **read-only, hard**. No `Write`, no `Edit`, no `scribe` brief, no `updated:` touch, no adoption, no creation. Takes `[project|all]`; `all` sweeps every project (`PROJ_RE='.*'`), which is why the shared scan carries an explicit `session_type: dreaming` skip — under `.*` a dreaming report's empty `project:` would otherwise match.
  - **`skills/_session-shared/active-sessions.md`** — the scan and the summary `awk` now live in **exactly one place**, next to `recall.md`. `sl` and `sr` read it; `ss` does not use it at all.

### Fixed
- **Active-session scan no longer exits 1 on a normal run (KJP-41).** Cause: a `while read` loop's exit status is the exit status of the **last command of its last iteration**, so a final non-matching `grep -q` (1) propagated through the pipeline and out of the snippet. Fixed at the new home (`_session-shared/active-sessions.md` §1) with a `|| :` loop-body terminator. Measured on fixture vaults, before → after: **0 matches `exit=1` → `exit=0`**, and — beyond the ticket's original claim — **2 matches also `exit=1` → `exit=0`** (the last file scanned happened to be a non-match, so the false positive was never limited to the empty case). ⚠ The visually identical `while read` loops in `_session-shared/recall.md` (lines 47·55·61·67) are **deliberately left alone**: they sit inside a `{ … } | awk | sort | head` pipeline where the tail decides the exit status, so the bug cannot occur and a terminator there would be noise.

### Changed
- **🔴 Branch naming reversed — the type prefix is now required: `<type>/<PREFIX>-<number>-<title-slug>` (KJP-46, user decision 2026-07-26).** This **overturns the entry above** ("the type prefix is deliberately excluded"), decided earlier the same day. The user's ground: **one vocabulary across four surfaces.** Ticket · commit · PR title · branch now read in the same language, so `git branch` alone answers *what kind of change is this* without a round-trip to the tracker. Example: `feat/KJP-46-pr-mr-canon`.
  - **Two of the three original grounds are answered by the format, not waived.** Keeping `<PREFIX>-<number>` *inside* the name preserves **identity** — the PM still reads which ticket produced a branch, which was the entire parallel-worktree requirement — so the "identity, not classification" objection does not apply to a name that carries both. And the **"fourth copy drifts"** objection is answered by prohibition rather than by omission: **no file in this repo restates the vocabulary**, verified by grep at time of change (0 enumerations; every mention was already a pointer to `~/.claude/skills/at/SKILL.md` §타입 접두어 규약). A pointer is not a copy.
  - 🔴 **The third ground survives, unresolved, and is recorded in canon on purpose** (`agents/coder.md` §First action · `docs/versioning-convention.md` §Pull / merge requests). **Type is mutable; a branch is the only one of the four surfaces that cannot be renamed once a PR is open on it.** A re-classification is cheap on the ticket, the commit, and the PR title, and impossible on a pushed branch under review. Resolution adopted: **the PR title carries the correction, the branch keeps its original name** — a stale type in a branch name is accepted noise, never a re-push. **If re-classification proves frequent, that is where the friction will show, and those two bullets are the entry point for revisiting this.**
  - **Cap mechanics**: 40 characters still covers the **whole** name, prefix included, so the slug is what gives way — shorten the slug, never the type or the ID. No ticket → `<type>/<PREFIX>-adhoc-<slug>`.
  - **PR/MR titles stated explicitly for the first time** — `<type>(scope): 요약 (<PREFIX>-<number>)`, the same message canon as the commit, with the ticket key in the title rather than only the body.
  - **`DELIVERY_STRATEGY` is untouched** — that document's fate is under separate review, and the `feature/*` reconciliation flagged by the earlier entry stays open. Human branch prefixes recorded in a repo's `RUNBOOK §Delivery` remain measurements that coexist with the agent format unreconciled.
  - Touches `agents/coder.md` (§First action branch block · §Last action PR title + `gh` example · `Outputs` template), `docs/versioning-convention.md` (§Worktree integration order step 2 · §Pull / merge requests naming block · push carve-out), `docs/doc-catalog.md` (§Delivery agent-branch note), `README.md` (coder narrative · canon-location table + a new row pinning the vocabulary's single home).
- **`doc-catalog` §Delivery widened to what the section already holds in practice, on a policy-vs-measurement split (KJP-46).** The definition read "delivery bucket + git-flow pointer + exceptions line" (`docs/doc-catalog.md:35`), but the reference vault's `001_rss-proj` delivery section had long outgrown it — branch prefixes, squash-merge policy, the squash-safe branch-cleanup judgment, and CI enforcement. A definition narrower than every real instance of it stops being a definition. Widened in place per the KJP-36 19→6 consolidation — **no new document, no new section, only the existing row and note.**
  - **The widening needed a rule that stops §Delivery from turning into a copy of the org policy**, since the old note's one hard prohibition ("never hardcode the path or restate a flow value — restate → drift") is what kept it thin. The rule now stated: **the vault note holds the *policy*, §Delivery holds *what this repo's remote actually enforces*.** Restating a policy value is drift; recording a measurement is the whole job — **a measurement has no second copy to disagree with, because the repo is its own source.** That is why PR/MR gate values belong here and a flow value still does not.
  - Enumerated as this section's own: **the PR/MR gate as measured** (required or not · approvals · surviving merge methods · whether any status check actually blocks · who may bypass), with the query recorded so it can be re-run, and 🔴 **an absent gate written as a finding, not omitted** — `protection: none (measured <command>, <date>)`, because a blank line and an unmeasured line look identical. Also **branch naming** (the prefixes this repo's branches actually carry, and whether they are a rule or an observation) and **branch cleanup judgment** (squash-dependent, hence per-repo). Explicitly *not* this section's call: **agent branch names**, which are harness canon in `agents/coder.md` and identical in every repo.
  - 🔴 **"Read it off the host, never off the workflow file"** — a CI workflow proves what *runs*, not what *blocks*. Measured 2026-07-26 on `kimnamwook1/rss-proj`, which is exactly this trap: its workflow runs `test` on every push and PR and gates `deploy` behind `needs: test`, yet the active ruleset carries **no `required_status_checks` rule at all** (`rules[].type` = `deletion`, `non_fast_forward`, `required_linear_history`, `pull_request`) — so a red build does not block a merge, only a deploy. The delivery section's standing "TBD — protection ruleset values unverified" line is answerable now, and the answer is in this ticket's handoff for the PM to route.
- **Session `status` redefined to `active` / `parked` / `done` — `parked` promoted to a first-class status, `cancel` retired (KJP-48).** The 3-value cap is unchanged; the *members* changed. Ground: **parking was not a status at all.** `sh` left `status: active` (`skills/sh/SKILL.md:22`, pre-change), so the only way to tell a suspended session from a running one was to parse the session body — the newest `## Progress` `### ` heading for the literal string `(parked)`. That parse became a contract via **KJP-20**, and the cost landed on the newly-split `sl`/`sr` (KJP-43): every open session reads `active`, so the list could not distinguish "suspended on purpose" from "left running". Measured 2026-07-25 on the reference vault (`sessions/`): `status: active` 11 · `status: done` 25 · **`status: cancel` 0** · `(parked` notation present in **6** of the 11 actives. Two consequences — the state was real but unreadable (6 of 11), and **retiring `cancel` costs nothing** (0 occurrences).
  - **Transitions are now explicit and each belongs to exactly one skill**: `ss` writes `active` (unchanged) · **`sh` flips `active` → `parked`** · **`sr` restores `parked` → `active`** · **`sc` writes `done`**, its only value. `sr` gaining a status write reverses its KJP-43 "never change `status`" hard rule — that rule existed because there was nothing to change it to.
  - **An abandoned session is `done` + an `abandoned` tag** (`sc` ③), not a fourth value. Rationale recorded in canon: "did we finish it" and "was it worth finishing" are different questions, and only the first belongs in a lifecycle field — the same axis-splitting argument dreaming already applies to `status` vs `result`. `sc` ③ is no longer a status fork at all, so an ambiguous outcome call can never stall a closure.
  - **The shared scan now matches `active` **and** `parked`** (`_session-shared/active-sessions.md` §1) and emits `<status>\t<path>`, so §3's `[parked]`/`[active]` marker — now **required** in both `sl` and `sr` output — costs no second read. 🔴 **Scanning only `parked` would be a bug**: a session interrupted without `sh` (crash, closed terminal) never got its `parked` write, and `sr` is the one path back into it. Fixture-verified 2026-07-25: `done` excluded · quoted `status: "parked"` matched · a body line `status: …` ignored (frontmatter is the first `^status:`) · dreaming report excluded under both `PROJ_RE=<project>` and `PROJ_RE='.*'`. The KJP-41 `|| :` terminator was **re-measured against the rewritten loop** and is still load-bearing — last file = another project's open session gives `with=0 / without=1`; the added `case … continue` does not absorb it.
  - **The KJP-20 statusline contract is dissolved.** The `(parked)` / `(completed)` Progress suffix **stays** as the per-entry historical record — a session's history is not recoverable from one frontmatter field — but it is **no longer a machine-judgment source**: state is read from `^status:`, one grep, no body parsing. sessions-note-convention §Progress entry status suffix is retitled "(record notation)", gains a `status:` column per row, and drops the "unknown suffix falls back to `active`" parser-defence clause (nothing parses it, so there is no fallback to specify). Where a legacy file's suffix and `status:` disagree, **`status:` wins**.
  - **`validate.sh`** accepts `active|parked|done` and gives `cancel` its own message rather than a generic "invalid" — the message *is* the migration instruction (`retired status "cancel" … an abandoned session is done + an abandoned tag`), which is what a legacy vault's operator needs at the point of the finding. Self-test grows a legal-`parked` fixture and a `cancel` positive fixture (44 asserts, sessions count 12 → 14); the placeholder literal becomes `<active|parked|done>`.
  - **Follow-on `sh`/`sc` corrections (KJP-43 leftovers).** KJP-43 removed the resume path from `ss` but left `sh` advertising it — 6 statements telling the user "the next `ss` will ask whether to resume", including the description and the closing notice. All rewritten to name `sr`. The `sh`/`sc` "when in doubt" forks listed 3 skills (`ss`/`sh`/`sc`); both now list all 5.
  - Touches `docs/sessions-note-convention.md` (vocabulary + per-value semantics + suffix section), `skills/sh` · `sr` · `sc` · `sl` · `ss`, `skills/_session-shared/active-sessions.md` (§1 scan · §3 presentation · title), `skills/dreaming` (the `status: done` guard now states *why* `done` is the only value that keeps a report out of the scan), `scripts/validate.sh` + `scripts/validate-selftest.sh`, `brain/README.md`, root `README.md`. **Vault migration is not included** — no vault file was touched; the judgment script and transition rules ship with the handoff for the PM to run.
- **`ss` no longer asks about a worktree fork (KJP-42).** A PM session always runs in the current working tree, and isolation is already automatic for the `coder` agent via its `isolation: worktree` frontmatter (`agents/coder.md:4`) — the question duplicated a decision nobody makes at session start. `git_branch` is still recorded (`git rev-parse --abbrev-ref HEAD`); **`git_worktree` stays in the schema but `ss` always writes it empty**, leaving room for a coder worktree path to be recorded later. Removes the only `EnterWorktree` call in the session skills.
- **Skill trigger vocabulary de-conflicted** — `ss` keeps new-session wording only (all resume vocabulary removed); `sr` owns "sr"/"재개"/"resume"/"이어서"/"세션 이어"; `sl` owns "sl"/"세션 목록"/"열린 세션"/"뭐 하다 말았지". Zero token overlap between the three trigger sets.
- **Canon synced** — `docs/sessions-note-convention.md` (`project:` scan owner · `git_worktree` always-empty rule · `## Context` written by `ss` / re-presented by `sr` · skill-step tag example `ss§6a → ss§3a`), `docs/memory-control-convention.md` (§Recall split into the two callers and their two dispositions), `_session-shared/recall.md` (caller list + "Continue" → "Resume (`sr`)"), `skills/dreaming/SKILL.md` (the `status: active` / empty-`project:` guards now name the shared scan, plus why `sl all` makes the dreaming skip load-bearing), `skills/init/SKILL.md` (generated PM-role line), `CLAUDE.md`, `brain/README.md`, root `README.md`. **`sh` and `sc` are untouched by design** — see the note in the Risks of the KJP-43 handoff about two now-stale sentences in `sh`.
- **`Docs draft` gains the creation path — a document that does not exist yet is drafted too (KJP-40).** The KJP-37 clause was written entirely in update terms ("invalidated or extended a document"), so **a feature kickoff needing a new FRD·TDC fell outside it**: canon left new-document creation to the PM's brief, and the PM — the party being reported to, not the one holding the feature detail — misses it. Measured recurrence 2026-07-25: a session's five coder briefs all carried CHANGELOG in their DoD and none carried a docs update, the same gap KJP-18 named. The fix keeps every existing boundary and adds one sentence to each of the three canon sites: `worker`·`coder` profiles ("if that document does not exist yet, draft it as a **new document** all the same — whether it actually gets created, and where, is the PM's call from `doc-catalog`, and `scribe` does the creating"; the closing prohibition widens from "never patch" to "never patch **or create**" — a worker still never touches the vault, serialization unchanged), `memory-control-convention` §Handoff Format (same path stated as canon, gated on a catalog trigger — 🔴 **not** a license to create documents habitually; the KJP-36 19→6 consolidation stands), and `sc` ④b. **④b's blanket "no new files" was the concrete blocker** and is now scoped to its actual intent — the heading reads "a new file only from a Handoff draft", the body has the draft ride the same scribe brief with the file created by `Write`, and the "never fabricate content the session did not produce" guard is what carries the original meaning: a new document comes only from a draft the session produced, never as an empty stub (a pre-emptive stub is exactly what KJP-18 measured rotting). Role split otherwise unchanged: content = the worker · routing = the PM · vault write = `scribe` · commit = the PM. Wording fix along the way — `memory-control-convention`'s "copies the draft verbatim — **creation forbidden**" now reads "**authoring forbidden**"; with scribe creating files the old phrase read as a self-contradiction, and authoring is what it always meant (`sc` ④b already said "scribe copies, never authors"). Precedent for scribe file creation was already canon: ADR creation as a recording brief (`project-docs-convention` §stub Pre-creation) and `Write`-only-for-new (`knowledge-promotion` §Forbidden). Touches `worker`, `coder`, `memory-control-convention` (§Handoff Format), `sc` (④b heading + body).

## 0.1.5 — 2026-07-25

### Changed
- **Doc catalog consolidated: 19 pre-created stubs → 6, feature set 6 → 3; delivery re-framed onto the vault's classification note (KJP-36).** Ground: the KJP-18 fill-rate measurement (787/1029 vault doc content untouched after scaffold) + user confirmation 2026-07-25 — SSOT boundaries stay, but rarely-filled standalone files become **sections of documents that actually get read**. Pre-created stubs are now **PRD · ARCHITECTURE · CODE_CONVENTION · RUNBOOK · THREAT_MODEL · BUSINESS**: NFR·GLOSSARY → PRD §비기능 요구·§용어; ERD·INTEGRATION → ARCHITECTURE §데이터 모델·§외부 연동 (split into a file only when a section actually grows heavy); FULL_TEST_PLAN → CODE_CONVENTION §테스트 규율 (per-feature acceptance criteria stay in each FRD); GIT_STRATEGY·OBSERVABILITY·DR·MIGRATION → RUNBOOK §Delivery·§관측·§재해 복구·§마이그레이션; BM·GTM → one BUSINESS file, two sections (kind `business`); THREAT_MODEL stays independent (security/evidence character). **API_SPEC leaves pre-creation only** — the catalog row stays, and dreaming's api-mirror generates the repo-spec mirror once an API exists (a stub mirror of nothing is noise); COMPLIANCE·DESIGN remain situational files. Feature set: DATA_FLOW·SEQUENCE·STATE_DIAGRAM fold into **TDC §Diagrams** — the catalog's "prose goes in the TDC" rows were already the argument, only diagrams remained in those files (state diagram still 🔴 real-state-machines-only); reference direction tightens to `FRD → TDC → (ADR · policy)`. Naming now pairs abbreviation + full name in the catalog table and every stub H1 (`# PRD (Product Requirements Doc — 제품 요구사항)`). **Delivery frame**: the "org single default flow" premise is dropped — **the vault's delivery classification note decides** (a table mapping project type → flow; org is only where documents live — one org legitimately runs different flows per project type), its **path differs per vault** (binding = `000_common/policies/`, e.g. `DELIVERY_STRATEGY.md`; stabilizing = a non-binding reference in `000_common/facts/`, promoted when stable) and is never hardcoded; onboard Q1/Q2 stay, only the recording home moves (GIT_STRATEGY file → RUNBOOK §Delivery), and the KJP-35 init pointer seed moves with it (RUNBOOK seeded §Delivery: note pointer + bucket TBD + "예외: 없음 — 분류표 준수"). **MILESTONE home = `docs/` root** — `docs/planning/` removed (KJP-17 partially reversed: a folder for one file was over-design; recreate `planning/` only when a second planning document appears). **DESIGN SSOT generalized** — "External SSOT = Figma/Pencil" becomes: the design tool if one is used, else the repo component source (code-first — git history = the change history); § renamed "SSOT (design tool or repo)"; explicitly isomorphic to the API_SPEC repo-mirror pattern; new 3-layer component-confirmation rule (pixels·history = SSOT / confirmed spec = §Components row + §States / confirmed why = row Notes + one `history:` line, ADR when hard to reverse; 🔴 per-component `.md` files forbidden — a second SSOT drifts). onboard gains a tool-neutral **Q3 design-tool sub-question** seeding DESIGN §SSOT. Conflict precedence keeps its semantics with the NFR tier renamed to `PRD §비기능 요구(NFR)` (a section outranking its own document's body is intentional — constraints bind requirements). Touches `doc-catalog` (header · all tables · Delivery/MILESTONE notes), `vault-tree` (docs tree + naming examples), `project-docs-convention` (§stub Pre-creation · §TDC · precedence), `doc-templates` (DESIGN SSOT + 3-layer rule · MILESTONE home), `init` (step 4: 6 stubs + RUNBOOK §Delivery seed), `onboard` (③ Q2/Q3 + mapping ②③), `README` (tree · Handoff lines).
- **Worker Handoff gains an optional `Docs draft` section — document updates enter the work loop, authored by whoever did the work (KJP-37).** Role split (user-confirmed): **DoD designation = the PM** (brief, as far as it can predict) · **content = the worker/coder** (goal · structure · behavior written in the Handoff `Docs draft` section; documents discovered mid-work get drafted too) · **vault write = scribe** (copies verbatim — creation forbidden) · **commit = the PM**. `worker`/`coder` profiles upgrade the KJP-18 Risks one-liner from notification to draft submission (folded into a single paragraph — no duplicate rule; `verifier` unchanged — report-only work cannot invalidate a document). `memory-control-convention` §Handoff Format synced as canon (format line + role-split note; the PM forwarding the draft **unedited** is part of the contract — a PM rewrite reintroduces the guessing the split removes). `sc` ④b wires the loop end: a Handoff-carried Docs draft rides the outgoing scribe brief verbatim. `init` 2a PM role adds one brief-discipline line — a brief touching features·architecture·deployment·schema includes the affected document updates in its DoD — deliberately reversing KJP-18's "init PM role text unchanged" boundary now that the draft channel exists. Direct worker vault writes stay forbidden, with the reason now recorded: **serialization** — concurrent sessions would collide in the same vault files.

## 0.1.4 — 2026-07-25

### Changed
- **`validate.sh` recognizes dreaming-report uids (KJP-32).** Dreaming reports carry `uid: YYYYMMDD-HHMMSS` with no PREFIX by canon (`dreaming/SKILL.md` §Report format — a dream is a cross-project batch), but the session uid check knew only `<PREFIX>-YYYYMMDD-HHMMSS` and flagged every dreaming report. The check now branches on `session_type: dreaming` (same shape-only + filename-match discipline, dreaming-specific pattern). Ticket resolution: the flagged vault file was **legal** — the specific canon (dreaming skill) outranks the general one (sessions-note-convention); the linter, not the file, was wrong. Self-test grows a legal-dreaming fixture + a PREFIX-carrying positive fixture (42 asserts).
- **docs gain a post-onboard write path — detection + PM routing, never auto-writing (KJP-18).** Measured gap: filled documents matched exactly what the onboard interview asks; no skill touched docs afterward, dreaming's stub-scan gated on inbound ≥1 so freshly scaffolded stubs (inbound 0) never surfaced, and the worker Handoff had no channel for document impact. Fixes stay on the dreaming "fabrication is not recall" principle — nothing generates document content: `sc` gains a **④b document check** (if the session's `Outputs` touched architecture · API surface · deployment · schema, pick the document from `doc-catalog` and fold the update into the outgoing ⑤·⑥ scribe brief or leave a To-Do; `sc` only — park is frequent and cheap, closure is the natural document boundary); dreaming §5 stub-scan now also reports **inbound-0 stubs older than N days (default 30)** — age is the only signal for a forgotten scaffold; `worker`·`coder` profiles route invalidated documents into Handoff `Risks` (existing field, no schema change; `verifier` unchanged — report-only work cannot invalidate a document); `onboard` ③ flips `GIT_STRATEGY` `status: stub → draft` at the bucket-recording point (stub rule — the init-seeded pointer body alone does not clear stub). **init PM role text deliberately unchanged** — the `sc` anchor propagates via the plugin skill itself, and a standing role line would read as blanket doc obligation (the ticket's over-design boundary). Touches `sc` (④b), `dreaming` (§5 stub-scan), `worker`/`coder` (Risks line), `onboard` (③).
- **ADR creation wording aligned with scribe governance (KJP-22).** `project-docs-convention` §stub Pre-creation said "the `architecture` worker creates one", contradicting `memory-control-convention` ("Workers never write to the vault directly" — all vault writes are PM-delegated recording briefs). The sentence now reads: the PM delegates ADR creation as a recording brief carrying the `architecture` owner label (a brief label, not a resident agent). Fix (b) chosen over (a) widening `knowledge-promotion`'s scribe write-scope enumeration — (a) would leave the contradicting sentence standing while stretching a procedure-scoped list past what that procedure actually writes; (b) rewrites the one false sentence at its source.
- **brain config split across two project-root files for team sharing (KJP-23).** `/brain:init` used to write all 4 sections (brain config · PM role · Router · Worker profiles) into one gitignored `CLAUDE.local.md`, so shareable discipline never reached a team checkout. init now writes **two** marker-managed files: **`CLAUDE.md`** (committed — shared keys `org` · `project` · `prefix` · `ticket-system` identifier + PM role + Worker profiles; 🔴 never gitignored) and **`CLAUDE.local.md`** (gitignored — `vault-root` + Router, both machine-absolute by nature: vault paths differ per person even on a team, and Router lines are literally-expanded home paths). Credentials wording corrected: the old "CLAUDE.local.md holds ticket-system credentials" line misdescribed reality — `ticket-system` is an identifier only, and real credentials live in **neither** CLAUDE file (separate env, e.g. `~/.config/claude/huly.env`). Pre-split projects migrate by re-running init: each file's brain block is regenerated between its own `brain:begin/end` markers, so the shared sections leave `.local` in the normal idempotent rewrite (documented only — no project was migrated here). Touches `init` (step 2 split 2a/2b + step 3 "never gitignore CLAUDE.md"), `onboard` (prerequisite + ① ticket-system home), `project-inference` (`project:` read from `CLAUDE.md` first), `sessions-note-convention` (PM-role canon home), `README` (quickstart · ownership table · FAQ). vault-root/Router references in `ss`·`sh`·`sc`·`recall`·`knowledge-promotion`·`dreaming` stay pointed at `CLAUDE.local.md` — still its home. Follow-on consistency (verifier findings): `hooks/force-delegate.sh` whitelists the project-root `CLAUDE.md` (init 2a would otherwise be blocked where the hook is wired) and the "CLAUDE.md pointers" recall-layer shorthand in `memory-control-convention`·`dreaming` is renamed to **CLAUDE.local.md Router pointers** — post-split, the old name pointed at the committed file, the exact leak this split prevents.
- **Delivery classification wired into `onboard` + the `GIT_STRATEGY` stub, org-parameterized (KJP-35).** The Stack interview (`onboard` ③) now asks two classification questions — **Q1** who controls deployment (*I control*: server/web/GitOps/self-host · *someone else*: store review / shipped client artifact) and **Q2** real-user SaaS? (Axis 2, "I control" bucket only) — and maps the answers to one of the three `DELIVERY_STRATEGY` buckets (**server-SaaS / server-personal / client**), recording the bucket in the project `GIT_STRATEGY` stub. `init` now seeds `GIT_STRATEGY.md` as a **pointer** to the common `DELIVERY_STRATEGY` policy (3 buckets + a "예외 없음 — DELIVERY_STRATEGY 준수" exceptions line) instead of a bare H1; the scaffold's skip-if-exists idempotency is unchanged. 🔴 **No git-flow value is hardcoded** — the org default is keyed by the brain-config `org` and lives in `DELIVERY_STRATEGY` §0 (`000_common/policies/`); `onboard`/`init`/`doc-catalog` **point** there and never restate it (restate → drift). The old "default is trunk-based" wording is dropped from `onboard` and `doc-catalog` for the same reason.
- **`GIT_STRATEGY` vs `DELIVERY_STRATEGY` naming disambiguated (KJP-29).** The `doc-catalog` GIT_STRATEGY row now pins the two as distinct artifacts: `GIT_STRATEGY` = per-project (`docs/tech-design/`, may carve exceptions) **points at** `DELIVERY_STRATEGY` = org-wide common policy (`000_common/policies/`, org default git flow, no project exceptions) — grounded on DELIVERY_STRATEGY §6.
- **Knowledge promotion is now a two-gate judgment; every reject carries a coded reason (KJP-6).** The single score gate (sum ≥ 3) stays as **Gate 1** — deterministic, unchanged. A **Gate 2 similar-note verdict** now follows for every Gate-1 passer: scribe checks the item against the existing-note title list the brief already carries (KJP-5 glossary anchoring — recall's grep priming, run once and reused; no new scan, no vector search, no LLM gate) plus the update-over-create note Read, and returns exactly one of the closed enum **`promote / already_known / not_durable / unsupported`** — only `promote` writes. The `0.rejected.md` line gains a reason slot: `- <YYYY-MM-DD> · reuse/prevent/conf=a/b/c(=sum) · <reason> · "<verbatim>" · <uid>`; a Gate-1 below-bar line always carries `not_durable` (its real rationale is the score triple — `sum < 3` tells the gates apart, and the fixed code keeps one uniform machine-groupable format instead of two). Dreaming §3 now tallies the codes — **key diagnostic: recurring `already_known` = the note exists but recall failed to surface it → a retrieval defect, not a capture defect** (recall-fix signal: title/Trigger wording, missing `related:` links). Dedup discipline unchanged ("write roughly, Dreaming cleans up") — when torn, `promote` wins; `already_known` is for literally-nothing-to-add only. cognee-distill mapping: Gate 1 = the deterministic no-LLM gate, Gate 2 = the writer/rejecter with its rejection enum unextended (the curator between them = Dreaming, already in place). Touches `knowledge-promotion` (canon header · Why-separate · Steps 1·2 · format · returns), `knowledge-escalate-convention` (Stage-1 line), `dreaming` (§3 aggregation clause shared with KJP-8 + §6 Rejects row), `sh`/`sc` (gate wording), `README` (§Single-Source Map row).
- **`ss` now establishes `GOAL` before every recall (KJP-25).** The recall ranker hard-requires `GOAL` via a `${GOAL:?}` guard (`recall.md:18`, added in KJP-4), but `ss` only established `VAULT` — leaving the guard to fail loudly. `ss` now exports `GOAL` at **both** recall points, symmetric with `VAULT`: step 3 (resume, from the adopted session's `## Goal`) and step 6a (new, from the step-4 goal). Ticket named step 6a only; resume was fixed too since the guard fires there as well.

### Added
- **Knowledge notes gain a helpful/harmful feedback loop — `recalled:`/`useful:` counters wired from injection to ranking (KJP-7).** Promotion was one-way: nothing fed back whether recalled knowledge actually helped, and staleness was an after-the-fact flag. Two optional integer frontmatter fields close the loop (canon: `knowledge-convention` §Feedback counters): `recalled:` = sessions the note was injected into, `useful:` = sessions it actually helped; **absence = 0** — no backfill across pre-existing notes (vault measured 2026-07-25: 382 notes, 0 carrying either field). The session `## Context` recall block is declared the **canonical injected-notes record** (each line already carries its source path — `ss` step 6 / `recall.md` §Injection; no new section, no separate list; resume recall stays screen-only and counts nothing). `sh`/`sc` bump the counters inside their existing promotion delegation: PM extracts the injected list + designates the actually-used subset, scribe Edits `recalled:` +1 / `useful:` +1 (CAS on the literal value; absent → insert at 1), **once per session** via a counter-marker line in the recall block (later parks top-up `useful:` only); `[M]` session lines and `000_common/policies/` (signature tier) are never touched; counter-bumped paths join the commit pathspec. The ranker's **W_CONFIDENCE term now reads `useful:`** — `min(useful,10)*10`, replacing the never-persisted `score:` read; **weight structure unchanged** (S10/C5/O4/P1/rec0.5) — the C term was forward-compatible 0 until now. 🔴 **No automatic deletion from counters, ever** — a low `useful`/`recalled` ratio yields exactly ranking demotion (self-acting via the C term) + a Dreaming §1 rewrite·merge-candidate flag (proposal only). Touches `knowledge-convention` (fields + §Feedback counters), `recall` (conf source · ranker table · §Injection · marker), `ss` (6b canonical record · step-3 boundary), `sh`/`sc` (counter clause · Context marker bullet · pathspec), `dreaming` (§1 net-help flag), `README` (map row).
- **Dreaming §3 gains the session-Mistake recurrence scan — the 0.1.2 deferral is lifted by landing its two preconditions (KJP-9).** The deferral said "do not re-add this scan until that format change lands"; this change **is** that format change. Precondition 1 — the **dream-log gets its first format canon** (`dreaming` §7, grounded on the live file's measured shape): one date heading per run (newest at top = §0's baseline read), 🔴 a `<project>` field on every project-scoped item line (one vault-wide file — without it, keys from different projects cross-suppress), 🔴 cumulative whole-log read for every consumer beyond the baseline date (keys accumulate; the newest entry is not the state). Precondition 2 — knowledge notes gain **`source_items:`** (`knowledge-convention` §Item-level provenance): the verbatim Mistake/Learned line(s) a note restates, copied by scribe at promotion; pre-existing notes have none → **uid-level `source_sessions` fallback, with item-level suppression explicitly impossible** for them. The scan itself (canon: `dreaming` §3): combs `#### Mistake` blocks for the same mistake in **≥2 distinct sessions** (third-time-test precedent), detect·propose only — 🔴 session files are never marked (non-negotiable); close-out lives in dream-log suppression keys. Its six built-in guards: **(1)** §0 gains an explicit full-scan carve-out with the pollution-argument rebuttal (detect·propose only ⇒ no pollution surface; cost handled per-scan); **(2)** hard cap `DREAM_MISTAKE_N` (default 30, newest per project) bounds the O(N²) pairwise test (≤435 pairs) — the `RECALL_N` precedent; **(3)** machine boundary stated, not solved — sessions may be local-only while the log is shared, so counts are per-machine observations (never "correct" another machine's keys); **(4)** both recall [M] guards inherited (`session_type: dreaming` skip + per-`project:` bucketing, empty project skipped); **(5)** fuzzy/exact symmetry — one normalized-token-overlap test (≥0.6 vs the smaller set) for counting **and** suppression-key matching (exact-string close-out would resurrect paraphrases); **(6)** the recall.md [M] awk now terminates on `/^##/` instead of `/^####/` — a trailing Mistake block no longer swallows the next `### ` date entry / `## ` section (fixture-verified; minimal one-line diff to the KJP-4 pipe). Kept distinct from the KJP-8 tag count: machine string vs content similarity. Touches `dreaming` (§0 carve-out · §3 scan · §6 row · §7 format), `knowledge-convention` (`source_items`), `knowledge-promotion` (Step 2 source_items write), `recall` (awk fix), `README` (map rows).
- **Mistakes can name the skill step they happened in; dreaming aggregates them into skill-improvement proposals — and can never apply them (KJP-8).** Dreaming consolidated knowledge but never fed back into the skills themselves, so a procedure step could fail repeatedly with nothing accumulating. Session `#### Mistake` lines gain an **optional** inline tag `[skill:<skill>§<step>]` (at most one, end of line, step label exactly as the skill doc writes it; absent = not skill-tied — never guessed). Chosen over a frontmatter field because Mistakes are per-line items (a session field cannot attach to one mistake), and the bracket token follows the statusline-suffix precedent of machine-parsed inline notation — while staying off the Progress-suffix closed set. Dreaming §3 counts tags by **exact string** (full grep over `sessions/` — a machine-tag count needs no similarity judgment or handled-marker, so it is exempt from the KJP-9 content-recurrence deferral — since lifted by the KJP-9 entry above); **the same tag on ≥2 distinct sessions** (third-time-test precedent) produces a proposal section in `<vault>/000_common/skill-proposals.md` (runtime output written by scribe — not a repo file): current wording / proposed wording / evidence session uids (plain, no `[[ ]]`). Existing sections get a uid refresh, never a duplicate; `~`-marked sections are handled and skipped. 🔴 **No auto-apply path exists, by design** — the pipeline terminates at the proposal file; the user reads and applies by hand (the propose·approve principle applied to the harness's own procedures). The KJP-6 reason-code tally and this tag count land as **one** §3 structured-signal clause — the same move twice (free text → coded field → deterministic batch tally). Touches `sessions-note-convention` (Mistake tag), `dreaming` (§3 clause shared with KJP-6 · §3 deferral carve-out · §6 metrics row).

## 0.1.3 — 2026-07-23

### Changed
- **`DESIGN_SPEC` renamed to `DESIGN`, role expanded from link-holder to design-system spec (KJP-17).** Was: `DESIGN_SPEC` / kind `design-spec`, scoped to "holds Figma/Pencil links". Now `DESIGN` / kind `design` — an **FRD-grade design-system spec** (design tokens · component inventory · states · interaction rules) that **also** links the external SSOT (Figma/Pencil), not just links. Touches `doc-catalog` (row + kind), `vault-tree` (`DESIGN_SPEC.md` → `DESIGN.md` + annotation). Tier stays `situational` (UI products only). ⚠️ Canon only — the vault's existing `DESIGN_SPEC.md` files are renamed under KJP-24, not here.
- **`common/policies/` re-defined by binding force + scope, not origin (KJP-15).** The tier was described as *external* mandates — laws, regulations, certifications. It is now any norm that **binds every project in the vault**, origin-agnostic: an external mandate **or** a self-imposed org-wide invariant, gated on must vs. advice/technique. Touches `knowledge-convention` (3-axis table Content/Origin cells), `knowledge-escalate-convention` (§common/policies diagram + bullet), and `knowledge-promotion` (the obligation test in Step 2 and the sole-exception note in Step 1). The `Binding force = yes (no project exceptions)` cell was already on this axis and is unchanged.
- **ADR counterexample re-premised (KJP-15).** The "don't bundle decisions that may diverge per project" warning stands, but its test is now *whether an exception can arise inside this vault*, not *whether the org differs*. Consequently git branch strategy does **not** diverge in a single-org vault and stays in `common/policies/`; it would split per project only across distinct orgs sharing one vault.
- **Session commit is a per-vault choice, not a fixed gitignore (KJP-15).** The earlier session share-scope split stands — `sessions/` sits outside the team-shared surface; what changes is the premise that a gitignore was its *sole* implementation. The gitignore is now framed as the team-vault implementation only — a solo vault may track sessions (the commit doubles as backup). The plain-uid (never `[[wikilink]]`) rule for shared notes is unchanged, re-grounded on "a session may be absent from the shared surface." Touches `versioning-convention` §Share scope, `knowledge-promotion` reject-log clause, `knowledge-convention` `source_sessions` comment.
- **`ss` project-resolution wording tightened (KJP-15).** Step 2 said "infer from cwd"; it now reads "config `project:` first, then cwd", matching `project-inference.md` §Priority (config outranks cwd). Behavior unchanged — wording only.
- **recall grep priming now ranks deterministically with a hard cap (KJP-4).** The unranked "pick relevant + eyeballed 5–8" is replaced by a weighted-sum ranker (`10·section + 5·confidence + 4·overlap + 1·priority`, recency as strict tiebreaker) capped at `RECALL_N` (default 8). Shell/awk integer arithmetic, zero deps, deterministic per `(note, goal)`. Requires a `GOAL` env var (guarded — no silent failure). `confidence`/`priority` terms are forward-compatible (0 until the schema persists them).

### Added
- **`MILESTONE` document kind + new `docs/planning/` axis (KJP-17).** A roadmap document — **"when what"**: sequences already-defined scope across time. kind `milestone` · owner `pm` · tier `situational` · **not part of the 19-stub baseline** (created on trigger, like ADR/feature docs). Home is a new `docs/planning/` axis (a roadmap spans tech+business, so it sits above the tech/business split, not inside either). Boundaries pinned so it never duplicates neighbors: references PRD/FRD scope (never restates), links ADRs for the *why* (never records decisions), leaves fluid task/sprint status to the tracker (`docs/planning/` = durable plan). Touches `doc-catalog` (new group + boundary note), `vault-tree` (`docs/planning/MILESTONE.md` + naming example).
- **First body-template canon `doc-templates.md` (KJP-17).** Before this, only the frontmatter standard existed — no body-section templates. New file establishes the minimal skeleton (templates are copyable body skeletons living in this one canon file, not separate `.md`s; frontmatter is never repeated) and defines exactly two: `DESIGN` and `MILESTONE`. 🔴 Deliberately scoped to those two — the other ~22 kinds get **no** template, because a stub pre-filled with empty headings reads as false facts; a kind earns a template only when its body shape is stable. Restates the stub rule for templated docs (empty heading = "not yet written", flip to `draft` on first content). Linked from `project-docs-convention` header.
- **`kj-plugins` `docs/` tree decision reversed (KJP-17).** The 2026-07-16 decision to keep no `docs/` tree for this project is overturned (user, 2026-07-23) — the tree is created, but seeded with only what is needed now (MILESTONE), not all ~24 kinds as stubs. The vault-side creation (`013_kj-plugins/docs/`) and the `CLAUDE.local.md` Router line are PM follow-ups (vault write + shared-checkout config, both outside the plugin repo).
- **`update-over-create` names its tooling in canon (KJP-15).** `knowledge-convention` now states edit-existing-with-`Edit` / `Write`-only-for-new at the canon layer, so the scribe procedure's tool rule (`knowledge-promotion`) reads as canon-derived rather than a lone skill-level instruction.
- **Glossary anchoring in knowledge promotion (KJP-5).** The scribe promotion brief now attaches the existing-note title list (project `[K]` + common `[C]`) that recall's grep priming already emits, and instructs verbatim reuse for `title`/body/`related:` — guarding `related:` links against silent forks (`Cloudflare Tunnel`/`cf tunnel`/`CF Tunnel`). No new scan, no fuzzy/uuid layer.
- **Progress entry status-suffix convention (KJP-20).** `sessions-note-convention` now regulates the `### YYYY-MM-DD (suffix)` heading as a statusline contract: closed set `(parked)`/`(completed)`/none, one status token (round-count `#N` allowed after), vocabulary capped under 5, non-status descriptions barred from the suffix. Parser falls back to `active` on an unknown suffix. `sh`/`sc` carry canon pointers.

## 0.1.2 — 2026-07-18

### Added
- **`scripts/validate.sh` — schema checker (KJP-1).** Was: zero mechanical validation, every rule enforced by documentation discipline alone. Checks session uid shape + uid/filename agreement, the 3-value `status` vocabulary, required frontmatter keys, knowledge-note `title:`, and doc-status values leaking into sessions. Reports `file:line`, warns by default, `--strict` exits 1. bash 3.2, no dependencies beyond `awk`/`find` and coreutils. `scripts/validate-selftest.sh` covers it with 39 assertions; the suite is mutation-tested (16/16 mutants killed) because a passing assertion is not evidence that it can fail.
- **Human sign-off gate for `common/policies/` (KJP-2).** The normative axis — top of Document Conflict Precedence — no longer gets written by an agent. Candidates land on their normal tier and return as a `common-policy candidates` list, presented as one batch at `sh`/`sc`/dreaming §7. Agents draft; the user signs. Per-item interrupts are forbidden, and every other tier keeps the automatic score gate unchanged.
- **Third-time test (KJP-3).** dreaming §3's reject-log recurrence scan now states the rule it was implying: two occurrences make the third a rule. Detection and proposal only.

### Changed
- **`dreaming` §5 `project-policy candidates` renamed.** Both axes (org-wide `common/policies/` and per-project `docs/policy/`) surface approval-pending candidates in `## For PM` under near-identical wording — a signer could approve a project policy as an org-wide one. The two lists are now named apart and must never be merged.
- **`knowledge-promotion.md` reject-log clause** points at dreaming §3 as canon instead of describing the scan, which had made this file read as the spec.

### Notes
- Session `#### Mistake` recurrence was evaluated for inclusion in the third-time test and **deferred, not dropped**. The blind spot is real — a Mistake never mirrored into `Learned` escapes both the score gate and the reject-log — but sessions are an incremental scope and are records that carry no handled-marker, so recurrence there can neither be counted nor closed out. Record-format work has to land first (KJP-9). The reasoning is recorded in dreaming §3 so it is not rediscovered and re-attempted.

### Changed
- **ss resume: whole-file Read → awk section extract.** Resume injected the entire session file (worst case 94,948 B ≈ 31.6k tokens); it now extracts only `## Goal` + `## To-Do-List` + the newest Progress entry. Measured across 8 active sessions: 222,979 → 58,202 B (−74%).
- **Sessions split from the team share scope.** The shared surface is project trees (`NNN_*/`) + `000_common/`; `sessions/` is a gitignored personal episodic log. Shared notes reference sessions as plain uid text — never `[[wikilink]]` (dangles in teammates' vaults):
  - `versioning-convention`: new **Share scope** bullet (git canon)
  - `knowledge-convention`: wikilink ban noted on `source_sessions`
  - `knowledge-promotion`: `0.rejected.md` format `[[uid]]` → plain uid
  - `vault-tree`: `sessions/` annotated local-only

## 0.1.0 — 2026-07-16

- Initial release: session lifecycle skills (`ss`/`sh`/`sc`), `init`/`onboard`, `dreaming`, worker/coder/verifier agent profiles, vault conventions (`docs/`), recall + knowledge promotion procedures.
