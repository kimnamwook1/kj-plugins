# brain 0.3.0 설계 전서 (2026-08-13)

> - 근거 사슬: `전서.md`(0.2.5 실측) → `전서_토론후.md`(4자 회담 + 사용자 확정) → 본 문서(백지 재설계).
> - §1 저장 구조는 R4 패널 검증 통과(이의 2건 — 식별자 2키 분리·ss 유일성 검사 — 채택 반영).
> - 전제: 볼트 백지 리셋 · 훅·스크립트 전부 신규 작성(기존 코드 재사용 없음) · 기존 스킬 문서도 전부 재작성.
> - 원칙 5: ①선별 주입(상한) ②PM 직접 쓰기 ③단일 원본 ④fail-visible(0도 0이라 말한다) ⑤명시 선언(경로·이름에서 자동 파생 금지).

---

## 1. 저장 구조

### 1.1 볼트 — 2층 평탄

```
<vault>/                      # 볼트 루트 = org 경계 (techtainment / beafter 볼트 분리)
  sessions/                   # 세션 노트 — git 밖(볼트 .gitignore), 휘발층
  memory/                     # 전 지식 단일 트리 — 폴더 계층 없음
    _index.md                 # 공용 인덱스 1파일
```

- 폐지: `projects/NNN_slug/` · `p_memory/` · `neocortex/` · 공통층(`org/`·`000_common/`) · `999_tools/` · `.brain-paths` · 번호 밴드.
- 구분축은 폴더가 아니라 frontmatter(`scope`·`kind`).

### 1.2 memory 노트 스키마

```yaml
---
summary: <언제 + 주장 — 검색 트리거가 되는 한 문장>
scope: [kjp]          # 프로젝트 식별자 목록. org = 회사 지식. 명시 선언만
kind: fact            # fact | policy — 생략 시 fact
updated: YYYY-MM-DD
---
## Insight   ← 주장·결론
## Why       ← 근거 (파일:줄·명령 출력·숫자·URL)
```

- 절은 2개(사용자 확정 2026-08-13 — Trigger 절 폐지): 트리거는 **summary가 겸한다**. summary = "언제(트리거) + 주장" 두 성분 필수 — brain-recall·recall의 매칭 재료가 summary 한 곳에 모임. validate는 절 부재 대신 summary 부재·트리거 성분 결손을 잡는다.
- 본문 작성은 **개조식 bullet만** — 줄글(산문) 금지 (사용자 확정 2026-08-13). 한 줄 = 한 사실. 세션 노트(Progress·To-Do 등) 포함 볼트 기록 전반에 적용 — 주입·recall 토큰 절감 목적.
- kind 판별: **위반할 수 있으면 policy, 틀릴 수 있으면 fact.** 교훈·절차→fact, 취향→policy. 제3종은 실측 필요 시 어휘 1줄 추가.
- 파일명 = 토픽 kebab (`obsidian-cli_silent-fork.md`). 프로젝트 접두 없음 — 프로젝트는 scope가 안다.
- 승격 ②(교차) = scope에 식별자 추가 1줄 편집. git mv·링크 재배선 없음. scope 2+ = 구 neocortex 상당.
- `_index.md` 줄 형식: `- [[<stem>]] (kjp, org) — <summary>` — recall이 frontmatter 안 열고 인덱스 grep으로 필터.

### 1.3 세션 노트 스키마

```yaml
# sessions/kjp_20260813_rebuild.md   ← 파일명 = <id>_<YYYYMMDD>_<slug>.md, 파일명이 곧 식별자
---
status: active        # active | parked | done — 쓰기 권한: ss=active 생성 · sr=active 복원 · sh=parked · sc=done
project: kjp          # 프로젝트 식별자 (scope와 같은 어휘)
updated: YYYY-MM-DD
related_ticket: KJP-12   # 없으면 빈 값
cc_session_ids: [<uuid>] # 최신을 앞에 prepend
---
## Goal       ← 한 줄
## Recall     ← ss가 주입한 결과 기록 (파일 수·바이트 포함)
## To-Do      ← bullet — 남은 일·pending 마커
## Progress   ← 최신 엔트리가 맨 위. bullet만, 줄글 금지
```

- 유일성(R4 grok 이의 채택): ss는 생성 전 동일 파일명 존재 검사 — 존재하면 자동 접미사 금지, 사용자에게 구분 slug 질문. sessions는 git 밖이라 덮어쓰기 = 복구 불가.

#### 4절 작성 양식 (고정 — 에이전트 자유 서식 금지, 정본은 canon `memory.md`)

```markdown
## Goal
빌드 검사기 3종을 0.3.0 스펙으로 신규 작성          ← 한 줄만. 바뀌면 덮어씀(이력은 Progress가)

## Recall
- 3건 · 2,410B 주입 (scope: kjp) · 절단 0건        ← ss가 계수 라인 1줄
- [[obsidian-cli_silent-fork]] · [[plane-cli_uuid]]  ← 주입한 노트 stem 목록

## To-Do
- [ ] brain-check.sh 작성
- [x] 스키마 확정
- [ ] ASK: 8KiB 상한 값 재확인                      ← 사용자 입력 대기는 ASK: 접두

## Progress
### 2026-08-13 15:30 (parked)              ← 헤딩 = 날짜 시각 (started|resumed|parked|completed) — 시각 필수
- done
  - brain-check.sh 골격 작성 — brain/hooks/brain-check.sh    ← 산출 경로는 done 줄에 포함
  - selftest 픽스처 볼트 생성
- learned
  - PostToolUse stderr+exit 2 만 모델 피드백 채널
  - matcher에 NotebookEdit 누락 실수 → 추가로 복구            ← 실수도 여기 — `실수 → 복구` 형태 의무
- next
  - selftest 픽스처부터                    ← 파킹 시 필수 — 하위 1줄만(재개 첫 행동)
```

- Progress 엔트리 = **중첩 카테고리 3종**(사용자 확정 2026-08-15): `done` `learned` `next`. 카테고리 줄은 `- done` 고정 표기(콜론·부가 텍스트 금지 — 파서 앵커), 사실은 하위 bullet. 같은 카테고리 반복 금지(하위에 줄 추가). 빈 카테고리는 줄 생략. 순서 고정: done → learned → next. `next`는 하위 1줄만.
- 카테고리 소비자: done=sr 맥락 / learned=sc 승격 수확 / next=sr 재개 — 소비자 없는 카테고리 추가 금지. 산출 경로는 done 줄에 포함(output 칸 폐지). 실수는 learned 하위에 `실수 → 복구` 형태 의무(mistake 칸 병합) — 실수 은폐가 실측되면 mistake 재분리(validate 어휘 1줄).
- Progress 헤딩 = `### YYYY-MM-DD HH:MM (started|resumed|parked|completed)` — **시각 필수**(하루 다중 파킹·재개의 순서 보장).
- **sr 주입 = Goal + 미완 To-Do(`- [ ]`)만 + 최신 Progress 엔트리 1개 — 그 외 절대 미주입.** 과거 엔트리는 저장만(주입 비용 0), 필요 시 on-demand Read.
- 양식 정본 = canon `memory.md` 한 곳. 강제 2단(확정 2026-08-15): ①생성 시점 — ss·sh·sr·sc는 작성 전 `memory.md`의 양식 절을 Read하고 그대로 따름, 스킬 문서에 양식 사본 금지(포인터만 — 사본 4개 = 드리프트) ②사후 — brain-validate가 헤딩·접두 어휘 검사.
- 절별 쓰기 주인: Goal·Recall=ss / To-Do=수시(PM) / Progress 기계(스킬) 쓰기=sh(parked)·sc(completed)만(확정 2026-08-15) — ss·sr은 Progress 미기록(세션 파일 생성·cc_session_ids prepend가 각각 시작·재개 증거) · 헤딩 어휘 4값 유지, started·resumed는 사람 수동 기록용.

### 1.4 프로젝트 식별자

- `project: kjp`(소문자) — 세션 파일명·scope 담당. `ticket-prefix: KJP`(대문자) — `related_ticket` 표기·검증 담당. **두 키 각각 명시 선언**(R4 codex 이의 채택 — 대소문자 자동 변환은 명시 선언 원칙 위반).
- 출처는 AGENTS.md brain config. 경로 파생 금지(claude-mem 파편화 4건 실측 근거).

### 1.5 레포 — 문서의 집

```
repo/
  .claude/
    settings.local.json
    skills/
      .gitkeep
    hooks/
      .gitkeep
    rules/
      .gitkeep
  AGENTS.md                   # 원본 안내 (§3.1)
  CLAUDE.md                   # 같은 내용 실작성 (§3.1 — 마커 블록 동일)
  CLAUDE.local.md             # vault-root: 1키 — gitignored
  docs/
    research/
      .gitkeep
    business/
      PRD.md
      GTM.md
      COMPLIANCE.md           # §Legal Sources 표 필수 (§5.1)
      MILESTONE.md
    develop/
      ARCHITECTURE.md
      INFORMATION_ARCHITECTURE.md
      CODE_CONVENTION.md
      RUNBOOK.md
      THREAT_MODEL.md
      DESIGN.md
      policy/               # 프로젝트 및 기능 개발에 필요한 정책 
        POL-0000N-<slug>.md # 예시 POL-00001-login.md
      feature/
        FEAT-0000N-<slug>.md    # §FRD §TDC 
    adr/
```

- 스텁 사전 생성 없음 — "pre-created ≠ evidence". 문서는 트리거 시(onboard 답변·기능 착수) 생성.
- POL·FEAT 파일명 규칙(사용자 확정 2026-08-15): **ID부 대문자+dash+숫자 5자리 · slug 소문자 kebab** — `POL-00001-login.md` · `FEAT-00001-checkout.md`. ADR ID(dash) 전례·memory 파일명(소문자 kebab)과 통일.
- API_SPEC 볼트 미러 없음. 멀티레포 = 주 레포 1곳 원본, 나머지는 포인터.
- 볼트↔레포 경계: **코드 PR과 함께 리뷰되면 레포 docs/ · 세션에서 배웠으면 볼트 memory/.** 레포에 지식 파일 금지.

## 2. 지식 흐름

### 2.1 승격 (2게이트 유지, 메커니즘 교체)

- ① sc: 세션에서 남길 것(사용자 정정·AI 자인 실수·재사용 가치 사실) → `memory/<topic>.md` 생성/갱신 + `_index.md` 같은 커밋 append. **PM 직접 Write/Edit** (scribe 폐지).
- ② dreaming: 같은 교훈이 타 프로젝트에서 확인 → scope에 식별자 추가.

### 2.2 recall

- ss/sr 주입 = ①채택 세션 요약(awk 추출) ②`_index.md`에서 현재 프로젝트 scope 행만.
- org·타 프로젝트 행은 "N건 — 요청 시" 1줄 포인터. **하드 상한 8KiB** — 초과분은 절단하고 파일 수·바이트·잘린 수 보고.
- on-demand: `brain-recall <query> [--scope <id>] [-n N]` — summary grep 매칭 top-N 본문 반환(트리거는 summary가 겸함 — §1.2). read-only, bash, 의존성 0. 타 에이전트(codex·grok)의 소비 경로.

### 2.3 dreaming

- 배치 2연산: Refine(사실 불변 정제) · 승격 ②(scope 확장). Link 연산 폐지 — `related` 키는 소비자 없음(recall·brain-recall 모두 안 읽음, 0.2.x에서 YAML 붕괴 354건·재배선 세금만 유발). 노트 연결이 필요하면 본문(Why)에 `[[링크]]`.
- 운영: 볼트 원자 락(`<vault>/.dreaming.lock` — mkdir 원자 획득·종료 시 제거·존재하면 skip·볼트 .gitignore 대상) · 증분 커서(`<vault>/.dreaming-cursor` 1줄 = 마지막 성공 run 커밋 해시 — **git 추적**, 커서 갱신도 1 run 1 commit에 포함해 revert 시 커서 동반 복귀) · 1 run 1 commit(나쁜 run = `git revert` 1개 — worktree 격리 폐지 근거) · 파괴적 변경은 제안-승인 (확정 2026-08-15).
- 트리거: **sc가 실행하지 않는다.** sc는 커서 이후 미통합 커밋 수를 세어 `dreaming 권장 — 미통합 N건` 1줄 제안만. 실행은 사용자 승인 또는 `/brain:dreaming`. 큐·cron 없음.

## 3. 설정 파일

### 3.1 AGENTS.md · CLAUDE.md — 둘 다 실작성, TOC형

- 공용 마커 블록(두 파일 바이트 동일 — 갱신은 스킬이 항상 동시 수행):

```markdown
<!-- brain:begin -->
## brain config
project: kjp         # 소문자 — 세션 파일명·scope
ticket-prefix: KJP   # 대문자 — 티켓 표기
org: techtainment
ticket: plane        # 식별자만 — 크레덴셜 금지

## 필수 규칙
- 지식은 볼트 memory/ 한 곳 — 레포에 지식 파일 금지. 조회: brain-recall <query>
- 문서는 docs/ — 코드와 같은 PR로 리뷰
- 세션: ss 시작 · sr 재개 · sl 목록 · sh 파킹 · sc 종료
- 정책 우선순위: 볼트 memory 노트(kind: policy, scope: [org]) > 레포 docs/develop/policy/POL-* — 하위가 상위를 침묵 오버라이드 금지

## 포인터
- 볼트 구조·노트 양식: ~/.claude/brain-docs/memory.md
- 레포 문서 규약: ~/.claude/brain-docs/project-docs.md
- git 규약: ~/.claude/brain-docs/git-convention.md
<!-- brain:end -->
```

- CLAUDE.md는 같은 블록 + Claude 전용분(있을 때만). 두 블록 드리프트는 검사기가 잡는다(§6).
- `CLAUDE.local.md` = `vault-root: <절대경로>` 1키. Router 없음.

## 4. 스킬 (전부 재작성)


| 스킬         | 하는 일 (0.3.0)                                                                                                                   | 0.2.x 대비 삭제                                          |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- |
| `ss`       | 세션 파일 생성(`active`, 생성 전 유일성 검사 — 충돌 시 사용자에 slug 질문) + recall 주입(8KiB)                                                          | 프로젝트 폴더 생성·scribe 위임·PREFIX 해석                       |
| `sr`       | 열린 세션 스캔→요약 제시→채택. frontmatter 3줄 **PM 직접 Edit** + recall 화면 출력                                                                | scribe 위임                                            |
| `sl`       | read-only 목록 (`[parked]/[active]` · updated 내림차순)                                                                              | 변화 없음(원래 쓰기 0)                                       |
| `sh`       | 파킹 — Park Entry·To-Do·frontmatter **PM 직접**                                                                                    | scribe 위임·볼트 커밋 의식 축소                                |
| `sc`       | 종료(`done`) + 승격 ① **PM 직접** + 문서 라우팅(레포 docs) + dreaming 제안 1줄                                                                 | scribe·동기 dreaming 실행                                |
| `init`     | 질문(vault-root·project id) → AGENTS.md+CLAUDE.md 마커 블록 · CLAUDE.local.md 1키 · 볼트 `sessions/`+`memory/`+`_index.md` · .gitignore | 공통층 5폴더·neocortex·docs 트리·스텁 5·`.brain-paths`·Router |
| `onboard`  | **grill식 인터뷰**(1문 1답 + 추천답 제시 · 코드로 답 가능하면 질문 대신 실측 · 답이 굳는 즉시 해당 문서 lazy 생성/갱신 · ADR은 3중 게이트 · 미결은 plane 티켓 차팅) → 산출은 **레포 docs/** · 환경 실측은 memory 노트(`scope: [org]`) · 문서별 종료 조건(draft 최소 확정) 명시 | 5문 일괄 배터리·티켓 질문(init 이동)·볼트 docs 매핑·공통층 machines 축·tools 층 |
| `dreaming` | §2.3                                                                                                                           | git mv 승격·aliases 재배선·worktree 격리(폐지 — 락+CAS+1 run 1 commit로 대체) |


- 공통 규율: 쓰기는 Write/Edit만(`obsidian create` 금지 유지) · Edit old_string = CAS · vault-root 밖 쓰기 금지 · fail-visible.

## 5. 규약 문서 (canon) — 3종 + 별첨


| 문서                  | 내용                                                                                                                                    |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `memory.md`         | 볼트 2층 구조 · memory/세션 노트 스키마 · scope/kind · 승격 2게이트 · recall 스펙 · dreaming 가드레일                                                        |
| `project-docs.md`   | 레포 docs/ 트리(§1.5)·frontmatter·feature 규약(FEAT 파일 §FRD·§TDC, 도메인 유일 명칭·범용 동작어 금지)·COMPLIANCE §Legal Sources 표·정책 폴더형(POL 파일 — 프로젝트·기능 정책 공용, 사용자 확정 2026-08-15)·우선순위(org > 레포 POL)+override 레코드(상위 ID·사유·만료일+close 보고)·ADR ID |
| `git-convention.md` | 커밋 타입·브랜치·워크트리(`-d`만)·PR 게이트 실측                                                                                                       |


- 별첨(비-canon): `security-audit.md` — verifier 보안 브리프 시에만 Read.
- 전 문서 첫 줄에 소비자 명시. vault-tree.md는 소멸(구조가 §1.1 몇 줄로 끝).
- SessionStart 심링크 `~/.claude/brain-docs` → 플러그인 docs/ 유지 (포인터 안정 경로).

### 5.1 COMPLIANCE §Legal Sources 표 (열 고정)

`규범 | 구분(법률/시행령/시행규칙/고시) | 상태(시행중/제정예정/개정예정/입법예고) | 시행·적용일 | 적용여부(적용/일부/비적용/검토중) | 적용근거 | 조문 | 하위법령 | 확인일 | 근거 URL`

- 비적용도 행으로 남김. 확인일 없는 행 인용 금지. 1차 출처 도구: korean-law MCP.

## 6. 훅·검사기 (신규 작성)

### 6.1 훅 — 2이벤트 최소


| 이벤트                      | 동작                                                                                |
| ------------------------ | --------------------------------------------------------------------------------- |
| SessionStart             | ①canon 심링크 `ln -sfn` ②`brain-check.sh --quiet` — findings만 stdout(통과 무음)          |
| PostToolUse (Edit|Write) | 대상이 AGENTS.md/CLAUDE.md/agents/*.md일 때만 `brain-check.sh` — findings stderr+exit 2 |


- force-delegate류(쓰기 차단) 없음 — PM 직접 쓰기가 정식이므로 존재 이유 소멸.

### 6.2 스크립트 3개


| 스크립트                | 역할                                                                                                                                                                                                                                                                 |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `brain-check.sh`    | 블록 대조 통합 검사기 — ①AGENTS.md↔CLAUDE.md 마커 블록 바이트 동일 ②agents 4종 KERNEL 블록 동일 ③0스캔 = finding                                                                                                                                                                            |
| `brain-validate.sh` | 스키마 린터 — 볼트 모드(세션 5키·status 어휘·Progress 헤딩·카테고리 어휘 3종·memory 4키·kind 어휘·2절(Insight/Why) 부재·summary 트리거 성분 결손·인덱스 정합(dangling+커버리지)·오염 패턴 `\[Image #N\]`·`<!--`) + 레포 모드(docs frontmatter·ADR ID·§Legal Sources 부재·value-axis drift) — **레포 모드가 이전 컷 필수 조건**(검사기 재조준) |
| `brain-recall`      | §2.2 질의 스크립트                                                                                                                                                                                                                                                       |


- 공통 규율: bash 3.2 + POSIX · 의존성 0 · `file:line: message` · 스캔 계수 상시 출력 · selftest(픽스처 볼트+레포) 동반.

## 7. 에이전트 — 4종 유지

- `worker`(범용 — scribe 절 삭제) · `coder`(`isolation: worktree`) · `verifier`(report-only, disallowedTools) · `researcher`(외부 근거, disallowedTools).
- KERNEL 공통 블록 유지(브리프가 스코프 전부 · 실측 우선 · 증거 인용 · Handoff 7절) — 단 "볼트 직접 쓰기 금지 → 산출물은 Handoff" 조항은 유지(볼트 쓰기는 PM만).
- worktree: coder 기본 CC isolation. **Orca가 workspace worktree를 연 세션은 isolation off** — 격리 주인 1개.

## 8. 릴리스·이전

1. 플러그인 0.3.0 작성(스킬·훅·스크립트·canon 전부 신규) → selftest 통과.
2. 볼트 리셋(tar 백업 → 삭제 → init) — techtainment·beafter 각각.
3. 레포 docs 이전과 `brain-validate.sh` 레포 모드는 **같은 컷** (검사기 무음 사망 방지 — 회담 이의 채택).
4. 릴리스 게이트: 설치 캐시 byte 대조(0.2.x 드리프트 재발 방지).

## 9. 미결 해소 (2026-08-15 확정)

- onboard: 5문 배터리 폐지 → **grill식 재설계**(§4 표) — Matt Pocock grilling 방법론 흡수(1문 1답+추천답·실측 우선·lazy 문서 생성·ADR 3중 게이트·미결은 plane 티켓/wayfinder 차용). 외부 스킬 의존 없음(primitive 동반 설치 전제라 배포 취약) · CONTEXT.md 미신설(용어는 PRD·ARCHITECTURE 절로). 근거: `scratchpad/ref-pocock-skills.md`.
- dreaming worktree 격리: **폐지** — 락 + Edit CAS + 1 run 1 commit(revert 가능) + 제안-승인이 대체.
- beafter 적용: **순차** — techtainment 리셋·실사용 1~2주 검증 후 적용. 그동안 beafter 볼트 동결(brain 스킬 미사용).

